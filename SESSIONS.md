# SESSIONS.md — Bảng điều phối các session AI đang làm việc

> **Mục đích:** Nhiều session Claude (nhiều cửa sổ / nhiều thời điểm) cùng sửa repo này.
> File này là "bảng tin" để mỗi session biết **ai đang đụng file nào**, tránh đè code của nhau.
> Đây là **quy ước hợp tác (advisory)**, KHÔNG phải khóa file thật — chỉ hiệu quả khi MỌI session đều ghi vào.

## Cách dùng (cho Claude)
1. **MỖI lần nhận prompt:** ĐỌC file này TRƯỚC để biết session khác đang sửa gì.
2. **TRƯỚC khi sửa 1 file:** kiểm tra file đó có nằm trong mục 🟢 ĐANG LÀM của session khác không.
   - Nếu CÓ → cảnh báo user "file X đang được session khác đụng (theo SESSIONS.md)" rồi HỎI trước khi sửa.
3. **Khi bắt đầu sửa code:** THÊM/cập nhật mục của mình — việc đang làm + **danh sách file đụng tới** + trạng thái.
4. **Khi xong / chờ user commit:** đổi trạng thái ✅ XONG. Mục đã commit hoặc cũ hơn ~2 ngày → user (hoặc chính session đó) dọn.
5. **KHÔNG xóa/sửa mục của session khác.** Chỉ thêm mục của mình.

Trạng thái: 🟢 ĐANG LÀM · ⏸ TẠM DỪNG · ✅ XONG (chờ user commit)

### 🟢 [2026-07-03] MERGE LINEAGE app-src → repo (đưa 45 hàm chỉ-app-src về git) rồi REBUILD — đụng web/index.html, web_app.py
- **YC user:** thấy tab Render THIẾU zoom → phát hiện app-src (app đang chạy/bán) LỆCH repo. User chốt: push code + rebuild.
- **PHÁT HIỆN QUAN TRỌNG:** app-src và repo là 2 LINEAGE song song. app-src có **38 hàm JS + 7 Python KHÔNG ở git** (datTenHangLoat, ghepVideo, doiTenFile, cụm LohaPage db*/kn*, wfAutoMax...) — grep `--all` git = KHÔNG có nguồn → mất là mất luôn. Ngược lại repo có 28 Python + 14 JS app-src thiếu (theo dõi worker, queue bền, ZOOM, gate expired). localize.py: repo có `_bo_manh_trung`, app-src=0 (memory cũ GHI NGƯỢC).
- **Kế hoạch (user chốt "merge từng cụm, verify"):** đưa 45 hàm app-src VÀO repo (repo thành superset) → commit+push → `desktop/build/build_installer.ps1` (stage_app copy repo→desktop\app-src→electron-builder→installer NSIS). Build stage tới `desktop\app-src` KHÁC `Program Files\...\app-src` (app đang chạy) → KHÔNG đụng app đang chạy.
- **Đã trích nguyên văn 45 hàm** ra scratchpad (appsrc_only_js.txt 38, appsrc_only_webapp.txt 7). Subagent đang lập bản đồ CỤM + dependency + xung đột + thứ tự merge.
- **File đụng:** `web/index.html`, `web_app.py` (merge lớn — CHÈN hàm app-src, KHÔNG xóa hàm repo). ⚠️ NHIỀU session đụng 2 file này — tôi CHỈ CHÈN cụm app-src, giữ nguyên code hiện có. CHƯA commit.
- **⚠️ CHƯA build. CHƯA đụng Program Files\app-src.** Chỉ làm trên repo dev.
- **Trạng thái:** 🟢 ĐANG LÀM (chờ bản đồ merge từ subagent).

### ✅ [2026-07-03] FIX "render nhiều video chỉ thấy 1 (bị đè)" + Supertonic ar/hi/ru/id → chỉ edge — đụng web_app.py (CHỈ `_phan_loai_sau_render`), ngon_ngu.py, web/index.html (APP_LANGS)
- **Bug user gặp:** render video 1 (Việt) ra file; render video 2 → KHÔNG thấy 2 file, chỉ thấy bản mới nhất (bị ĐÈ). Tab Render (xu_ly_chon).
- **Root cause:** `xu_ly_chon.py:134-137` ĐÃ chống trùng khi tạo `_xuly.mp4` cạnh gốc (ra ' (2)'). NHƯNG `web_app._phan_loai_sau_render` MOVE output vào folder thể loại bằng `shutil.move(out, target)` với `target=dest/basename(out)` **KHÔNG chống trùng** → 2 video ra cùng basename `_xuly.mp4` + phân loại về CÙNG folder → đè.
- **Fix (web_app.py, trong `_phan_loai_sau_render` trước `shutil.move`):** thêm `while os.path.exists(target)` → tích luỹ `<ten>_xuly (2).mp4`,`(3)`... TRONG folder thể loại. Vị trí `(N)` SAU `_xuly` (khác `xu_ly_chon` đặt TRƯỚC) — KHÔNG va `_output_xuly_moi_nhat` (nó quét thư mục GỐC, không quét folder thể loại). Sidecar .txt caption dùng `splitext(target)` → tự khớp tên mới.
- **Verify:** py_compile OK; test cô lập 3 video cùng basename → ra 3 file (không mất), nội dung nguyên vẹn; `_output_xuly_moi_nhat` vẫn nhận đúng ' (N)_xuly.mp4' cạnh gốc. ✅ DEPLOY app-src (vá surgical UAC, app-src khớp repo tại đoạn này, chỉ CHÈN khối chống-đè). CHƯA commit repo.
- **+ Supertonic ar/hi/ru/id → supertonic:false (chỉ edge):** ngoài nhóm lõi Supertonic đọc ra English-ish. Sửa `ngon_ngu.LANGS` + `APP_LANGS`, xóa 40 MP3 nghe-thử (repo+app-src còn 124). [[supertonic-chi-bat-ngon-ngu-loi]]
- **File đụng:** `web_app.py` (CHỈ `_phan_loai_sau_render` ~3483 — session Supertonic sở hữu hàm này, chỉ THÊM chống-đè, KHÔNG đụng logic phân loại/caption), `ngon_ngu.py`, `web/index.html` (APP_LANGS). **User TẮT/MỞ LẠI app** để hiệu lực.
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-07-03] PUSH + đồng bộ app-src (commit 0634014) — vá gate Gemini + retry-Gemini còn thiếu ở app-src
- **YC user:** push code lên git (1 commit gộp cả cụm) + đồng bộ lại app-src từ git.
- **Đã push:** commit `0634014` (đa ngôn ngữ 14 + Supertonic + fix render đè + 124 MP3 + .gitignore bỏ _render_profile.jsonl). `origin/feat/dang-bai-longtieng-en`.
- **Đồng bộ app-src — phát hiện 2 fix THIẾU (đã có trên git, chưa vào app-src) → VÁ SURGICAL UAC:**
  (1) `dich_gemini_web.py:313` còn gate CŨ `if tgt not in ("vi","en","ko")` → Thái/Nhật/Pháp ra tiếng VIỆT. Vá → `ngon_ngu.HO_TRO` (mirror phu_de). (2) `localize.py` THIẾU khối retry-Gemini câu sót (~55 dòng: `_thieu_vi`, retry mini-srt, `VC_GEMINI_SAP_PCT`, Google chỉ khi Gemini sập). Vá bằng anchor code (không lệ thuộc dấu) + dry-run trên bản copy trước (code retry == repo, bỏ comment) → UAC.
- **Verify:** app-src compile in-memory OK; bảng đồng bộ 10/10 fix ✓ + 124 MP3 + supertonic_synth. Supertonic LẪN retry cùng tồn tại (không cái nào bị patch xóa).
- **⚠️ ĐÍNH CHÍNH memory/SESSIONS cũ:** `_bo_manh_trung` KHÔNG có ở app-src (repo MỚI có). Backup .BEFORE 14:25 cũng =0 → app-src CHƯA TỪNG có. Ghi cũ "app-src có _bo_manh_trung repo không" là NGƯỢC/SAI. [[deploy-appsrc-lech-nguon-repo]]
- **Model:** đã kiểm — Whisper medium + Supertonic-3 (385MB, ~/.cache/supertonic3) + Kokoro/Piper/OmniVoice/VieNeu ĐỀU đã tải. KHÔNG thiếu model. (Whisper large chưa tải nhưng app mặc định medium.)
- **File đụng:** app-src `dich_gemini_web.py`, `localize.py` (chỉ vá 2 fix trên). Repo đã push. **User TẮT/MỞ LẠI app** để retry-Gemini + gate mới hiệu lực.
- **Trạng thái:** ✅ XONG.

### ✅ [2026-07-03] SCAN-CACHE tách ngôn ngữ đích (đổi ngôn ngữ KHỎI quét lại OCR/whisper) — đụng localize.py
- **YC user:** đổi ngôn ngữ đích thì "srt scan dùng lại bình thường" — hiện `_srt_key` (kèm tgt) lưu CẢ `.zh.srt` (scan nghe/OCR) LẪN `.vi.srt` → đổi tgt = MISS = quét lại whisper/OCR (phần NẶNG CPU) dù bản nghe tiếng Trung KHÔNG đổi theo ngôn ngữ đích.
- **Làm (localize.py, vùng cache 2646 + else-block ASR):** thêm `_scan_key = tinh_key("scan", _vhash, model, src, tach_truoc, e_asr, e_ocrfps, e_funseg, e_destutter, e_cheocr)` — TÁCH khỏi tgt/engine-dịch. Trong else-block: lookup `_scan_key/.zh.srt` TRƯỚC → HIT thì nạp `segs` (zh đã dedup), `dich.huy()`+`dich=None`, BỎ `asr_segments`+dedup, đi thẳng dịch; MISS thì ASR+dedup như cũ rồi `luu_noi_dung(_scan_key, ".zh.srt", <srt>)`. `dich_lai` (Render từ đầu) bỏ qua lookup. Blur-box đã có cache riêng (`boxes`, cũng tách tgt). `_srt_key` (vi.srt cùng tgt) VẪN check trước = full-skip; scan-cache chỉ khi vi.srt MISS.
- **An toàn:** đổi model/engine-ASR/tách-nhạc/OCR-fps → key scan khác → quét lại đúng (không reuse nhầm scan whisper khi user chuyển OCR). Thay đổi THUẦN THÊM đường HIT, MISS giữ nguyên hành vi cũ.
- **Verify (render THẬT):** đường HIT xác định (pre-seed scan-cache 1 zh.srt giả) → `chay(src=zh,tgt=en)` HIT + **KHÔNG chạy ASR** + dịch `你好世界`→"Hello World" (sạch chữ Trung, 3 cue); `chay(tgt=vi)` HIT LẠI cùng scan → "Xin chào thế giới" (khác EN). Đường MISS (cache rỗng) → không HIT nhầm. Format lưu = doc_srt parse được (round-trip OK). py_compile OK. CHƯA test full round-trip real-ASR-save (clip test whisper ra 0 cue — vấn đề clip, không phải code; SAVE trivial + format đã verify).
- **File đụng:** `localize.py` (CHỈ vùng cache/ASR 2646-2760 — KHÔNG đụng Supertonic WIP ở 1798/2084/3073+/3300). CHƯA commit. localize=subprocess load fresh → không cần restart; cache cũ chưa có scan-key → lần render tiếp mới tạo scan, đổi ngôn ngữ SAU đó mới HIT.
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-07-03] FIX gate ngôn ngữ hardcode ở dịch Gemini (Thái/Nhật/Nga... ra phụ đề VIỆT) — đụng dich_gemini_web.py
- **Bug user gặp:** chọn đích Tiếng Thái → phụ đề ra tiếng VIỆT. Nghi cache — thực ra KHÔNG (cache key có tgt, đã verify). Gốc: session Supertonic thêm 11 ngôn ngữ vào `ngon_ngu.LANGS` (14 tổng: +fr/es/pt/th/de/it/ja/ru/id/hi/ar) + prompt Gemini data-driven (dòng 390-407), NHƯNG SÓT gate cứng `dich_gemini_web.py:313` `if tgt not in ("vi","en","ko"): tgt="vi"` → mọi ngôn ngữ ≠ vi/en/ko bị ép về vi TRƯỚC khi tới prompt → dịch ra Việt. (Ảnh hưởng CẢ 11 ngôn ngữ mới, không riêng Thái.)
- **Fix (mirror phu_de._dich_vi ĐÃ đúng):** đổi gate 313 sang `try: import ngon_ngu; if tgt not in ngon_ngu.HO_TRO: tgt="vi"` (fallback hardcode chỉ khi import lỗi). CHỈ 1 chỗ. `phu_de.py:62` KHÔNG đụng (đã đúng — gate cứng ở đó nằm trong `except`, đường thường dùng HO_TRO).
- **Verify:** py_compile OK; test logic 14/14 ngôn ngữ (gate KHÔNG ép vi + prompt {L} ra đúng tên: th→Thai, ja→Japanese...). CHƯA test live Gemini dịch Thái thật (cần Chrome+mạng) — nhưng prompt data-driven đã chạy cho en/ko.
- **CÒN (thứ yếu, cosmetic — đặt TÊN folder/tiêu đề lúc cào):** gate `("vi","en")` ở `doi_ten_kenh.py:81`, `MediaCrawler/tools/dich_ten.py:28`, `douyin/core.py:100` → đích ≠ vi/en đặt tên folder bằng Việt. Chưa fix (fork MediaCrawler + không ảnh hưởng phụ đề/lồng tiếng).
- **File đụng:** `dich_gemini_web.py` (CHỈ gate 313 — vùng Supertonic WIP, chỉ sửa đúng gate họ sót). CHƯA commit. Cần deploy CÙNG cụm Supertonic (ngon_ngu/localize/phu_de/index.html) mới hiệu lực.

### 🟢 [2026-07-03] UI: drawer "Tiến trình render" vuốt ra/vào (#vcpd, additive) — đụng web/index.html
- **YC user:** nhúng giao diện progress vuốt ra/vào (đã duyệt mockup side-drawer) vào app thật.
- **Cách làm (né xung đột):** THÊM 1 khối **tự chứa `#vcpd`** (style+aside+script, prefix `vcpd-`, `window.__vcpd` guard) ngay TRƯỚC `</body>` — KHÔNG sửa markup/JS cũ. Poll `GET /api/render_progress` (pct/step/eta/segs) mỗi 1.2s; drawer phải, flip trái/phải, thu gọn còn thanh + vòng %; stepper 6 pha theo `pct` (dùng `_pct_tu_log` mới); feed phụ đề zh→vi từ `segs`. Tự hiện khi có render active, tự mở lần đầu.
- **File đụng:** `web/index.html` (chỉ CHÈN cuối body — KHÔNG đụng vùng xl*/ar*/wf*/vm* của session Supertonic/Theo-dõi/edge-EN). Backend KHÔNG đổi (dùng endpoint sẵn có).
- **Deploy:** đã copy vào app-src `web\index.html` (UAC). Repo cũng đã chèn (working tree, CHƯA commit — index.html còn WIP session khác).
- **Verify:** node --check JS OK; headless render với data giả → drawer đúng (24% OCR, stepper, feed zh→vi). Live cần app F5. Backup: `scratchpad/appsrc_backup` (chưa gồm index.html — dùng git repo/backup app-src cũ nếu cần rollback).
- **Trạng thái:** 🟢 ĐANG LÀM (chờ user F5 test app thật).

### ✅ [2026-07-03] Render: thêm CROP/ZOOM tĩnh (phóng to, cắt mép) — đụng xu_ly_video.py, xu_ly_chon.py (LÕI, không session khác) + web_app.py, web/index.html (CHỈ block zoom cô lập)
- **YC khách:** render phóng to/thu nhỏ video. Chốt: **zoom TĨNH** (thanh trượt 1 mức, 100%→150%, phóng to = cắt mép + đổi khung chống bản quyền, giữ độ phân giải).
- **Thiết kế:** zoom = `scale=iw*Z:ih*Z,crop` (giữ resolution) = 1 vf filter. NHƯNG che-chu (blur chữ Trung) toạ độ lấy từ frame GỐC → KHÔNG nhét vào extra_vf (lệch blur, như lưu ý mirror). → **Zoom là pass reframe TRƯỚC localize** (force pre-encode như "cắt đầu/cuối"): `xu_ly_chon` đổi `if co_cat:` → `if co_cat or co_zoom:`, `_cfg_hinh` set `cfg["zoom"]`; `xu_ly_video.dung_lenh_ffmpeg` thêm zoom vào vf (đầu chain). localize chạy trên khung ĐÃ zoom → dò/che/burn đúng. **KHÔNG đụng localize.py** (session khác đang sửa).
- **web_app `_lenh_xu_ly`:** thêm `--zoom` (2 dòng cô lập). **index.html:** thêm thanh zoom trong tab Render (block riêng, không đụng vùng giọng xl*/ar*/vm* của session khác).
- **File đụng:** `xu_ly_video.py`, `xu_ly_chon.py` (lõi, tự do) + `web_app.py`, `web/index.html` (chỉ thêm zoom). render_worker chạy xu_ly_chon.main() in-process → `--zoom` tự chạy cả 2 đường.
- **✅ XONG + ĐÃ PUSH:** backend `f6ba657` (xu_ly_video/xu_ly_chon); glue+UI `5e3bfd7` (web_app `--zoom` + index.html thanh Phóng to tab Render `xlZoom` + Tự động `arZoom` + đọc zoom trong xlRun/arOpts/wfRenderOpts = CẢ 3 đường Render/Quy trình/Tự động). Stage **theo hunk** (git apply --cached) → KHÔNG đụng WIP Supertonic/đa-ngôn-ngữ trong 2 file chung; blob staged verify compile+JS OK. CHƯA deploy app-src (chờ dịp gộp cùng WIP session khác).

### 🟢 [2026-07-03] ĐA NGÔN NGỮ ĐÍCH (~14): tổng quát hoá — Edge (mọi ngôn ngữ) + Supertonic (offline, ngôn ngữ nó có sẵn) — đụng ngon_ngu.py, phu_de.py, dich_gemini_web.py, localize.py, web/index.html
- **YC user:** thêm nhiều ngôn ngữ đích (Pháp/TBN/BĐN/Thái + phổ biến). Chốt: TỔNG QUÁT HOÁ ~10-15 ngôn ngữ. Engine: Edge cho tất cả + **Supertonic cho ngôn ngữ NÓ CÓ SẴN** (bỏ MeloTTS — Supertonic đã phủ offline fr/es/pt/de/it/ja/ru...). Thái không có ở Supertonic → edge.
- **Gate feasibility:** Supertonic synth THẬT OK fr/es/de/pt/ja/ru (test); edge 23 giọng mặc định đều hợp lệ; MeloTTS BỎ (khỏi torch/venv).
- **Ngôn ngữ (14):** vi/en/ko (có) + fr/es/pt/de/it/ja/ru/id/hi/ar (Supertonic+edge) + th (edge only). Supertonic AVAILABLE_LANGUAGES phủ 31, dùng subset.
- **Kiến trúc:** `ngon_ngu.LANGS` = bảng {ten, ten_en, google, edge mặc định, edge voices, supertonic bool}; HO_TRO/_GOOGLE/_VOICE suy từ bảng (backward-compat). Prompt `dich_gemini_web.dich_srt`: GỘP nhánh en/ko + ngôn ngữ mới thành 1 nhánh PARAMETRIC theo tên ngôn ngữ ({LANG_NAME}); vi giữ riêng (glossary Hán-Việt). `phu_de._dich_vi` mở gate. localize dispatch supertonic ĐÃ parametric theo tgt (không đổi). UI: dropdown + picker giọng data-driven.
- **⚠️ ĐA SESSION:** dich_gemini_web.py + localize.py CHUNG session "Gemini dịch"/"Bỏ Google bù" — họ đụng hàm `_ghi`/khối dịch, KHÁC vùng tôi (dich_srt prompt / TTS dispatch). index.html chung session "Theo dõi kênh" (vm*). Chỉ sửa vùng mình.
- **MeloTTS: BỎ (user chốt sau đo).** Test THẬT: 40s/câu CPU (Supertonic ~2s = nhanh 20×) + footprint ~4GB (torch+unidic+BERT/lang). Không thực tế cho sản phẩm bán; Supertonic đã phủ cùng ngôn ngữ. → chỉ Edge + Supertonic.
- **✅ VERIFY:** compile 4 py OK; JS 2/0; dịch THẬT zh→fr/es/th/de/ja OK (Google); lồng tiếng PHÁP qua localize (supertonic lang=fr) OK; DOM mock **14/14** (fr→supertonic+edge, th→edge-only, en/ko→supertonic mặc định, vi không vỡ). EN mặc định ĐỔI edge→supertonic (offline-first; đổi lại 1 dòng nếu cần).
- **✅ ĐÃ DEPLOY app-src (5 file, UAC):** ngon_ngu/phu_de = copy repo (diff=chỉ thay đổi tôi); dich_gemini_web (splice prompt parametric)/localize (2 startswith→count("-")>=2)/index.html (splice UI block + _fillLangDropdowns + arUpd non-vi + preview) = VÁ SURGICAL trên bản app-src (normalize CRLF→LF cho index). Verify: app-src==staged, HO_TRO=14 ngôn ngữ, KHÔNG regress (index wfAutoMax/goNangCap giữ; localize KHÔNG kéo _bo_manh_trung repo). Backup: `Desktop/_backup_appsrc_multilang/`. CHƯA commit repo.
- **✅ + FILE CỨNG NGHE THỬ (2026-07-03):** pre-tạo **164 MP3** (`web/giong_thu/<lang>_<engine>_<voice>.mp3`): Supertonic 130 (13 ngôn ngữ×10 giọng) + Edge 34 (14×giọng), ~10MB. `xlNgheThu` sửa: HEAD-check file cứng → phát TỨC THÌ (không synth/mạng); thiếu/omnivoice-piper → fallback synth. `web_app`: route `/giong_thu/` + ctype .mp3 vào `_serve_static`. Test: serve mp3 HTTP200 audio/mpeg. Deploy app-src (web_app+index.html VÁ SURGICAL + xcopy 164 mp3, UAC; KHÔNG regress). Backup: `Desktop/_backup_appsrc_giongthu/`. **User TẮT/MỞ LẠI app** để kích hoạt (đa ngôn ngữ + nghe thử file cứng).

### 🟢 [2026-07-03] Progress render video dài: OCR log mỗi 100 câu + dịch mỗi lô + map % (KHÔNG đứng 1%) — đụng ocr_text.py, dai_sub_rapid.py, web_app.py, dich_gemini_web.py
- **YC user (máy 13900K+3060):** render video 5h "đứng 1%" (đang OCR im lặng). Làm progress trực quan: OCR log mỗi 100 câu, dịch log mỗi lô, thanh % nhích theo.
- **Làm:** (1) `ocr_text.ocr_dong`: bỏ chặn `_PROF` + log "⏳ X/Y khung ETA" mỗi 20s + "📖 Đã đọc N câu · X/Y khung · ETA" mỗi 100 câu + start "📖 Đang đọc phụ đề" + truyền log THẬT vào `phat_hien_hop_dong`. (2) `dai_sub_rapid.phat_hien_hop_dong`: start + "🔎 Dò dải X/Y khung". (3) `web_app._pct_tu_log`: parse "X/Y khung|frame" → dò-dải 6→12, OCR 12→38; "lô k/M" → 38→52 (feed hàng đợi render :3749 + tab dub :703). (4) `dich_gemini_web._ghi`: log "📝 Dịch xong lô k/M (câu a–b)" per lô — stream LIVE vì render queue chạy render_worker (VC_RENDER_WORKER_PROC=1 → dịch in-process).
- **File đụng:** `ocr_text.py`, `dai_sub_rapid.py` (KHÔNG session khác), `web_app.py` (⚠️ CHUNG session Theo-dõi + Supertonic — CHỈ sửa `_pct_tu_log` ~3585, KHÔNG đụng endpoints/render_opts của họ), `dich_gemini_web.py` (⚠️ CHUNG session Supertonic — CHỈ thêm 1 dòng log trong `_ghi`, KHÔNG đụng prompt/dispatch).
- **Verify:** py_compile 4 file + test import web_app gọi `_pct_tu_log` chuỗi log → % nhích 1→6→12→38→52. Live cần app. CHƯA commit.
- **Trạng thái:** 🟢 ĐANG LÀM.

### 🟢 [2026-07-03] Nâng cấp "Theo dõi kênh" (tab Video mới): auto CỨNG 60p in-app + thêm-kênh crawl ra avatar/tên/videos mọi nền + đổi tên — đụng web_app.py, web/index.html
- **YC user:** (1) auto theo dõi hiện dựa Windows task `ToolCaoVideoTheoDoi` → task tự mất sau update/toggle → auto chết âm thầm; user chốt **THAY HẲN bằng worker CỨNG 60 phút trong app, không cần UI**. (2) Thêm kênh hiện chỉ hiện LINK (`lay_kenh_info.py` chỉ hỗ trợ Douyin, bili/xhs/yt trả rỗng) → user muốn **"làm như tab Kênh nguồn"** (crawl ra avatar+tên+videos mọi nền + cho ĐỔI TÊN). (3) Chỉ DÒ video mới (metadata), KHÔNG tải/render.
- **Ràng buộc:** Kênh nguồn bị `_can_lohapage` ẨN với khách chưa mua LohaPage → KHÔNG gộp Theo dõi vào đó (mất tính năng cho free/pro). Theo dõi gate theo TIER (theodoi_max), là tính năng CHUNG → giữ ở tab Video mới, chỉ TÁI DÙNG crawler (không đụng store/gate LohaPage).
- **Làm:** (a) tách `_cao_kenh_meta(platform,link)` từ `_kn_cao_kenh` (crawl `tim_anh creator` → {nick,avatar,sec,items}) — refactor `_kn_cao_kenh` gọi lại (hành vi Kênh nguồn KHÔNG đổi). (b) `theo_doi_config` schema giàu: creators=[{link,ten(rename),nickname,avatar,sec_uid,videos[{id,title,thumb,url,time,da_xem}]}]; endpoints td_add(crawl)/td_rename/td_list/td_seen. (c) `_theodoi_worker` cứng 60p → re-crawl metadata → merge video mới (da_xem=false); bỏ task Windows. (d) UI vm*: add crawl-on-add, đổi tên, bỏ ô interval/on-off, hiện thumbnail metadata.
- **File đụng:** `web_app.py` (+`_cao_kenh_meta`, block `_td_*`+`_theodoi_worker`, endpoints td_add/list/rename/remove/seen/refresh, `_khoi_dong_theodoi` — KHÔNG đụng `_kn_render_opts` 2833 & `_phan_loai_sau_render` 3335 của session Supertonic), `web/index.html` (vm*: vmLoad/vmAddFollow/vmRemoveFollow/vmOpenKenh/vmThumb/vmKey/vmDaXem +vmRename — KHÔNG đụng vùng giọng/TTS xl*/ar*/wf*). Đã kiểm WIP: KHÔNG giao vùng.
- **Verify (chạy THẬT):** py_compile OK; JS validate 2/0; **backend live** (`_cao_kenh_meta` dùng chung + baseline + dò video mới + seen) PASS; **HTTP E2E** (instance test 8899 + CSRF nonce thật → td_add cào ra nick+avatar+30 video, td_list có thumb, rename, refresh dò đúng 2 mới, seen→0, remove sạch, POST thiếu nonce→403) PASS; **DOM-mock 16/16** (vmLoad map; vmRender tên+ten_vi+badge+avatar; vmOpenKenh 🆕+thumbnail metadata+đã-xem-mờ+Đổi tên+Đã xem hết; KHÔNG còn nút render); **merge-preserve** (td_save/td_on cũ KHÔNG wipe videos/ten/avatar) PASS. CHƯA click-through app thật.
- **Coexistence (KHÔNG phá tab Quy trình):** KHÔNG xoá task `ToolCaoVideoTheoDoi` (wfTd còn dùng); `td_save`/`td_on` → `_td_ghi_creators` (merge-preserve). Dead code harmless chưa xoá (theo rule): `vmRenderMot/vmRenderKenh/vmOpen/vmSecFromLink`+`vmFollow/vmCfg` (JS), `lay_kenh_info.py` (Theo dõi không còn gọi). ⚠️ Edge: dùng CẢ Workflow-kenh (td_on) LẪN tab Theo dõi với set kênh KHÁC → td_on rebuild theo set incoming (có thể drop kênh chỉ-Theo-dõi) — chưa xử.
- **CẦN:** user restart app + F5 → test tab "Theo dõi kênh": thêm kênh (ra avatar+tên mọi nền), đổi tên, xem video mới; worker 60p tự dò. Deploy app-src vá surgical [[deploy-appsrc-lech-nguon-repo]]. CHƯA commit.
- **Trạng thái:** 🟢 ĐANG LÀM (chờ user test UI thật + commit).

### 🟢 [2026-07-03] Bỏ Google bù ở dịch Gemini → RETRY GEMINI câu sót (Google chỉ khi Gemini sập) — đụng localize.py
- **YC user:** chất lượng Google dịch không chấp nhận được → bỏ fallback Google trong luồng Gemini; câu Gemini sót → dịch LẠI bằng Gemini. User chốt (AskUserQuestion): giữ Google CHỈ khi Gemini sập cả mẻ (khỏi ra phụ đề tiếng Trung).
- **Sửa (localize.py, khối `elif dung_gemini` ~2817-2833):** sau pass Gemini 1 → gom câu sót → ghi mini-srt chỉ câu sót → gọi LẠI dich_gemini_web (subprocess) → map về đúng chỉ số. Còn sót: Google CHỈ khi got_final < `VC_GEMINI_SAP_PCT` (mặc định 10% = Gemini sập); else GIỮ zh. KHÔNG đụng engine Google người-dùng-chọn (666/2848) hay dich_gemini_web.py.
- **File đụng:** CHỈ `localize.py` (vùng dịch Gemini). ⚠️ localize.py CHUNG session Supertonic/Hàn — chỉ sửa khối DỊCH, KHÔNG đụng dispatch TTS/ngôn ngữ của họ.
- **Verify:** py_compile OK. Live Gemini cần chạy trên app (venv+playwright) — user test UI. CHƯA commit.
- **Trạng thái:** 🟢 ĐANG LÀM.

### 🟢 [2026-07-03] Tích hợp Supertonic-3 TTS + ngôn ngữ đích TIẾNG HÀN (ko) — đụng ngon_ngu.py, supertonic_synth.py(MỚI), localize.py, dich_gemini_web.py, web/index.html
- **YC user:** tích hợp Supertonic-3 (test EN+Hàn OK, chạy nhanh CPU) làm engine; thêm lồng tiếng TIẾNG HÀN; "sửa cả phần prompt" (dịch sang Hàn). (User tưởng picker đã có Hàn — thực tế CHƯA, cả repo lẫn app-src chỉ vi/en → phải THÊM.)
- **License:** code MIT, model OpenRAIL-M (thương mại ĐƯỢC + đòi attribution + cấm mạo danh). Giấu tên UI ("ViralVoice") nhưng cần ghi công ở chỗ khác (About/License).
- **Kiến trúc (mirror Kokoro):** Supertonic = pip `supertonic` (onnxruntime, KHÔNG torch) → nhẹ, ở venv CHÍNH app (khác Kokoro cần venv riêng). `supertonic_synth.py` (worker: texts.json+lang+voice → seg_<i>.wav 24k) chạy qua SUPERTONIC_PY (mặc định sys.executable, env override). Model auto-download HF. 10 giọng M1-M5/F1-F5. 31 ngôn ngữ (en/ko/vi...).
- **Backend:** ngon_ngu HO_TRO +ko; localize +long_tieng_supertonic +dispatch (tgt≠vi & tts=supertonic → supertonic, fallback edge ko-KR); dich_gemini_web +nhánh prompt KO (mirror EN, glossary Hán-Việt KHÔNG nạp); phu_de._dich_vi đã đọc TARGET_LANG → Google ko chạy sẵn.
- **UI:** xlLangTgt/csTarget +Tiếng Hàn; xlApplyLangVoices/arApplyLangVoices +nhánh ko (Supertonic mặc định + SUPERTONIC_VOICES picker).
- **🐛 BUG TÌM KHI RENDER THẬT + ĐÃ SỬA:** (1) `phu_de._dich_vi` chặn cứng `tl not in ("vi","en")→"vi"` → đích ko rơi về VIỆT (Supertonic đọc chữ Việt bằng giọng Hàn = hỏng). Sửa: +"ko". (2) `localize.py` CLI `--tts choices` THIẾU "supertonic" → CLI từ chối. Sửa: +"supertonic". CÒN gate vi/en ở `doi_ten_kenh.py:81`, `MediaCrawler/tools/dich_ten.py:28`, `douyin/core.py:100` = dịch TÊN/tiêu đề lúc CÀO (folder naming), KHÔNG thuộc dub — để sau. [[them-ngon-ngu-dich-nhieu-gate-vi-en]]
- **VERIFY:** py_compile 5 file OK; JS 2/0; DOM mock **15/15 PASS** (ko→supertonic mặc định + giọng M1-F5; ko+edge→ko-KR; vi/en KHÔNG vỡ); e2e `long_tieng_supertonic` synth Hàn OK; **RENDER THẬT 2 clip 30s (Hàn+Anh Supertonic) qua pipeline đầy đủ** → phụ đề Hàn ĐÚNG Hàn, dub khớp, ~66s/clip (nvenc). Demo: `Desktop/Render_test_Supertonic/`.
- **File đụng:** `ngon_ngu.py`, `supertonic_synth.py`(mới), `localize.py`, `dich_gemini_web.py`, `phu_de.py`, `web/index.html`. ⚠️ localize.py + index.html CHUNG nhiều session — chỉ THÊM nhánh ko/supertonic. CHƯA commit (repo).
- **✅ ĐÃ DEPLOY VÀO APP (2026-07-03):** (1) `localize._supertonic_py()` sửa: ưu tiên `.venv_supertonic` ở runtime (mirror kokoro). (2) Tạo `.venv_supertonic` RIÊNG ở `runtime/` (uv, KHÔNG đụng venv chính đang chạy → an toàn numpy) + `pip install supertonic` + **pre-warm model** (tải sẵn). (3) Deploy 6 file vào `app-src` qua UAC: ngon_ngu/phu_de/dich_gemini_web/supertonic_synth = copy repo (diff=chỉ thay đổi tôi); localize.py + index.html = **VÁ SURGICAL** trên bản app-src (app-src lệch repo: localize.py có code riêng `_bo_manh_trung` KHÔNG có ở repo → không copy đè; index.html giữ wfAutoMax/goNangCap). Verify: 6 file byte-khớp staged, app-src KHÔNG regress, deployed localize tự phân giải SUPERTONIC_PY→.venv_supertonic. Backup: `Desktop/_backup_appsrc_supertonic/`. **User TẮT/MỞ LẠI app** để kích hoạt. Attribution OpenRAIL-M ở About (CÒN).
- **+ EN dùng Supertonic + BỎ Kokoro khỏi UI (2026-07-03):** dropdown EN thay `kokoro`→`supertonic` ("ViralVoice — English (offline)"); xlFillVoicePicker EN→SUPERTONIC_VOICES. Backend kokoro giữ (dead code). Deploy repo + app-src (index.html redeploy UAC).
- **🐛 + FIX nút "Nghe thử giọng" LỖI với Supertonic (2026-07-03):** `dub_preview.py` choices + web_app `/api/dub/preview` gate ĐỀU thiếu "supertonic" → chọn giọng Supertonic bấm nghe thử: web_app ép engine→edge + truyền voice "F1" cho edge-tts → edge từ chối → LỖI. Sửa 3 file: dub_preview (+supertonic +--lang +nhánh), web_app preview (+supertonic +truyền --lang), index.html xlNgheThu (+&lang=tgt). Test THẬT: `dub_preview --engine supertonic --lang ko/en` → OUT| wav OK. Deploy app-src (dub_preview copy; web_app+index.html VÁ SURGICAL — web_app app-src lệch repo 1165 dòng). File giọng thử WAV: `Desktop/Giong_thu_Supertonic_WAV/` (20). **User mở lại app + F5.**

### 🟢 [2026-07-03] FIX crash khi click video → "không vào lại được" (GPU video-decode + thiếu crash-recovery Electron) — đụng desktop/main.js
- **Bug khách:** ấn vào video → app CRASH → **"không mở được" NỮA, phải XÓA + CÀI LẠI** mới chạy (restart thường KHÔNG cứu). **CHỈ 1 khách bị** (RTX 3060), khách khác dùng thử KHÔNG bị.
- **Root cause (chốt sau khi user làm rõ):** "restart không cứu, chỉ cài lại được" + "chỉ 1 máy bị" = **poison-state ở ĐĨA đặc thù máy đó**. Máy khách RTX 3060 (GPU MẠNH → KHÔNG phải yếu GPU) — driver NVIDIA cụ thể + hardware VIDEO DECODE Chromium/Electron xung khắc → GPU-process crash khi decode video → **làm HỎNG GPUCache trong userData** → lần mở sau app crash NGAY lúc khởi động ("không mở được") → xoá cache/cài lại mới hết.
- **Fix 3 lớp (desktop/main.js):** (1) `appendSwitch("disable-accelerated-video-decode")` TRƯỚC whenReady — tắt RIÊNG HW video-decode (về CPU), GIỮ GPU cho UI (không phí RTX 3060); `VC_GPU=1` bật lại. = CHẶN GỐC crash. (2) `render-process-gone`→`w.reload()` (chống loop >8s) = recovery in-session. (3) **`child-process-gone` type GPU → ghi cờ `.gpu_crashed`; `healGpuCacheIfCrashed()` đầu whenReady xoá GPUCache/ShaderCache/GrShaderCache/DawnCache khi thấy cờ** = TỰ LÀNH ca "không mở được" (khỏi cài lại). Fix #2 KHÔNG cứu ca này (crash lúc khởi động, trước renderer) → cần #3.
- **Verify:** node --check OK. **CHƯA repro** (đặc thù driver máy khách — khó tái hiện). Fix là hardening đúng lớp lỗi + tự lành, rủi ro thấp, reversible. CẦN copy main.js sang app-src laptop khách + tắt/mở app + click video. **Song song: khách nên cập nhật driver NVIDIA** (fix độc lập nếu đúng bug driver).
- **File đụng:** `desktop/main.js` (đang clean, KHÔNG đụng phần 9router/LohaPage đã commit).
- **✅ ĐÃ COMMIT + PUSH `d6a45f4`.** User chốt (2026-07-03): **CHƯA deploy** — tự quyết cách giao sau. Ghi chú deploy: main.js **nằm TRONG app.asar** (không phải file rời); app.asar đang cài build **Jul 1 = CŨ hơn repo** (còn 9router, thiếu crash-fix + gate LohaPage). Máy dev này (i3/RX560) **≠ máy khách RTX 3060** → session không chạm được máy khách. 2 hướng khi cần: (A) vá app.asar CHỈ crash-fix (giữ 9router, KHÔNG bật LohaPage gate) rồi remote copy đè máy khách = hotfix tối thiểu; (B) rebuild installer từ branch (kèm gate LohaPage — cần server ký cờ `lohapage` trước + gỡ 9router). `build.files` gói `desktop/main.js` → asar root `main.js`.
- **🐛 + FIX bug "Phiên đã hết hạn do ứng dụng vừa khởi động lại" (2026-07-03) — commit `8231a29` push:** user mở tab "File đã tải" → dialog session hết hạn (tab khác không sao). **Chẩn đoán máy THẬT:** 2 cây `ViralCrawl.exe` (25552@16:25:03 + 27712@16:25:08) = **app chạy 2 INSTANCE**; 2 `web_app.py`, port 8770 do backend instance-sau giữ; GET `/api/auth/bootstrap` không k → `bootstrap nonce sai`. **Root cause:** app THIẾU single-instance lock → mở lần 2 → `startBackend` instance-2 `killByPort(8770)` giết backend instance-1 rồi chiếm cổng với BOOT_NONCE mới → cửa sổ instance-1 mồ côi (nonce lệch) → POST đầu (mở File đã tải → `napTieuDe` POST `/api/dich_tieu_de`) 403 → `_csrfFail` (index.html:2020). Tab khác chỉ GET (không cần CSRF) → không dính. **Fix:** `app.requestSingleInstanceLock()` (line 45) → instance 2 `app.quit()` + guard `whenReady` (`if(!_laInstanceChinh)return`); instance chính nhận `second-instance` → focus cửa sổ cũ. node --check OK. Cùng file main.js → deploy CHUNG với crash-fix. **Workaround tức thời cho user:** đóng HẾT cửa sổ app + kill web_app.py mồ côi → mở LẠI 1 lần.

### 🟢 [2026-07-03] Lồng tiếng EN: Edge = MẶC ĐỊNH (biểu cảm hơn Kokoro) + đồng bộ edge-EN vào auto-render — đụng web/index.html
- **YC user:** test giọng EN (Kokoro thiếu cảm xúc) → chốt dùng Edge (Microsoft neural, biểu cảm nhất chạy được trên CPU). "Tích hợp mỗi edge" + "giấu tên model". User chốt: Edge làm MẶC ĐỊNH EN nhưng GIỮ Kokoro/Piper (dự phòng offline) + đồng bộ HẾT các luồng render.
- **Điều tra (đã có sẵn nhiều):** edge-EN ĐÃ tích hợp + đã giấu tên ("ViralVoice — Online (English)") ở tab Render (`xlTts`+`xlEdgeVoice`, `EDGE_EN_VOICES` Aria/Guy/Jenny/Sonia/Ryan); backend `long_tieng_edge` (nhánh else localize.py) + nghe thử đã chạy. `rdTts`/`dubTts` KHÔNG còn trong DOM (File-đã-tải dùng CHUNG panel tab Render). Workflow/Quy trình (`wfRenderOpts`) đọc THẲNG xlTts → tự có edge-EN. → chỉ CÒN `arTts` (auto-render cũ `/api/auto_on`) thiếu bộ giọng EN.
- **Làm (web/index.html):** (1) `xlApplyLangVoices` (3043): EN mặc định `edge selected` (bỏ selected khỏi kokoro) — lợi cả tab Render + Workflow. (2) auto-render: thêm picker `arEnVoice` + `arApplyLangVoices()`/`arFillEnVoice()` (tái dùng const KOKORO_VOICES/EDGE_EN_VOICES/PIPER_EN_VOICES), `arUpd` hiện picker EN khi đích=en, `arOpts` truyền voice EN, hook trong `xlApplyLangVoices` (đổi lang → refresh cả ar) + init. VN giữ nguyên (integratePiperVoices baked lại piper VN).
- **KHÔNG đụng backend** (localize.py else-branch + passthrough voice đã đủ). Giấu tên: mọi nhãn đều "ViralVoice — ...", không lộ edge/Microsoft/Kokoro/Piper.
- **Verify:** JS new Function 2/0; **test DOM mock 14/14 PASS** (EN→edge mặc định + arEnVoice=Aria; đổi engine EN→kokoro/piper nạp đúng list; arOpts truyền voice EN đúng; VN không phá — integratePiperVoices baked lại, picker EN ẩn; tắt dub→ẩn). `xlRestoreCfg` KHÔNG lưu tts → mặc định edge hiệu lực sạch. CHƯA click-through browser thật (không drive headless được ở đây).
- **File đụng:** `web/index.html`. ⚠️ CHUNG nhiều session — chỉ thêm/sửa vùng voice EN (xlApplyLangVoices + auto-render ar*). CHƯA commit (chờ user).
- **⚠️ LỆCH NGUỒN BUILD (quan trọng):** app laptop đang chạy build từ NGUỒN KHÁC repo — `C:\Program Files\ViralCrawl\resources\app-src\web\index.html` (496KB) KHÁC repo feat (479KB) **824 dòng**, có `wfAutoMax`/`goNangCap`/`lock("ai")` mà repo KHÔNG có, và repo có `app-expired` mà app-src KHÔNG có → 2 lineage khác nhau. KHÔNG wholesale-copy repo→app-src (regress mất tính năng). [[deploy-appsrc-lech-nguon-repo]]
- **ĐÃ DEPLOY (vá surgical):** copy app-src ra staging → áp ĐÚNG 5 thay đổi edge-EN (diff sạch, chỉ +edge-EN, giữ nguyên 824 dòng khác) → test staging (syntax 2/2 + DOM mock **14/14 PASS**) → ghi lại app-src qua **UAC elevation** (Program Files read-only; `Start-Process -Verb RunAs`, user bấm Yes). Verify: app-src==staging byte-identical, wfAutoMax/goNangCap/dbTrang còn nguyên. Backup gốc: `Desktop/_backup_index_html/index.html.appsrc_BEFORE_edgeEN`. User cần **Ctrl+F5 / mở lại app** để thấy.

### ✅ [2026-07-03] GỠ HOÀN TOÀN 9router (AI dịch free) khỏi CẢ 2 repo — đụng ai_dich.py, desktop/main.js (phần 9router), desktop/build/*, web/lang_en.*, _lang_overrides.py, .gitignore, README.md
- **YC user:** "xóa hết 9router" kể cả UI (orphan) ở cả toolcaovideo (local) LẪN repo private mac-build (vannhph53095/viralcrawl-mac-build).
- **Làm:** xóa thư mục `9router/` + `desktop/build/build_9router.ps1`; gỡ bước `[1b]` build + block staging 9router (`stage_app.ps1`); gỡ launcher `main.js` (start/stop9router, nineRouter*, ROUTER_PORT + 2 call-site); gỡ provider `9router` + `_9r_*`/`_9R_*` trong `ai_dich.py` (GIỮ 2 comment "dọn key/config provider đã gỡ" ở `danh_sach`+`cau_hinh` — generic, tự dọn 9router cũ của khách); gỡ 5 chuỗi lang orphan (`lang_en.json`+`lang_en.js` 1185→1180, `_lang_overrides.py`); `.gitignore` + README. AI dịch CÒN Groq/Gemini/Ollama Cloud/Ollama Local; UI provider data-driven từ `PROVIDERS` → dropdown 9router tự mất.
- **Verify:** py_compile + node --check + ps1 parse + JSON valid ĐỀU OK (cả 2 repo). mac-build ĐÃ commit+push (`a498028` xóa 9router, `5b09312` README) → build hết bước 9router = khỏi lỗi EPERM Windows CI. **Local CHƯA commit** (user tự commit).
- **⚠ ĐIỀU PHỐI:** `main.js` mình CHỈ đụng phần 9router; phần `licenseLohapage` + `web_app.py`/`web/index.html`/`lic_cli.py`/`lic_client.py` là của session LohaPage (mục 🟢 dưới) — KHÔNG đụng.
- **CÒN:** `docs/*.md` vẫn nhắc 9router (46× prose, docs đã lỗi thời) — CHƯA sweep (chờ user quyết vì sửa prose dễ hỏng câu).

### 🟢 [2026-07-03] Queue render BỀN (khôi phục sau tắt app) + retry + fix đổi-thư-mục + GATE quyền LohaPage — đụng web_app.py, lic_client.py, lic_cli.py, desktop/main.js, web/index.html (nhánh feat/dang-bai-longtieng-en)
- **YC user:** (1) rà "định hướng Nguồn kênh & Đăng bài" (23 mục) → làm tiếp mục thiếu; (2) đảm bảo workflow đúng khi ĐỔI THƯ MỤC; (3) GATE quyền LohaPage (tool RIÊNG) cho Kênh nguồn + Đăng bài, thiết kế để server bật/tắt.
- **XIX khôi phục queue (web_app.py — MIRROR `_task_luu/_task_nap`):** `_queue_file`=`<DATA_DIR>/_render_queue.json`; `_queue_luu` (atomic, chỉ khi ĐỔI, snapshot ALLOWLIST key → khỏi race "dict changed size"); `_queue_nap` ('dang'→'cho' tiếp tục; mất file gốc→'loi'); thread `_queue_saver` (4s, KHÔNG đụng vòng render). Cache artifact (mặc định bật) đã lưu srt sau dịch (localize:2811) + dub sau TTS (localize:2991) → render-lại HIT cache bỏ qua bước đã xong. **XI retry:** `_co_the_thu_lai` (env `VC_RENDER_RETRY`=1; bỏ qua huỷ/dịch-thủ-công/moov-atom) + 2 call-site điểm set 'loi'. **Verify:** unit 22/22 + boot thật (nạp→khôi phục→saver bền, không kích hoạt render oan).
- **Fix ĐỔI THƯ MỤC (web_app._doi_thu_muc_data):** phát hiện lỗ hổng (test 10 case): `_merge_move` dời video sang chỗ mới nhưng path tuyệt đối trong `_queue` vẫn trỏ CŨ → khôi phục 'loi' oan. Thêm `_queue_remap_paths(old→new)` cho path DƯỚI data/processed đã move (path ngoài như Desktop giữ nguyên). Verify: test đổi-thư-mục 10/10 PASS (C2: clip đã move → khôi phục vẫn 'cho').
- **GATE QUYỀN LOHAPAGE (5 file):** CONTRACT = server KÝ cờ boolean `lohapage` vào token (như `tier`). Chuỗi: `lic_client.kiem_tra` trích `cache['lohapage']` (online+offline) → `lic_cli status` in `lohapage` → `desktop/main.js` truyền `env.VC_LOHAPAGE` → `web_app.LOHAPAGE_OK`. `_refresh_tier` đọc lại khi "Làm mới gói" (server dashboard bật → tự mở khoá). Gate: `_la_loha_path` (kn_* + loha_dir_chung/trang_get/save/gom) chặn ở `_do_GET`+`_do_POST` → `_loi_lohapage` `{loi_quyen:true}`. `_goi_info` trả `lohapage`. UI (index.html `applyGating`): **ẨN "Kênh nguồn" + KHOÁ "Đăng bài"** (click→nâng cấp) khi `!PLAN.lohapage`. Mặc định (chưa mua) = KHOÁ. Override dev: `VC_LOHAPAGE=1`. **Verify:** unit 25/25 + boot LIVE khoá(kn_list loi_quyen, goi lohapage:false)/mở(kn_list ok, goi lohapage:true) + POST gate với nonce (trang_gom/kn_them/kn_tai_ngay→loi_quyen, queue_clear→ok).
- **+ GATE AUTOMATION (sau khi user hỏi "flow tự động"):** gate endpoint CHƯA kín — worker nền giao LohaPage KHÔNG qua endpoint → lọt. Thêm `_can_lohapage()` tại 4 điểm: `_kn_worker` (bỏ chu kỳ), `_kn_giao_sau_render` (chặn giao job cũ trong queue), `_phan_loai_sau_render` (chỉ bỏ sidecar .txt, VẪN phân loại+move folder thường), `_auto_gom_worker` (bỏ gom). Verify unit 6/6. → Phân loại: KHÔNG quyền = folder thường (thuần xếp loại); CÓ quyền = ghi sidecar → đăng LohaPage.
- **+ PICKER PHÂN LOẠI → LOHAPAGE (2026-07-03, user chốt build):** mỗi thể loại có thể gán ĐÍCH LohaPage (Trang page_id / Nhóm group_slug + hashtag) — CHỈ hiện khi `PLAN.lohapage`. Backend web_app: `_pl_loha_dir` (đọc loha_uploads_dir từ kenh_nguon = 1 nguồn), `_pl_resolve_dich` (reuse `gom_dang_bai.folder_loha` → `<loha>/<Tên>_<pageid>` / `__group_<slug>`, ĐỒNG BỘ contract Kênh nguồn), `_pl_hashtag_theo_folder` (sidecar caption); `_pl_folders` resolve dich→path (CHỈ khi `_can_lohapage`, không quyền→`base/ten` folder thường); GET trả `muc[].dich`+`lohapage`+`loha_dir`; POST nhận `muc[{ten,dich}]`; sidecar dùng hashtag thể loại. UI index.html: modal phân loại — mỗi dòng thêm select `📁 Chỉ lưu / 📤 Trang / 📤 Nhóm` + ô con page_ten/page_id/group_slug/hashtag + banner gợi ý (`plAddRow/plRowUpd/plLohaHint/plSave` viết lại). **Verify:** unit resolve 10/10 + LIVE HTTP e2e 9/9 (set loha_dir→POST dich page→GET resolve đúng folder+dich; nhac chỉ-lưu→base; không quyền→folder thường). File: `web_app.py`+`web/index.html` (đã có trong danh sách). CẦN restart app + F5.
- **⚠ SERVER (server.py/lic_db.py) do team user làm SAU:** chỉ cần KÝ `lohapage:true` vào token cho tài khoản mua LohaPage → toàn client tự khớp. KHÔNG đụng file server ở đây.
- **File đụng:** `web_app.py`, `license_server/lic_client.py`, `license_server/lic_cli.py`, `desktop/main.js`, `web/index.html`. Cần **restart app** (env + gate + UI). CHƯA commit.
- **Trạng thái:** 🟢 ĐANG LÀM (chờ user review + test UI thật + server ký cờ).

### ✅ [2026-07-02] Dọn .srt cạnh video SAU render (đỡ rối mắt khách; cache vẫn giữ ở _cache_artifact) — đụng web_app.py
- **YC user:** .vi.srt/.zh.srt để cạnh video làm rối thư mục khách; nên dùng thư mục cache riêng, xoá sau. Chọn hướng A: DỌN sau render.
- **Bối cảnh:** cache SRT riêng ĐÃ CÓ (`cache_artifact.py` → `_cache_artifact/` ở userData, TTL 7d, LRU 3GB, tự sweep). localize vẫn ghi .srt cạnh video vì: burn phụ đề (localize), phân loại đọc sau render, sửa-SRT-tay, đổi tên sidecar.
- **Làm (web_app.py):** helper `_don_srt_canh_video(video)` xoá `.vi.srt/.zh.srt/.zh.gem.vi.srt/.dubsync.vi.srt` cạnh video (đuôi TƯỜNG MINH, không glob → không đụng video khác cùng tiền tố); env `VC_GIU_SRT=1` = thoát hiểm giữ lại. Gọi SAU render THÀNH CÔNG: trong `_phan_loai_sau_render` (finally — sau khi đã đọc .vi.srt) + `_queue_worker` nhánh kn_giao/phân-loại-tắt. CHỪA `dich_thu_cong` (khách cần .zh.srt đã sửa). cache_artifact lưu TRƯỚC khi dọn → re-render vẫn HIT nhanh.
- **Verify:** py OK; unit-test logic PASS (xoá 4 đuôi của video, giữ video.mp4 + video khác cùng tiền tố; VC_GIU_SRT giữ lại).
- **⚠ SỰ CỐ ĐIỀU PHỐI (đã xử lý):** commit dọn-srt `7146d8c` LỠ gom cả khối Kênh-nguồn/LohaPage (`_kn_*`, `_doi_ten_video`, `_ghep_video_*`, `_loha_auto_uploads`…) ĐANG DỞ trên đĩa (chưa commit) của session khác. Đã sửa bằng commit `d0d833c`: đưa web_app.py về f6bd332+dọn-srt (GỠ Kênh-nguồn khỏi main), rồi KHÔI PHỤC bản đầy đủ lên ĐĨA → **code Kênh-nguồn còn nguyên dạng uncommitted (826 dòng) cho session kia tiếp tục + tự commit khi xong**. KHÔNG mất gì. Bài học: `git diff --stat` TRƯỚC khi `git add` file dùng chung.
- **File đụng:** `web_app.py`. Dọn-srt ĐÃ commit (`d0d833c`). **CHƯA deploy laptop** (đang cho user thử nghiệm — chờ hết trial mới deploy). 🟢 web_app.py trên đĩa CÒN Kênh-nguồn uncommitted (session Kênh-nguồn đang làm) — session khác ĐỪNG `git add web_app.py` trọn gói.

### ✅ [2026-07-02] Fix sub Việt hiện TRỄ hơn chữ Trung (onset OCR) + tắt Whisper-fill + test chữ-giữa — đụng ocr_text.py, localize.py (MAIN)
- **YC user:** (a) "sub Việt hiện chưa nhanh bằng chữ Trung" → onset trễ; (b) test render chữ ở tâm 50% khung (video makeup) xem dò/che/đè sub đúng không.
- **Test chữ-giữa (render THẬT, xem tận frame):** tạo clip giả-lập chữ tâm 50% + chữ cao40%+banner đáy. KẾT QUẢ: chữ tâm 50% **dò+che+đè ĐÚNG** (box y49-53%). Ca có banner đáy cạnh tranh → mặc định `blur_band` (phat_hien_dai_rapid, **thiên đáy**) bám nhầm banner → chữ Trung KHÔNG bị che + sub Việt xuống đáy; **bật `CHE_DONG=1`** (blur động theo box OCR) → che+đè ĐÚNG tâm. → đề xuất tương lai: auto bật CHE_DONG khi sub không ở đáy (CHƯA làm, chờ user).
- **Bug onset (ocr_text.ocr_dong):** đo clip mốc-chữ-thật 0/3/6/9s → cue1 gán 0.250s (chữ có từ 0.0s). Gốc: dòng backdate `0.0 < t_new <= t` — `<` NGHIÊM loại `t_new=0.0` (chữ từ frame đầu) → cue ĐẦU rơi về `t` = trễ 1 nhịp kiểm. + trễ granularity ~½ nhịp `OCR_CHK` (onset chỉ bắt tại mốc kiểm).
- **Fix (2 phần):** (1) `0.0 <= t_new` cho cue đầu về 0.0; (2) BÙ TRỄ XÁC ĐỊNH ĐƯỢC (không đoán hằng số): độ trễ kỳ vọng = ½ khoảng-kiểm = `chk/(2·fps)` (tính từ fps+OCR_CHK THẬT) → lùi t_on đúng bằng đó, guard KHÔNG chồng câu trước, áp cho CẢ box (blur+sub đồng bộ). Hệ số `OCR_SUB_LEAD` (mặc định 0.5=midpoint/unbiased; 1.0=lùi cả nhịp; 0=tắt).
- **Verify:** py_compile OK; re-render clip A → zh.srt cue1 **0.000** (trước 0.250), cue2/3/4 lùi **0.125s**=chk/(2·12); frame t=0.1s **sub đầu ĐÃ hiện** (trước trống). 4 video ở `Desktop\test_chugiua\` (A_tam50 / B_banner_MACDINH_bam-day / B_banner_CHEDONG1_bam-giua / REAL_canh13).
- **File đụng:** `ocr_text.py` (onset t0), `localize.py` (destutter — mục dưới). localize/ocr_text=subprocess → KHÔNG cần restart; cache srt cũ giữ timing cũ → **Render TỪ ĐẦU** mới thấy. CHƯA commit.

### ✅ [2026-07-02] Fix LẶP-TỪ trong 1 cue (Whisper vấp) → hết "1 câu nhanh 1 câu chậm" — đụng localize.py (trên MAIN)
- **YC user:** phụ đề/lồng tiếng bị lặp từ khiến 1 câu đọc nhanh bất thường (engine nén cho khỏi trôi timestamp), 1 câu bình thường → nghe lệch tốc độ. Muốn chấm dứt; chốt đưa fix lên **main**.
- **Điều tra (dữ liệu THẬT, KHÔNG đoán):** quét 32 file zh.srt + 1131 cue vi.srt cache thật. (1) Cross-cue lặp (cue≡cue kề) ĐÃ được `_gop_trung`+`_dedup_lap` xử lý — có sẵn TRÊN CẢ main lẫn feat (diff localize.py main↔feat chỉ là Piper-EN, KHÔNG đụng dedup) → "merge feat sang main" vô ích cho việc này. (2) Lớp CÒN SÓT = **lặp LIỀN-KỀ NGAY TRONG 1 cue do Whisper vấp/ảo giác** (`林总好林总好，林总，好` → dịch `Xin chào anh Lin. Xin chào anh Lin. Tốt` **cps 28.7 vs median 17 = nhanh bất thường**). `_dedup_lap` KHÔNG bắt (nó so GIỮA cue, không soi TRONG cue).
- **Fix 1 (gốc — user chốt sau khi trao đổi):** **TẮT Whisper-fill mặc định** (`ASR_OCR_WHISPER_FILL` default "1"→"0"). User xác nhận: lặp-từ CHỈ xuất hiện SAU khi có Whisper-fill, trước đó (OCR-only) chưa bao giờ lặp → video hardsub của user OCR đọc đủ, fill chỉ chèn cue rác trùng nội dung ở khe. Fallback full-Whisper (video KHÔNG hardsub) VẪN chạy (nhánh dưới, độc lập fill) → không mất nội dung. Bật lại: `ASR_OCR_WHISPER_FILL=1`.
- **Fix 2 (safety-net — giữ cho full-Whisper):** thêm `_bo_lap_lien(text)` (gộp khối LIỀN-KỀ giống hệt, đơn vị ≥3 ký tự CJK / ≥3 từ Latin) — GIỮ điệp-từ hợp lệ (看看/精神精神 đơn-vị≤2; 一边..一边 / 一杯敬..一杯敬 / 有人..有人 = lặp cách quãng có chữ xen). Áp trong `chay()` NGAY sau `_dedup_lap` cho mọi cue (trước dịch+ghi zh.srt). Kill-switch env `DUB_DESTUTTER=0`. (Vẫn hữu ích khi video không hardsub → full-Whisper có thể vấp.)
- **Verify:** py_compile OK; unit 13/13 (gộp stutter ≥3, giữ mọi điệp hợp lệ, 0 false-positive); chuỗi `_gop_trung→_dedup_lap→_bo_lap_lien` trên tập cue thật đúng; **regression TOÀN CORPUS: 975 cue zh thật → CHỈ 1 đổi (0.1%) = đúng stutter `林总好林总好`**, không đụng nội dung hợp lệ; bản VI tương ứng (cue nhanh cps28.7) diệt tại gốc. **CHƯA test full render LIVE** (heavy CPU) — transform text thuần, deterministic, đã test corpus+chain+compile.
- **File đụng:** `localize.py` (thêm `_bo_lap_lien` + 1 call-site). localize = subprocess load fresh mỗi render → KHÔNG cần restart web_app; NHƯNG cache-hit srt cũ vẫn còn lặp → phải **"Render TỪ ĐẦU" (dich_lai)** mới thấy hết. CHƯA commit (chờ user).
### 🟢 [2026-07-01] Tab "Kênh nguồn" kiểu TikTok — profile kênh (video ĐÃ CÀO chưa tải) + lịch tự tải/ngày → Loha — đụng kenh_nguon.py(MỚI), web_app.py, web/index.html
- **YC user:** tab mới style TikTok: cào theo kênh → hiện MỌI video đã cào (chưa tải, như lịch sử cào) dạng trang cá nhân (avatar+tên từ link kênh + lưới video). Đặt lịch → mỗi ngày dùng API tải N video (như nút "tải về" lịch sử cào) → nối Loha auto đăng. Import kênh từ tab kênh (theo_doi) làm nguồn.
- **Nghiên cứu (2 explore agent + đọc):** downstream render→gom→Loha ĐÃ XONG (`gom_dang_bai.gom` ghi thẳng `<loha_uploads>/<Tên>_<page_id>/`+`.txt`; Loha có scheduler per-page tự đăng đúng giờ). Creator-crawl metadata-only = `tim_anh.py --type creator` → items {id,title,thumb,url,like,time,nick,avatar}; tải theo id = `cao_anh_chon(platform,ids)` (/api/cao_anh_chon). JSONL persist. Preview hiện EPHEMERAL → cần store per-kênh mới.
- **Kế hoạch:** `kenh_nguon.py`(MỚI, store userData bền) = profile kênh {platform,link,ten,avatar,sec_uid,videos[{id,title,thumb,url,tai}],lich{on,so_moi_ngay,gio,trang_id}}; web_app endpoint /api/kn_them·list·videos·lich·import + worker-ngày (chua_tai→cao_anh_chon→chuỗi sẵn có); index.html tab TikTok-profile.
- **CHỐT KIẾN TRÚC (sau khi verify contract LohaPage THẬT + phát hiện gap):** feature TỰ CHỦ, KHÔNG qua gom (gom khó dùng). Verify: (1) contract Loha đã đồng bộ (file-service.js có comment `// SIDECAR (reupo)` = build riêng cho ViralCrawl; sidecar `.txt`=`caption#tags`, folder `<Tên>_<page_id>` lastIndexOf('_'), page_id≥5, .txt-trước-.mp4 chống race). (2) GAP: `cao_anh_chon` dùng `--type detail` → video vào `link/` mà gom CHỈ gom `tu-khoa/`+`kenh/` → BỎ QUA. → KHÔNG nhờ gom dò. (3) Route theo sec_uid (user chốt) + cho ĐỔI TÊN kênh (chỉ hiển thị).
- **GIẢI PHÁP:** tách phần GIAO của gom thành hàm DÙNG CHUNG `giao_loha`/`folder_loha`; kenh_nguon TẢI→RENDER→`giao_loha` THẲNG (biết video+page). Không quét link/, không sidecar hack, không patch crawler.
- **XONG BƯỚC 1 (backend foundation, test đầy đủ):**
  - `gom_dang_bai.py`: +`folder_loha(loha_dir,page_ten,page_id)` + `giao_loha(dest_dir,video,caption,ten_goi_y)` (ghi .txt trước .mp4, tên unique, contract verify). Refactor `gom()` gọi 2 hàm này (hành vi Y CŨ — dry+real test OK). ⚠️ gom pipeline cũ CẦN verify real run trước deploy.
  - `kenh_nguon.py`: self-contained. Kênh = {sec_uid, ten(đổi tự do), nick_goc, avatar, videos[{id,title,thumb,url,tai}], lich{on,so_moi_ngay,gio}, dich{kieu:page|group, page_id, page_ten, group_slug, hashtag}}. +loha_uploads_dir (global). Hàm: them_kenh(merge giữ 'tai'+tên đã đổi), dat_lich, dat_dich, doi_ten, danh_dau_tai, chua_tai, folder_dich (page→<Tên>_<id>, group→__group_<slug> tối ưu NHIỀU page), thong_ke. Test: page+group+rename+re-crawl-không-đè-tên PASS.
- **XONG B2+B3 (2026-07-01, user đi vắng — làm autonomous):**
  - **web_app.py:** block "KÊNH NGUỒN" (sau chay_tim_anh): `_kn_cao_kenh` (cào creator metadata→them_kenh, guard _proc/_dang_cao), `_kn_tai_va_render` (chua_tai→cao_anh_chon→đợi tải→tìm file _id6→_queue_them marker kn_giao), `_kn_giao_sau_render` (_output_xuly_moi_nhat→giao_loha), `_kn_worker`+`_khoi_dong_kn` (mỗi 60s, kênh tới giờ+chưa chạy hôm nay→tải+render+giao), `_kn_render_opts`/`_kn_caption_tu_stem`. Hook `kn_giao` trong `_queue_worker` (cạnh phan_loai_sau). Endpoints GET kn_list/kn_videos + POST kn_them/kn_loha_dir/kn_lich/kn_dich/kn_doiten/kn_xoa/kn_tai_ngay. `_khoi_dong_kn()` vào main.
  - **web/index.html:** nav "Kênh nguồn" + section `v-kenhnguon` (config loha_dir + ném-kênh + card kênh avatar/tên/số-video) + modal profile TikTok (đổi tên inline + lưới thumb + gán page/group + hashtag + lịch on/N-ngày/giờ + Tải-ngay + Xoá). JS kn* (knLoad/knThem/knOpen/knSaveCfg/knTaiNgay/knXoa...). Click-hook `kenhnguon→knLoad`.
  - **VERIFY:** kenh_nguon+giao_loha+folder_dich unit PASS; E2E delivery (kênh→page→giao_loha→folder `<Page>_<id>/` + filename=caption, LohaPage parse đúng) PASS; py_compile 3 file OK; JS new Function 2/0; **web_app THẬT khởi động sạch (worker kn không crash) + GET /api/kn_list→200 `{ok,loha_uploads_dir,kenh:[]}`**. (POST/crawl/render full cần login+network+render — chưa test headless; wiring đã review.)
  - **BẪY gặp:** cổng 8770 bị web_app CŨ của user giữ → test 404 oan (code cũ) → phải kill hết web_app cũ rồi start clean mới đúng.
- **+ IMPORT kênh đã cào (2026-07-01):** `_kn_import_da_cao(platform)` (web_app) đọc `creator_contents_*.jsonl` (file cào creator) → gom theo sec_uid/user_id → mỗi kênh = nick+avatar+videos (parser tim_anh) → `them_kenh` (MERGE, giữ tai+tên). Link tái tạo: dy=douyin.com/user/<sec>, bili=space.bilibili.com/<uid>, xhs=user/profile, wb=u/. Endpoint POST /api/kn_import + nút "📥 Import kênh đã cào" (knImport JS). Test THẬT trên data dev: 31 kênh (17 dy+14 bili) · 819 video, idempotent (re-import không nhân đôi) PASS. py+JS OK.
- **+ PHÂN LOẠI → LohaPage (2026-07-01):** `_phan_loai_sau_render` (web_app.py) SAU khi move `_xuly.mp4` vào folder thể loại → NẾU folder là folder LohaPage (`<Tên>_<pageid≥5số>` HOẶC `__group_<slug>`, regex gate) thì ghi sidecar `.txt` caption (`tao_caption.tieu_de_thuong` từ tên file, +#lohatech; KHÔNG AI = rẻ). GIỮ tên .mp4 (khỏi vỡ matching "File đã tải" theo _xuly). Folder thường → bỏ qua (chỉ xếp thể loại). Tắt: env PL_LOHA_TXT=0. → luồng THỂ LOẠI đăng Loha đẹp: user set path thể loại = folder Loha là xong. Test gate+caption PASS; py_compile OK.
- **CÒN:** e2e THẬT qua UI (cào 1 kênh có login→gán page→Tải ngay→render→check folder Loha; + phân loại path=folder Loha→render→check .txt) + deploy laptop + **verify gom() cũ real-run** (đã refactor gọi giao_loha, mới test dry+cô lập). CHƯA commit.
- **ĐÃ DEPLOY LAPTOP app-src (2026-07-01):** 4 file (kenh_nguon.py + gom_dang_bai.py + web_app.py + web/index.html) hash khớp dev 100% (79626cd/9c4679d/b7951b6/27309d5), backup `_bak_0701_kenhnguon`. **User phải TẮT HẲN + MỞ LẠI app ViralCrawl** (Electron spawn web_app đủ env — KHÔNG tự spawn qua SSH [[deploy-laptop-khong-spawn-web-app-tran]]). Rồi tab Kênh nguồn → "📥 Import kênh đã cào" gom kênh đã cào THẬT trên laptop → gán page/lịch.
- **File đụng:** `kenh_nguon.py`(mới), `gom_dang_bai.py`, `web_app.py`, `web/index.html`. ⚠️ web_app.py+index.html CHUNG nhiều session — CHỈ THÊM block/route/tab kn, không đụng vùng khác.

### 🟢 [2026-07-01] KHÔI PHỤC che sub GIỮA khung (regression split-screen) — đụng dai_sub_rapid.py
- **YC user:** "cần thêm che sub ở giữa, hình như trước có mà nay mất". ĐÚNG — regression thật.
- **Chẩn (git):** commit 213e318 (24/6) từng fix "che-sub giữa khung" ở `dai_sub_ocr.py` (24 dải + ưu tiên dải CHỮ-ĐỔI = sub thật mọi vị trí). SAU đó thêm `dai_sub_rapid` (RapidOCR) làm detector #1: det-only (không đọc chữ) + `score=nf*avg_w*(0.6+yc)` THIÊN VỊ ĐÁY → sub giữa (split-screen Douyin "2 ảnh chồng") thua banner/watermark đáy. dai_sub_ocr (còn logic giữa) bị che khuất vì RapidOCR chạy trước.
- **Fix (dai_sub_rapid.phat_hien_dai_rapid):** phân biệt SUB (nội dung ĐỔI qua frame, mọi vị trí) vs biển-hiệu TĨNH bằng CHÊNH-LỆCH ẢNH giữa frame (cv2.absdiff grayscale dải mỗi cluster — RẺ, KHÔNG OCR: thử rec = +70s QUÁ CHẬM, bỏ). Có cluster đổi-ảnh (≥CHE_GIUA_THR=4.0 thang xám) → chọn theo nhiều-frame×rộng BỎ bottom-bias → sub giữa thắng. Không cluster nào đổi → GIỮ logic cũ bottom-biased (0 regression). Tắt: env CHE_GIUA=0.
- **Verify (clip giả-lập PIL+CJK, đo thật):** split-screen (sub giữa ĐỔI + banner đáy rộng TĨNH) → fix BẬT ra GIỮA 50%, TẮT (CHE_GIUA=0) ra ĐÁY 90% (tái hiện regression); video sub-đáy thường → fix vẫn ra ĐÁY 90% (0 regression). Chỉ chậm +2s (11.9 vs 9.7s det-only). py_compile OK.
- **CÒN:** verify trên VIDEO THẬT của user (họ có clip split-screen trên laptop — chờ đường dẫn) + deploy.
- **File đụng:** `dai_sub_rapid.py`. localize = load fresh mỗi render → KHÔNG restart; cache band cũ → phải "Render từ đầu" (dich_lai bỏ cache band).

### 🟢 [2026-07-01] Tích hợp giọng Piper TIẾNG ANH (narrator lessac/ryan/amy) cho render EN — đụng localize.py, web/index.html
- **YC user:** thử giọng Piper EN → cài trên laptop để render thử. Đã synth thử lessac-high trên dev (RTF~1.0 CPU i3, length_scale=speed native) — user duyệt hướng.
- **Phát hiện:** EN đang gate cứng Kokoro (`localize.py:2851 tgt=="vi"` mới cho Piper); piper-tts tự tải giọng HF qua `python -m piper.download_voices <ten>` (cơ chế đã có ở `long_tieng_piper`); web_app+xu_ly_chon truyền tts/voice TỰ DO (không whitelist) → chỉ cần sửa localize + index.html.
- **Làm — localize.py:** (1) `long_tieng_piper(..., chuan_hoa=True)` — thêm param, EN → `chuan_hoa=False` (KHÔNG `_chuan_hoa_tts` = chuẩn-hoá số/ngày kiểu VN, sai cho Anh); (2) thêm nhánh dispatch `tgt!="vi" and tts_engine=="piper"` → `long_tieng_piper(model_path=PIPER_DIR/<voice>.onnx, chuan_hoa=False)`, voice mặc định `en_US-lessac-high`, fail→edge-en. **web/index.html:** thêm `PIPER_EN_VOICES` (lessac/ryan/amy) + option Piper vào dropdown EN + gom nạp picker giọng theo (tgt,engine) vào `xlUpd` (1 nguồn).
- **KHÔNG đụng web_app.py** (voice passthrough tự do). Piper = FREE (offline).
- **File đụng:** `localize.py`, `web/index.html`. ⚠️ index.html CHUNG nhiều session — chỉ thêm vùng EN-voice.
- **Verify:** localize py_compile OK; index.html JS 2/0; **render THẬT dev** (Chinese cảnh13 → TARGET_LANG=en → Piper lessac): OCR 22 câu → Gemini dịch Anh tự nhiên ("Hand him over to the station official"...) → Piper synth 22 câu (nạp `en_US-lessac-high.onnx`) → ghép `_xuly.mp4` OK, RTF lồng tiếng ~1.3× realtime CPU i3. Video demo `demo_EN_piper_lessac.mp4`.
- **ĐÃ deploy laptop app-src:** localize.py (hash b9885334…) + web/index.html (fbb8e487…), backup `_bak_0701_piperen`; đặt sẵn `en_US-lessac-high.onnx`+json vào PIPER_DIR laptop (`…viralcrawl-desktop\runtime\piper_vn`, xác nhận đúng thư mục live vì có ngochuyen.onnx). ryan/amy chưa đặt sẵn — app tự tải HF khi user chọn.
- **Trạng thái:** ✅ XONG (chờ user tắt/mở lại app + render thử). CHƯA commit.

### ✅ [2026-07-01] Fix phụ đề LẶP do OCR "lộ dần" hardsub 2 dòng (prefix/FULL/suffix) — đụng localize.py
- **Bug user gặp (video tiếng Anh laptop, nhưng CHUNG mọi ngôn ngữ):** phụ đề lặp. Ví dụ EN cue 16-17-18 = "Scientists rushed the head into" / "…into a sealed pod for sterilization" / "a sealed pod for sterilization". Gốc trong `zh.srt` OCR y hệt: cue16 科学家紧急将头颅送进 (nửa đầu) / cue17 …密封仓中进行消毒杀菌 (NGUYÊN) / cue18 密封舱中进行消毒杀菌 (nửa cuối, 仓→舱 OCR nhầm).
- **Chẩn (đã xác minh, KHÔNG phải do bản dịch EN):** lặp nằm sẵn trong OCR tiếng Trung TRƯỚC dịch → render VI cũng lặp. `_dedup_lap` (localize.py) bỏ sót vì: (1) stage(1) chỉ bắt `FULL,mảnh1,mảnh2` (full ĐỨNG ĐẦU), đây là `mảnh,FULL,mảnh` (full ở GIỮA); (2) stage(2) prefix/suffix có ngưỡng 60% mà mỗi nửa ~45-50% → trượt; (3) OCR nhầm 仓/舱 làm exact endswith trượt.
- **Fix (Ưu tiên 1, user duyệt):** thêm stage reveal-triple vào `_dedup_lap`: cue[i] fuzzy-prefix của cue[i+1] VÀ cue[i+2] fuzzy-suffix của cue[i+1] + tiling `len(i)+len(i+2)≈len(i+1)` → bỏ i,i+2 giữ FULL. Fuzzy (ratio) để sống sót OCR variant. (Gemini 1:1 = Ưu tiên 2, CHƯA làm — ít gặp, khó chắc.)
- **Rủi ro:** core dedup dùng CHUNG pipeline VI đang chạy tốt ([[bug-localize-cross-cutting-render-dub]]) → chỉ THÊM stage (1b), giữ nguyên stage (1)(2)(3); test cả reproduce + regression.
- **Chi tiết fix (localize.py `_dedup_lap` stage 1b):** với mỗi cue FULL (≥8 ký tự), tìm mảnh fuzzy-prefix (`_pre_ok`: ratio vùng đầu ≥0.85) + fuzzy-suffix (`_suf_ok`: ratio vùng đuôi ≥0.85) trong cửa sổ win=3. Ngưỡng độ-dài mảnh: cặp đơn 45% (chặt), TRIPLE 2 phía (có cả prefix lẫn suffix quanh FULL = reveal chắc chắn) → nới 30% (bắt nửa lệch). Bỏ mảnh, giữ FULL (logic nối-thời-gian sẵn có phủ khe).
- **Verify:** py_compile OK; 8/8 ca đối kháng PASS (giữ câu khác nhau, bắt cả balanced + unbalanced reveal, OCR nhầm 仓/舱, cặp-đơn <45% KHÔNG gộp); real Prometheus zh 70→67 (bỏ đúng 3 mảnh), 0 cặp dư lặp; 31 file cache đã-dedup → 0 bỏ thêm (idempotent); ASR thật clip dev cảnh13 → 22 câu GIỐNG trước (không regression). ĐÃ deploy laptop app-src (hash 34af3e9a…, backup `_bak_0701_dedup`).
- **File đụng:** `localize.py`. localize = subprocess load fresh mỗi render → KHÔNG cần restart web_app; NHƯNG cache-hit srt cũ vẫn lặp → phải "Render TỪ ĐẦU" (dich_lai) mới thấy hết lặp.

### ✅ [2026-07-01] Fix HẠN MỨC LỒNG TIẾNG bị bypass qua đường render (khách lồng tiếng vô hạn) — đụng web_app.py
- **Bug user gặp:** gói pro giới hạn 60 phút lồng tiếng/ngày nhưng khách vẫn render (lồng tiếng) nhiều thoải mái.
- **Gốc:** quota `dub_phut_ngay` (free10/pro60/∞) CHỈ check + đếm ở `/api/dub/start` (tab Lồng tiếng riêng). Nhưng lồng tiếng còn chạy qua `/api/xu_ly` (tab Render tick "Lồng tiếng"), `/api/localize` (Việt hóa), `/api/workflow_run`+`_auto_on` (Quy trình) — các đường này bật `--long-tieng` NHƯNG không check quota + `_queue_worker` không `usage_cong("dub_phut")` → lồng tiếng qua render KHÔNG chặn, KHÔNG đếm = bypass hoàn toàn.
- **Fix (user chốt: mọi đường CHUNG 1 hạn mức, greedy nhận tới hết budget + chặn phần vượt):** thêm `_video_phut` (đo phút path tuyệt đối, dùng `tim_exe` ffprobe), `_dub_da_cam_ket` (usage đã đếm + job đang chờ/chạy: queue có `_dub_phut` + tab-Dub `_dub_busy`/`_dub_phut_dang`), `_dub_quota_loc` (lọc greedy theo số phút VÀ số video). Áp vào `/api/xu_ly`, `/api/localize`, `_wf_chay` render → enqueue kèm marker `opts["_dub_phut"]`; đếm `usage_cong("dub"+"dub_phut")` SAU render thành công (trong `_queue_worker`; localize đếm trong `_crawl_worker` finally theo mtime output). Chặn toàn bộ → `_block` (banner tự hiện); chặn 1 phần → `limit:true`+msg.
- **Verify:** py OK; unit-test logic 9/9 PASS (greedy 5×15p/60p→nhận4 bỏ1; cam kết queue+dub-tab; giới hạn số video dub_ngay; fail-closed ffprobe lỗi; free/unlimited).
- **File đụng:** `web_app.py`. Đã commit + deploy laptop app-src. Cần **restart web_app** (app mở lại) mới hiệu lực.

### ✅ [2026-07-01] Fix video render KHÔNG BAO GIỜ vào đúng thể loại (AI phân loại lỗi WinError5) — đụng dich_gemini_web.py
- **Bug user gặp (laptop):** video "phim" đã render đều nằm ở gốc `processed_videos\phân loại\` thay vì folder `phim`. Log: `[phan_loai] AI lỗi: [WinError 5] Access is denied: 'MediaCrawler\\browser_data\\...'`.
- **Gốc:** `dich_gemini_web.hoi_gemini_web_nhieu` fallback profile Chromium = ĐƯỜNG DẪN TƯƠNG ĐỐI `MediaCrawler/browser_data/gemini_user_data_dir` (dòng 199). `_phan_loai_sau_render` (web_app) gọi THẲNG hàm này TRONG TIẾN TRÌNH (không qua subprocess) — CWD lúc đó = app-src trong `Program Files` (READ-ONLY, không admin) → `os.makedirs`/Chromium ghi lock file vào đó FAIL WinError 5 → mọi lần gọi AI phân loại đều lỗi → 100% video rớt về mặc định (base), KHÔNG BAO GIỜ vào đúng thể loại con dù cấu hình đúng.
- **Fix:** fallback profile ưu tiên `MC_BROWSER_DATA_DIR` (web_app đã set = userData khi đóng gói, cùng os.environ vì chạy chung tiến trình) → `<MC_BROWSER_DATA_DIR>/gemini_user_data_dir`; không có env nào thì giữ hành vi cũ (đường dẫn tương đối, dev). Không phá `GEMINI_PROFILE_DIR` (ưu tiên cao nhất, render_worker.py set riêng).
- **Verify:** py OK; unit-test resolve 3/3 PASS (packaged app dùng userData ghi được; không env nào → fallback tương đối cũ; GEMINI_PROFILE_DIR set riêng vẫn ưu tiên đúng).
- **File đụng:** `dich_gemini_web.py`. Đã commit + deploy laptop app-src.

### ✅ [2026-07-01] Fix Quy trình tự động BĂM ĐÈ LÊN CHÍNH OUTPUT của nó → nổ clip_nho/clip_nho/... (649 file/3h) — đụng web_app.py
- **Bug user gặp (laptop):** "File đã tải" tự dưng lên 100+ video, trần 400, xoá thật vẫn hiện 400. Điều tra: 649 video file MỚI TẠO trong 3h qua, phần lớn nằm trong hàng trăm folder `clip_nho`, có cả lồng 3 tầng `clip_nho\clip_nho\clip_nho`.
- **Gốc:** `_wf_auto_worker` (Quy trình "🚀 Bật tự động", pipeline `[bam(so_ban=2), render]`, cứ 10' 1 lần) lấy candidate qua `_auto_ung_vien()` = "video Cào gốc chưa có hậu tố _xuly/_phude/_longtieng". Clip do `cat_nho.py` băm ra (`..._cảnhNN.mp4`) KHÔNG có hậu tố đó → vẫn tính là "video gốc mới" → cycle sau BĂM LẠI CHÍNH CLIP VỪA BĂM → `_bam_out_dir` tạo `clip_nho` LỒNG bên trong `clip_nho` cũ. Khuếch đại mạnh bởi: (1) render trong `_wf_chay` BLOCKING nhưng chậm (whisper/OCR) → clip chưa kịp gắn `_xuly` khi cycle sau tới; (2) `_wfauto_seen` (dedup) chỉ RAM, mỗi lần tôi restart web_app (nhiều lần trong session debug login hôm nay) → reset về rỗng → mọi clip cũ lại "mới tinh".
- **Fix:** thêm `_la_bam_clip(path)` (path nằm trong folder `clip_nho` HOẶC tên khớp `_cảnhNN[_TxT].mp4`) → trong `_wf_auto_worker`, khi pipeline CÓ block "bam" → lọc bỏ candidate là bam-clip khỏi `new` TRƯỚC khi băm (clip đã ở render queue từ cycle sinh ra nó, không cần xử lý lại). KHÔNG đụng `_auto_ung_vien()` chung (tránh regression: auto-render THUẦN — không có bước bam — vẫn cần nhặt clip đã băm để tự render).
- **Xử lý khẩn trên laptop:** TẮT NGAY `workflow_auto_off` qua user (API POST cần nonce app, tôi gọi ngoài bị 403) để dừng chảy máu trước khi sửa code.
- **Verify:** py OK; unit-test `_la_bam_clip` 7/7 PASS (kể cả case thật laptop `..._651979_cảnh13.mp4`→True, video gốc/đã-render→False, tên trùng nhưng sai pattern→False).
- **File đụng:** `web_app.py`. Đã commit + deploy laptop app-src. Cleanup file rác (nested clip_nho) làm riêng, có xác nhận trước khi xoá.

### ✅ [2026-07-01] Fix cửa sổ login MỌI nền tự đóng trước khi user login (`_da_login` tin cred cũ) — đụng mo_dang_nhap.py
- **Bug user:** ấn nền tảng mở cửa sổ login → **tự đóng dù CHƯA đăng nhập** (tất cả nền, không riêng dy). Recheck → dy vàng.
- **Gốc:** `mo_dang_nhap._da_login` tin **cred CÒN trên đĩa** → `ban_dau=True` + poll-loop True oan → đóng sau ~2-5s: dy tin `localStorage.HasUserLogin='1'` CŨ (trang captcha 验证码中间页 không có nút 登录 → false True); xhs/rednote tin `id_token` cookie CŨ (phiên chết cookie còn). Cùng lớp xanh-giả với kiem_tra_login.
- **Fix (cùng triết lý kiem_tra_login):** dy → thêm guard trang captcha/degraded (title 验证码 / body<40 ký tự) → trả False (giữ cửa sổ); xhs/rednote → đọc `__INITIAL_STATE__.user.loggedIn` (DOM THẬT, cửa sổ headful không degrade) thay cho id_token cookie. Bias "chưa login" khi mơ hồ → KHÔNG đóng oan. bili giữ cookie-check (đã login → đóng là đúng).
- **Verify:** py OK. dev dy captcha → `_da_login=False`; laptop rednote/xhs (id_token cũ, logged-out) → `False`; laptop dy headful logged-in (title thật 抖音) → `True` (đóng đúng). mo_dang_nhap = subprocess → HIỆU LỰC NGAY (không cần restart web_app).
- **File đụng:** `mo_dang_nhap.py`. Đã commit + deploy laptop app-src.

### ✅ [2026-07-01] Check login "đúng": fix xanh-giả rednote/xhs + auto-verify sau đóng cửa sổ login — đụng kiem_tra_login.py, web_app.py
- **Bug 1 — xanh giả rednote/xhs:** `kiem_tra_login._check` short-circuit theo `id_token` no-browser → cookie CÒN trên đĩa cả khi phiên chết → báo "in" oan (verify bằng ảnh: badge 'in' mà rednote.com hiện modal login). **Fix:** BỎ short-circuit id_token; luôn để `_xhs_dom` (dùng `__INITIAL_STATE__.user.loggedIn`) làm trọng tài; id_token chỉ còn là fallback khi DOM không chắc. Test laptop: rednote/xhs giờ trả "out" đúng.
- **Bug 2 — login xong badge vẫn đỏ:** recheck worker CHỈ re-check nền đang "in" (bắt xanh-giả) → nền "out" không bao giờ verify lại; login-record qua `_da_login`+debounce trong cửa sổ dễ TRƯỢT (user tự đóng nhanh) → cache kẹt "out". **Fix (web_app.mo_dang_nhap):** thêm thread `_xac_minh_sau_dong` — sau khi subprocess mo_dang_nhap thoát (cửa sổ đóng) + chờ 3s flush cookie → chạy `kiem_tra_login <plats>` xác minh THẬT + ghi cache. Bỏ qua nếu đang cào.
- **Nguyên tắc check đúng:** (1) mỗi nền dùng tín hiệu THẬT (bili=API, dy=pong, xhs/rednote=DOM loggedIn); (2) chạy check đúng lúc: định kỳ cho "in" + ngay sau đóng cửa sổ login.
- **Verify:** py OK cả 2; kiem_tra_login rednote/xhs="out" (đã test laptop). kiem_tra_login.py = subprocess → HIỆU LỰC NGAY (không cần restart). web_app.py mo_dang_nhap → cần **restart web_app** (app Electron mở lại) mới có auto-verify.
- **File đụng:** `kiem_tra_login.py`, `web_app.py`. Đã deploy kiem_tra_login vào laptop app-src (backup); web_app chờ deploy+reopen.

### ✅ [2026-07-01] Phân loại: đơn giản hoá config — CHỈ gõ TÊN, tự tạo folder con dưới base — đụng web_app.py, web/index.html
- **YC user:** bật phân loại thì mặc định vào folder đã render sẵn (folder tên "phân loại"); thêm thể loại chỉ cần gõ TÊN → tự tạo folder con, KHÔNG phải nhập đường dẫn từng thể loại.
- **Backend (web_app.py):** thêm `_pl_base()` = `<PROCESSED_DIR>/phân loại`. POST /api/phan_loai nhận `names` → tự dựng `muc=[{ten, path: base/ten}]`, `default_path=base` (thể loại MỚI của AI cũng vào base). Tạo sẵn base+folder con khi bật. GET trả thêm `base`+`names`. Backward-compat: client cũ gửi `muc` vẫn nhận. Downstream (`_pl_folders`/`phan_loai_lo`/`_resolve_nhan`/move/filter) KHÔNG đổi.
- **Frontend (web/index.html):** modal bỏ ô path từng thể loại + ô default path; `plAddRow` chỉ còn ô TÊN; `plSave` gửi `{on,names}`; `plLoad`/`plBaseInfo` hiện base. Tái dùng badge/filter/move đã có.
- **Verify:** web_app.py py OK; JS 2/2; **Gemini THẬT**: names[mukbang,xe cộ] → ẩm thực→`phân loại/mukbang`, bóng đá→`phân loại/thể thao bóng đá` (mới), TẤT CẢ dưới base ✓.
- **File đụng:** `web_app.py`, `web/index.html`. Đã commit + deploy laptop app-src.

### ✅ [2026-07-01] Fix race SPA Douyin trong mo_dang_nhap.py (đã fix ở kiem_tra_login.py nhưng CHƯA port) — đụng mo_dang_nhap.py
- **YC user:** "kiểm tra lại vẫn báo chưa login nhưng thực ra đã login rồi tự động đóng" — nghi ngờ bug đã biết mà chưa sửa hết, nhờ soi git history.
- **Xác minh bằng git:** `git log --all -p -- mo_dang_nhap.py | grep wait_for_timeout` → RỖNG trong TOÀN BỘ lịch sử file. Trong khi `kiem_tra_login.py._dy_login` có `page.wait_for_timeout(1800)` trước khi đọc DOM (thêm ở commit `8ae4207`, 29/6) — vì Douyin là SPA, đọc DOM ngay sau `domcontentloaded` là RACE (nút 登录 chưa kịp render). Commit `8ae4207` CHỈ sửa `kiem_tra_login.py`, quên port sang `mo_dang_nhap.py._da_login` (đọc y hệt đoạn JS, quyết định `ban_dau` → điều khiển toàn bộ hành vi auto-close của cửa sổ login thật).
- **Fix:** thêm `page.wait_for_timeout(1800)` NGAY SAU `page.goto()` (chỉ platform `dy`, 1 LẦN — không đặt trong `_da_login` vì hàm này bị gọi lặp lại mỗi 1s trong vòng poll, đặt trong đó sẽ làm chậm toàn bộ poll loop).
- **Verify:** py_compile OK; chạy trực tiếp `python mo_dang_nhap.py dy` (máy dev đang login sẵn) → vẫn đúng `"dy da dang nhap san -> dong sau 5s"`, không đổi hành vi ca bình thường. KHÔNG tái hiện được đúng khung race (cần SPA load chậm) — tin cậy vì fix mirror y hệt pattern đã verify ở kiem_tra_login.py.
- **File đụng:** `mo_dang_nhap.py`. CHƯA commit (chờ user).

### ✅ [2026-07-01] Fix cache "in" 1 chiều không invalidate (login xanh giả sau auto-close sai) — đụng mo_dang_nhap.py
- **YC user:** nghi ngờ hàm check-login có lưu cache mà không xoá. Yêu cầu soi kỹ hàm check-login + dữ liệu nó lưu lại.
- **Xác minh bằng code:** `mo_dang_nhap.py._ghi_status()` CHỈ được gọi với `{plat:"in"}` (2 chỗ: ban_dau lúc mở + "vừa login" trong poll loop) — KHÔNG BAO GIỜ ghi `"out"`. `web_app.py:5158-5178` (`/api/login_trangthai`, nguồn dữ liệu badge) đọc `_login_check.json` và TIN THẲNG (không xác minh lại) nếu file <5 PHÚT tuổi.
- **Chuỗi lỗi ghép lại:** race SPA (bug đã fix ở mục trên) đọc nhầm 1 lần → `_ghi_status({"dy":"in"})` ghi SAI vào cache → cửa sổ auto-close SỚM (chưa quét QR xong) → badge đọc cache sai → hiện xanh "đã đăng nhập" tới 5 PHÚT dù thực tế CHƯA có phiên → cào/dùng thật fail → đúng cảm giác user mô tả "báo đã login nhưng vẫn lỗi, rồi tự đóng".
- **Fix:** thêm DEBOUNCE trong vòng poll `mo_dang_nhap.py` (nhánh "vừa login trong phiên") — chỉ tin `_da_login()==True` khi đọc thấy 2 LẦN LIÊN TIẾP (cách nhau 1s, counter `xn` trong tuple `items`, reset về 0 nếu có lần False xen giữa) rồi mới ghi cache + đóng cửa sổ. Nhánh `ban_dau` (đã login sẵn lúc mở) giữ nguyên — đã được bảo vệ bởi fix wait_for_timeout(1800) ở mục trên.
- **Verify:** py_compile OK; giả lập state machine độc lập (không cần browser) — `[True]` đơn lẻ → NO_CLOSE (đúng, trước đây sẽ đóng sai); `[True,True]` / `[False,True,True]` → CONFIRMED đúng lượt thứ 2. CHƯA test qua QR login thật (cần user tự thao tác).
- **File đụng:** `mo_dang_nhap.py` (cùng file, gộp chung với fix wait_for_timeout ở mục trên). CHƯA commit (chờ user).

### ✅ [2026-07-01] Gộp UI "Đã render" với flow PHÂN LOẠI (cho dễ dùng) — đụng web/index.html, web_app.py
- **YC user:** đưa cấu hình + kết quả phân loại vào ngay tab 🎬 Đã render (thay vì modal ẩn trong tab Render). Chốt: FULL scope + chuyển hẳn nút 🗂️ khỏi tab Render.
- **Làm (web/index.html):** (1) gỡ nút 🗂️ Phân loại khỏi tab Render (~1061); (2) badge trạng thái + nút ⚙ Cấu hình phân loại vào header tab Đã render (`#fPlWrap`, chỉ hiện fMode=render); (3) thanh lọc THEO THỂ LOẠI `#filTheLoai` (đếm bằng map f.p→folder thể loại: `_plCfg`/`fileTheLoai`, facet "tl" trong fileKhop); (4) mỗi thẻ render: chip thể loại + nút ↪ Chuyển thể loại; (5) `renderOutDirs` phân biệt folder thể loại (🗂) vs thư mục đích thủ công (📁). Tái dùng modal `plOpenModal`/`plSave` cũ.
- **Làm (web_app.py):** route mới `POST /api/phan_loai_move` — move output sang folder thể loại chọn tay + `_them_out_dir` (sửa khi AI đoán sai). KHÔNG đụng `_phan_loai_sau_render`/`_pl_folders`/`phan_loai.py` (engine đã test 12/12 PASS cả dev+laptop).
- **Lưu ý laptop:** app_settings.json laptop dùng key CŨ rỗng (`phan_loai_folders`/`phan_loai_default`) → phân loại đang no-op; gộp UI xong vẫn phải cấu hình thể loại thật mới có tác dụng.
- **THÊM — thể loại MỚI tự sinh (phan_loai.py):** Gemini được phép TỰ ĐẶT thể loại mới khi video không hợp thể loại có sẵn (đổi prompt `_goi_ai_batch`); nhãn mới → TẠO FOLDER CON trong folder mặc định `<default>/<tên mới>` (helper `_resolve_nhan`+`_safe_folder`; cache nhãn thô để nhất quán). Trước đây prompt ép "chọn gần nhất" → iPhone bị nhét sai vào "xe cộ". web_app KHÔNG đổi (`_whitelist_out` cho path tuyệt đối; `_phan_loai_sau_render` tự makedirs+move+`_them_out_dir`).
- **Verify:** JS new Function 2/2 OK; `fileTheLoai` node 5/5; web_app.py py OK; GET /api/phan_loai chạy (POST cần session). phan_loai.py py OK; mock 8/8 PASS; **Gemini THẬT 5/5**: v1 lẩu→mukbang, v2 Lambo→xe, v3 mèo→thú cưng (sẵn); v4 iPhone→`_khac/Đánh giá công nghệ`⭐, v5 bóng đá→`_khac/Thể thao bóng đá`⭐ (folder MỚI). Cần **restart web_app** + **F5**. CHƯA test POST move live (auth chặn).
- **ĐÃ ĐÓNG GAP UI:** `fileTheLoai` giờ nhận diện folder-con-mới trong mặc định (`<default>/<genre>/` → label `sub:<genre>`, giữ hoa/dấu từ path gốc); thanh lọc + chip thẻ hiện mỗi thể loại mới thành nút riêng ✨. Sửa luôn bug escaping `plMoveOpen` (onclick dùng nháy kép + `aj` → vỡ attr; đổi sang nháy đơn theo chuẩn `aj`). Verify: node `fileTheLoai` 7/7 (kể cả "an uong2"≠"an uong", giữ dấu); `aj` onclick 5/5 (nháy đơn, `'`/`<`/`"`).
- **File đụng:** `web/index.html`, `web_app.py`, `phan_loai.py` (⚠ index.html CHUNG nhiều session). CHƯA commit (chờ user).

### ✅ [2026-07-01] Fix "chọn phong cách" (tab Lồng tiếng) làm MẤT từ điển tên riêng khi dịch Gemini — đụng dich_gemini_web.py
- **YC user:** test dịch bằng Gemini khi bật phong cách (nút chọn-nhiều: hài hước/viral/kịch tính/.../ngắn gọn) trong tab Lồng tiếng, xem flow có hợp lý không.
- **Test thật (live Gemini web, không login cần thiết trên máy dev — verify lại, KHÔNG cần login như tưởng ban đầu):** viết 1 SRT 15 câu dạng review (lộ kết luận câu 1) → chạy qua đúng hàm production `dich_gemini_web.dich_srt` với `DICH_QUY_TAC` y hệt web_app.py build, cho cả 9 phong cách + baseline. Lần đầu bug Ở SCRIPT TEST của tôi (quên `env=env` trong subprocess.run) khiến 10 bản output giống hệt nhau — ĐÃ sửa script, chạy lại ra 10 giọng văn khác biệt rõ rệt, đúng ý style, giữ nguyên 15/15 câu + timeline mọi trường hợp.
- **Bug THẬT tìm được:** `dich_gemini_web.py:304-311` — khi `DICH_QUY_TAC` set (chọn phong cách) code THAY THẾ HOÀN TOÀN thay vì gộp với từ điển tên riêng `translation_memory/*.md` (panel "Cải thiện dịch" → `00_user.md`) → chọn phong cách = mất nhất quán tên riêng/Hán-Việt. Root cause: kế thừa semantics "override" từ tính năng CŨ đã gỡ khỏi UI (`quy_tac_rieng`/"chung-riêng", xem comment `web_app.py:2761`) — không còn ai cần "chỉ dùng đúng rule đã tick" nữa.
- **Fix:** khi `DICH_QUY_TAC` non-empty → GHÉP `phong_cach + "\n\n" + doc_tm(tm_dir)` thay vì chỉ dùng phong_cach. `DICH_QUY_TAC=""` tường minh (CLI cũ) vẫn giữ hành vi cũ (không nạp gì) — backward-compat.
- **Verify:** py_compile OK; 2 test live: (1) style+glossary cùng log "Nạp phong cách đã chọn (130 ký tự) + từ điển tên riêng (1966 ký tự)"; (2) `DICH_QUY_TAC=""` vẫn 0 log (giữ nguyên hành vi cũ).
- **Nghi vấn phụ (nút Viral "câu mở gây tò mò" với video đã lộ kết luận):** KHÔNG phải bug — Gemini tôn trọng "giữ timeline/không thêm câu", chỉ đổi giọng câu 1 cho "chốt/nóng" hơn chứ không bịa hook. Chỉ là mô tả nút hơi quá lời, ưu tiên thấp, CHƯA sửa.
- **Đo thêm (user hỏi "tràn không") — ngân sách ký tự lồng tiếng (~48 ký tự/khe 3s, DUB_CPS=16) theo phong cách:** ngắn gọn 0.91× TB (an toàn nhất) < viral 0.94× < cảm xúc/kịch tính ~1.00× < baseline 1.05× (không chọn) < đời thường 1.06× < văn học 1.11× < hài hước 1.24× < **mc 1.31×** < **học thuật 1.59× TB, đỉnh 2.00×** (câu dài GẤP ĐÔI khe). Prompt phong cách không nhắc lại ràng buộc `[≤N ký tự]` sẵn có → style càng "nói nhiều" càng lấn ngân sách, dễ làm TTS bị nén/tăng tốc quá tay khi lồng tiếng.
- **QUYẾT ĐỊNH (user 2026-07-01): BỎ 2 nút "học thuật" + "mc"** khỏi UI (tràn ngân sách nặng nhất). Sửa: `web_app.py:2766-2772` xoá 2 key khỏi dict `_PC` (+ comment lý do); `web/index.html:3096` xoá 2 entry khỏi `PC_LIST`. An toàn ngược: nếu client cũ còn `hoc_thuat`/`mc` trong localStorage gửi lên, `_pc = [p for p in ... if p in _PC]` tự lọc bỏ (verify test: `['hoc_thuat']→[]`, không crash).
- **Verify cuối:** `py_compile web_app.py` OK (1 SyntaxWarning cũ không liên quan, dòng 5 docstring); JS `new Function` 2/0. Chỉ index.html → cần **F5**; web_app.py → cần **restart**.
- **File đụng:** `dich_gemini_web.py`, `web_app.py`, `web/index.html`. CHƯA commit (chờ user).

### ✅ [2026-07-01] Fix LỒNG TIẾNG ANH: dich_gemini_web prompt cứng "TIẾNG VIỆT" + nhồi từ điển Hán-Việt vào bản EN — đụng dich_gemini_web.py
- **Bug user chỉ ra:** "kiểm tra prompt tiếng Anh, hình như chưa có prompt → chưa lồng tiếng Anh được". ĐÚNG: `dich_gemini_web.dich_srt` prompt HARDCODE "Dịch ... sang TIẾNG VIỆT" bất kể `TARGET_LANG`. Chọn đích=en (giọng Kokoro/edge-en) → Gemini vẫn ra bản dịch TIẾNG VIỆT → TTS tiếng Anh đọc văn bản Việt = hỏng. (Google-fallback `phu_de._dich_vi` ĐÃ đọc TARGET_LANG đúng, chỉ nhánh Gemini-web thiếu.)
- **Fix 1 — prompt theo đích:** đọc `TARGET_LANG` (env web_app đã truyền, mirror `_dich_vi`) → `tgt` vi/en. Thêm nhánh `else` build prompt TIẾNG ANH mirror ĐẦY ĐỦ cấu trúc bản Việt (ngữ cảnh 1 đoạn liên tục, 1:1 số dòng = số dòng, ràng buộc `[Ts ≤N]` khớp lồng tiếng, cấm thêm lời dẫn/markdown). Nhánh vi giữ NGUYÊN 100%.
- **Fix 2 — từ điển Hán-VIỆT không nạp cho EN:** `translation_memory/*.md` bản chất Hán→Việt (tên Hán-Việt "赵露思=Triệu Lộ Tư", thuật ngữ wuxia Việt, luật độ-dài tiếng Việt) → nhồi vào prompt EN sẽ ép tên "Triệu Lộ Tư" vào câu tiếng Anh (đáng lẽ "Zhao Lusi"). Fix: `_glossary = doc_tm(tm_dir) if tgt=="vi" else ""`. Phong cách (DICH_QUY_TAC) VẪN giữ (lựa chọn văn phong của user).
- **Verify (live Gemini, đối chứng thật):** đích=en câu review → tiếng Anh tự nhiên, 15/15 câu, TB **0.90× ngân sách ký tự** (tốt hơn Việt ~1.05×); tên `赵露思` → EN ra **"Zhao Lusi"** (KHÔNG log [1b] glossary) vs VI ra **"Triệu Lộ Tư"** (CÓ [1b] 1966 ký tự). Regression đích=vi: bản dịch + glossary + Hán-Việt giữ y nguyên.
- **File đụng:** `dich_gemini_web.py` (CHỈ hàm `dich_srt`, cùng file với 2 mục phong-cách trên). CHƯA commit (chờ user). dich_gemini_web = gọi qua subprocess/in-process → chỉ cần chạy render mới, KHÔNG cần restart web_app.

### 🟢 [2026-06-30] Gộp automation Quy trình thành 1 CÔNG TẮC "🚀 Bật tự động" (tự cào→render) — đụng web/index.html
- **YC user:** "tự động từ cào đến render dễ dùng hơn". Trước RỐI: 3 thứ rời (🔄 tự render + ✅ Bật lịch cào + ✅ Bật theo dõi) → khách phải bật từng cái.
- **Làm:** rewrite `wfAutoRender` + thêm `wfMasterToggle`/`wfMasterStatus`. 1 công tắc `#wfMasterChk` "🚀 Bật tự động": ON → `workflow_auto_on` (tự render) + theo nguồn: cao→`lich_on`, kenh→`td_on`; guardLogin chặn nếu chưa login; OFF → tắt cả 3. Status gộp "🟢 Đang tự động: tự cào theo lịch → tự render". Nâng cao (gập): nút 🧪 chạy thử. Cảnh báo app phải mở.
- **GIỮ (không xoá):** wfAutoToggle/wfAutoStatus/wfLich*/wfTd* (tái dùng backend); 3 ref `wfAutoChk` còn lại đều guarded `if(c)` → không lỗi dù DOM bỏ.
- **Backend KHÔNG đổi** — tái dùng /api/workflow_auto_on·off·get + lich_* + td_*. Chuỗi: lịch cào (schtasks) tải video mới → `_wf_auto_worker` (`_auto_ung_vien`) tự render. Lưu ý: cào schtasks chạy cả khi đóng app, render chỉ khi mở app.
- **Verify:** JS new Function 2/0; symbol có; helper cũ còn. Chỉ index.html → **F5**. CHƯA test live (cần login+app chạy).
- **File đụng:** `web/index.html`. ⚠️ CHUNG nhiều session.

### 🟢 [2026-06-30] FIX login "xanh giả" (chưa login bị tính đã login) — auto-check live 3' + guardLogin cho Quy trình — đụng web_app.py, web/index.html
- **Gốc (user xác nhận "do cookie cũ"):** disk-check `_trang_thai_1lan` báo "in" chỉ theo CÓ MẶT cookie (sessionid/SESSDATA/web_session) — cookie CÒN trên đĩa dù phiên chết. Khi live-cache `_login_check.json` >5' (cũ) → badge/guardLogin rơi về cookie đĩa → "in" oan.
- **A (web_app.py):** thread `_login_recheck_worker`/`_khoi_dong_login_recheck` (gọi cạnh `_khoi_dong_auto_gom` trong main) — mỗi `VC_LOGIN_RECHECK_SEC`=180s: re-check LIVE nền đang "in" (rủi ro xanh-giả) qua `kiem_tra_login.py`, hoặc chỉ LÀM TƯƠI mtime cache nếu không có nền "in" → cache LUÔN <5' → badge không bao giờ đọc cookie cũ. BỎ QUA khi đang cào (`_proc`/`_dang_cao`) hoặc nền đang mở cửa sổ login (`_login_procs`) — tránh đè khóa profile ("out" oan).
- **C (web/index.html `wfRun`):** tab Quy trình TRƯỚC ĐÂY cào KHÔNG gọi `guardLogin` (tab Cào có) → cào bất kể login = "tính đã đăng nhập". Thêm: nguồn Cào/Kênh nền Trung (≠yt/tt) → `await guardLogin(plat)`; "out" → chặn + mở login. (guardLogin đã xác minh LIVE qua /api/login_kiemtra_one, KHÔNG tin disk.)
- **Verify:** web_app py_compile OK; index.html JS new Function 2/0. **Part A cần RESTART web_app** (thread mới); Part C **F5**.
- **File đụng:** `web_app.py`, `web/index.html`. ⚠️ CHUNG nhiều session (wfauto/ViralVoice/Task Queue) — chỉ thêm block login-recheck + guardLogin trong wfRun.

### 🟢 [2026-06-30] FIX tab Quy trình (Nguồn=Cào) KHÔNG dịch từ khóa VI→ZH → Douyin search 0 — đụng web/index.html
- **Bug user gặp:** ▶ Chạy cào Douyin search "review phim" (tiếng Việt) → 0 kết quả (4 retry). Douyin nền Trung, từ khóa VN ra 0.
- **Gốc:** `wfRun()` gửi `wfState.nguon.cfg.keyword` NGUYÊN VĂN lên `/api/workflow_run` — KHÔNG qua `autoZh` (tab Cào thường có `kwForSearch()`→`autoZh` ở 5072-5080, tab Quy trình thiếu).
- **Fix:** thêm vào đầu `wfRun()`: nếu `nguon.choice==="cao"` + platform ∈ `_NEN_TRUNG` + keyword non-Hán → `autoZh([keyword])` rồi ghi lại `cfg.keyword` + `wfConfigRender()` (user thấy từ khóa Trung). Mirror tab Cào, idempotent (autoZh bỏ qua nếu đã có chữ Trung). Né race đa-session bằng Python atomic.
- **CÒN GAP (chưa fix):** hẹn-giờ `wfLichOn`/`wfLichRun` + nhánh search cũng gửi keyword raw → cùng bug khi cào search theo lịch. (Theo dõi = kênh, không dính.)
- **Verify:** grep wfRun có autoZh; JS new Function 2/0. Chỉ index.html → **F5**. ⚠️ Có thể VẪN 0 nếu Douyin 风控/rate-limit (khác bug này).
- **File đụng:** `web/index.html`. ⚠️ CHUNG nhiều session.

### 🟢 [2026-06-30] GỠ UI "Gợi ý kênh" (giữ backend dead code + giữ Trợ lý AI tim_kenh) — đụng web/index.html
- **YC user:** xoá chức năng gợi ý kênh. Chốt: GỠ UI, GIỮ backend (/api/suggest + bili_goi_y.py/yt_goi_y.py + goi_y_kenh*() dead code); GIỮ `tim_kenh` của Trợ lý AI.
- **Gỡ (web/index.html):** panel inline `cgyWrap` (tab Cào); JS standalone chết `gySel`/`#gyPlats`/`suggest`/`HO_TRO`/`MO_PHONG`/`sendSaved`/`dienVaoCaoNgay`; JS inline `cgySaved`/`cgyToggle`/`cgySuggest`/`cgyChonHet`/`cgyAct`; call-site platform-onclick + updateMode; dọn text trỏ "Gợi ý kênh".
- **GIỮ (dùng chung):** `fmt`(3755), `xemKenh`(4849 list kênh đã dán), `themVaoTheoDoi`, `savedKenh`(state chung), `_may_goi_y`/`mayGoiY`/`xlGoiY`(=gợi ý ENGINE TTS, KHÁC), `_goi_y_kenh.json` trong `_nguon_avatar`.
- **Cách sửa:** IDE/session khác auto-save chèn giữa Read↔Edit ([[ide-ghi-de-mat-code]]) → né bằng script Python atomic (anchor nội dung, str.replace đúng vùng gợi-ý, 1 lần ghi UTF-8 no-BOM; dry-run 12/12 khớp trước --apply → KHÔNG đè vùng ViralVoice/Bilibili của session khác).
- **Verify:** JS new Function 2/0; grep symbol gỡ=0; 0 call-site mồ côi; xemKenh/fmt giữ; auto-gom không clobber. Chỉ index.html → **F5**.
- **File đụng:** `web/index.html`. ⚠️ CHUNG với 3 session khác đang sửa index.html (ViralVoice/Bilibili-deep/Kokoro/Task Queue) — chỉ gỡ vùng gợi-ý-kênh.

### 🟢 [2026-06-30] Bỏ F5-TTS + đổi tên engine → "ViralVoice" (giấu model free) — đụng web/index.html
- **User:** bỏ F5 (OmniVoice lo clone, F5 nặng) + đổi NHÃN để khách không phàn nàn "model free"/tên mã-nguồn-mở.
- **Bỏ F5 (UI):** gỡ `<option value="f5">` khỏi `xlTts` (tĩnh + xlApplyLangVoices VN branch) + `arTts`; clone wrap chỉ còn omnivoice (xlUpd/arUpd/xlNgheThu: `f5||omnivoice`→`omnivoice`); ẩn nút `btnCaiF5` ("Cài giọng Clone" F5-install) + bỏ note f5 trong caidat. **Backend f5 dispatch GIỮ (dead, backward-compat config cũ).**
- **Đổi tên (value giữ nguyên, chỉ label):** piper→"ViralVoice — Tiêu chuẩn (nhanh)", omnivoice→"ViralVoice — Clone giọng (cần GPU)", edge→"ViralVoice — Online", kokoro→"ViralVoice — English (offline)", edge-en→"ViralVoice — Online (English)"; optgroup arTts "Giọng Việt (CPU...)"→"ViralVoice — Giọng Việt (nhanh)"; giọng banmai "Model — nữ"→"Ban Mai — nữ". Label "Engine TTS"→"Giọng đọc".
- **VERIFY Playwright dev:** VN=[piper/omnivoice/edge ViralVoice], EN=[kokoro/edge ViralVoice], `con f5?False`, 0 pageerror. node 0 lỗi.
- **CÒN:** F5 vẫn có thể trong bundle installer (stage_app) = weight — gỡ build-side nếu muốn nhẹ thật. **CHƯA commit.** File: `web/index.html`.

### 🟢 [2026-06-30] Cào KHÔNG TRÙNG (deep-new) mở rộng Bilibili + làm rõ YT/TT — đụng bilibili/core.py, web_app.py, web/index.html
- **Trước:** chỉ Douyin. **Giờ:** thêm **Bilibili (search)**.
- **bilibili/core.py:** +`_seen_ids` + `_ledger_path/_load_seen/_mark_seen` (mirror Douyin, folder `bilibili/`); `get_bilibili_video` sau `store_video` → `_mark_seen(aid)+_mark_seen(bvid)`; `search()` +DEEP (MC_DEEP_NEW/MC_DEEP_PAGE_CAP=40) nới trần while + bỏ video đã-tải TRƯỚC fetch-detail + trang-toàn-seen→sang trang. **GATE sau `DEEP` → cào thường 0 regression.** py_compile OK.
- **web_app.py:2065:** MC_DEEP_NEW = `(dy & search/creator) OR (bili & search)`. py_compile OK.
- **YT/TikTok:** tai_ytdlp `download_archive` LUÔN bật (line 591) → đã tự bỏ trùng sẵn (chỉ chưa "đào đủ N mới"). KHÔNG đụng.
- **index.html:** `_khongTrungVis` hiện ô cho dy(search/creator)+bili(search); label cập nhật. node 0 lỗi.
- **PREVIEW:** không đào sâu (liệt kê+badge "đã tải"+sort, tự chọn). Bili cào-chính giờ ghi ledger(aid) → preview badge bili ĐÚNG (verify `video_id=aid` store/bilibili:58 khớp `_gan_badge`).
- **CHƯA test cào LIVE bili deep** (cần login+laptop). **CHƯA commit.** File: `bilibili/core.py`, `web_app.py`, `web/index.html`.

### 🟢 [2026-06-30] Workflow Engine (tab "🔀 Quy trình") — pipeline block-card dọc + validate + preset + chạy 1 flow
- **Việc:** chuyển tư duy Scheduler→Workflow. Tab MỚI: danh sách block dọc (Nguồn → Băm(tùy) → Render → Xuất(tùy)),
  mỗi block ON/OFF + thay-thế lựa chọn hợp lệ; mô hình I/O (artifact `videos`/`final`) tự validate phụ thuộc
  (tắt upstream → disable + báo đỏ "thiếu đầu vào"); lưu/nạp nhiều preset; nút ▶ chạy 1 flow thật end-to-end
  (orchestrator nền, gọi LẠI endpoint sẵn có — KHÔNG viết lại auto-chain).
- **File đụng:** `web/index.html` (tab+JS mới), `web_app.py` (endpoint `/api/workflow_*` MỚI + orchestrator nền).
  ⚠️ CHUNG file với session auto-gom [2026-06-29]. Tôi CHỈ THÊM tab/endpoint mới (phụ-trợ, on-demand), KHÔNG
  đụng `_auto_gom_worker`/`_queue_worker`/vùng họ. Reuse: chay_crawl, hàng đợi render, cat_nho, gom_dang_bai.
- **Trạng thái:** 🟢 đang code (MVP). Backend đổi → restart web_app; index.html F5. CHƯA commit từng phần.

### ✅ [2026-06-29] Fix XHS nội địa "đăng nhập rồi vẫn bắt login lại" — đụng kiem_tra_login.py
- **Gốc (chẩn THẬT, không đoán):** XHS nội địa (xiaohongshu.com) **KHÔNG có id_token** (dump cookie: web_session len=38 nhưng id_token RỖNG; id_token chỉ bản rednote) → `_check` bỏ shortcut → rơi xuống `_xhs_dom`. DOM cũ đếm nút '登录' + avatar: trang `/explore` **LUÔN có 6 nút '登录'** (mỗi post 1 nút) + 30 avatar FEED (không phải của user) → `hasLogin=true` → **LUÔN 'out'** bất kể login → đăng nhập rồi vẫn bị bắt login lại.
- **Fix:** `_xhs_dom` đọc **`window.__INITIAL_STATE__.user.loggedIn`** (Vue ref — cờ login THẬT của XHS) TRƯỚC: true→'in', false→'out'. Giữ DOM '登录'/avatar làm fallback (rednote / state vắng). py_compile OK; test THẬT trên profile dev: `loggedIn=false` → trả 'out' đúng, không crash.
- **Lưu ý:** profile XHS dev thật ra **ĐÃ HẾT HẠN** (`loggedIn=false`, web_session là cookie chết) — cần đăng nhập LẠI mới thành `loggedIn=true` → 'in'. (Trước đây check hỏng nên 'out' cả khi đã login.)
- **File đụng:** `kiem_tra_login.py` (subprocess → hot-sync laptop được, hiệu lực lần check kế).

### 🟢 [2026-06-29] AUTO-GOM đăng bài (vá khúc ĐỨT Cào→Render→[GOM]→LoHa) + UX tab Đăng bài — đụng web_app.py, web/index.html
- **Gốc:** `gom_dang_bai.gom` CHỈ gọi từ nút tay `/api/trang_gom` → render xong KHÔNG tự gom → video đứng, không tới LoHa. 2 đầu tự động (cào hẹn giờ + auto-render + LoHa watcher) nhưng khúc GIỮA tay.
- **Làm (Ưu tiên 1 + quick-win UX):**
  - **web_app.py:** thread NỀN độc lập `_auto_gom_worker`/`_khoi_dong_auto_gom` (gọi cạnh `_khoi_dong_task` trong main) — định kỳ `VC_AUTOGOM_SEC`=600s, đọc `cfg.auto_gom` → chạy `gom`. KHÔNG đụng `_queue_worker`. `_gom_lock` serialize gom-tay vs gom-tự (chống đua double-write). Helper `_chay_gom(dry,im_lang_khi_rong)`. trang_get +`auto_gom` + đếm `dang_bai` đổi sang `gom_dang_bai.DANG_BAI` (userData). trang_save +`auto_gom`. trang_gom +nhánh `dry`.
  - **web/index.html:** toggle `#dbAutoGom`, nút `🔍 Thử (dry-run)` (`dbDry`), validate Page ID inline `dbPidChk`. dbLoad/dbLuu mang `auto_gom`.
- **File đụng:** `web_app.py`, `web/index.html`. ⚠️ CHUNG file với session Task Queue [2026-06-28] + Kokoro EN [2026-06-29] — tôi CHỈ thêm block auto-gom + endpoint trang_*, KHÔNG đụng vùng của họ.
- **Trạng thái:** 🟢 đang code + test. Backend đổi → CẦN restart web_app; index.html F5. CHƯA commit.

### 🟢 [2026-06-29] Bản tiếng Anh: lồng tiếng EN bằng Kokoro (kokoro-onnx) — đụng kokoro_synth.py(MỚI), localize.py, web_app.py, web/index.html, MediaCrawler/pyproject.toml
- **Việc:** thêm engine TTS **Kokoro-82M (kokoro-onnx, Apache-2.0, CPU-friendly)** làm giọng lồng tiếng **mặc định cho đích=EN**, **fallback edge-tts en-US** khi thiếu model/lỗi. Nhánh giọng `vi` (Supertonic/Piper/F5/edge) **KHÔNG đụng**. Sau đó (Phần B) regen i18n UI EN.
- **Quyết định user:** runtime = **kokoro-onnx** (không torch, tải onnx+voices.bin on-demand như Piper); vai trò = **mặc định EN, edge fallback**; gating **FREE** (offline như Piper).
- **Kế hoạch (bảng A1-A6):** A1 `kokoro_synth.py` mirror [[giong_piper]] + tải model on-demand vào `LIC_CACHE_DIR/runtime`; A2 `localize.long_tieng_kokoro` mirror `long_tieng_piper` → `_ghep_track_khop` (24k mono khớp), dispatch trong `chay()` đích=en→kokoro→fallback edge; A3 web_app truyền `--tts kokoro --voice`; A4 index.html thêm giọng vào dropdown `splitTts` DÙNG CHUNG (3-4 chỗ); A5 pyproject+uv.lock; A6 benchmark RTF CPU TRƯỚC khi đặt mặc định.
- **Rủi ro:** localize.py dùng chung render+dub [[bug-localize-cross-cutting-render-dub]] → sửa phẫu thuật; KHÔNG chạy `_chuan_hoa_tts` (chuẩn-hoá VN) cho EN; deps/URL model kokoro-onnx + espeak-ng G2P verify lúc code.
- **BENCHMARK XONG (2026-06-29) — xem [[kokoro-tts-gpu-cudnn-pin]]:** GPU RTX3050 fp32 **RTF 0.39 (×2.6 realtime)** vs CPU i3/laptop ×0.5 (GPU ~5×). 28 giọng EN, sr24000 khớp `_ghep_track_khop`. **Mấu chốt GPU: pin cuDNN 9.5.1.17** (9.23 vỡ conv→lùi CPU) + onnxruntime-gpu 1.22(CUDA12) + preload_dlls + ONNX_PROVIDER env + model fp32.
- **KIẾN TRÚC CHỐT:** onnxruntime-gpu xung đột onnxruntime CPU (RapidOCR dùng) → **`.venv_kokoro` RIÊNG + gọi qua subprocess** (mirror F5/OmniVoice), GPU-conditional install như cai_gpu/cai_omnivoice; non-GPU = onnxruntime CPU + song song N luồng. KHÔNG nhét onnxruntime-gpu vào venv chính.
- **i18n (Phần B) ĐÃ LÀM TRƯỚC:** regen `lang_en.js` (988→**1185 cặp**, +197 chuỗi EN mới cho Task Queue/render worker/ETA...) — `_gen_lang_en.py`+`_lang_overrides.py`, node --check OK. **Ẩn giọng-Việt khi đích=EN** để gộp vào A4 (wire Kokoro).
- **File đụng (i18n):** `web/lang_en.js`, `web/lang_en.json` (regen, đã đổi).
- **CHỐT 8 GIỌNG (user duyệt qua VOICES.md hạng A/B + render thử 28 video laptop):** `af_heart`(mặc định,A), `af_bella`(A), `am_michael`,`am_fenrir`,`am_puck`(B nam Mỹ), `bf_emma`(B nữ Anh), `bm_george`,`bm_fable`(B nam Anh). BỎ adam(D)+nhóm C. Bench artifact 28 video ở laptop OneDrive\Desktop\kokoro_test.
- **BACKEND XONG + VERIFY GPU LAPTOP THẬT:**
  - `kokoro_synth.py`(MỚI): worker venv riêng, GPU auto (preload_dlls+ONNX_PROVIDER+fp32, lùi CPU int8). **Test thật .venv_kokoro GPU: `[kokoro] GPU (CUDA)` → seg_0/1/2.wav OK.**
  - `cai_kokoro.py`(MỚI): cài CHO MỌI MÁY (Kokoro chạy cả CPU); GPU→onnxruntime-gpu 1.22+cuDNN 9.5.1.17(pin)+nvidia cu12+fp32; CPU→onnxruntime CPU+int8. venv ở `LIC_CACHE_DIR/runtime/.venv_kokoro` (KHÔNG app-src, giống .venv_f5). **Test thật GPU: KOKORO_DONE.**
  - `localize.py`: +KOKORO_* constants + `long_tieng_kokoro` (mirror F5/omni) + nhánh dispatch `tgt!="vi" and tts_engine=="kokoro"`→kokoro→lùi edge en. py_compile OK. Dub cache-key đã gồm voice+tgt (đổi giọng bust đúng).
  - `web_app.py`: whitelist chay_long_tieng +"kokoro"(725) + voice passthrough(774) + `chay_tai_kokoro`(cài nền, gated `target_lang()=="en"`) + trigger main. Render `_lenh_xu_ly` truyền --tts/--voice tự do (ko chặn). Gating: kokoro=FREE (adv chỉ f5/clone). py_compile OK.
- **File đụng (backend):** `kokoro_synth.py`+`cai_kokoro.py`(mới), `localize.py`, `web_app.py`. CHƯA commit.
- **CÒN LẠI:** (1) `web/index.html` dropdown — thêm 8 giọng Kokoro khi đích=EN + ẩn giọng VN (chưa làm); (2) dub_preview.py hỗ trợ kokoro (nút "nghe thử" — phụ, dòng web_app 4322 giữ nguyên); (3) **e2e test laptop: render target=en + tts=kokoro qua UI thật + restart app**; (4) deploy app-src laptop + stage_app (file mới kokoro_synth.py/cai_kokoro.py phải vào installer).
- **UI DROPDOWN (Render panel) XONG — `web/index.html`:** đổi `xlLangTgt`=en → `xlApplyLangVoices()` rebuild: `xlTts`=[Kokoro(mặc định)+Edge English], `#xlPiperVoice`=8 giọng Kokoro (tái dùng picker), `#xlEdgeVoice`=en-US (Aria/Guy/Jenny/Sonia/Ryan); ẨN F5/OmniVoice/Piper-VN/edge-vi. vi → giữ nguyên. `xlUpd` hiện picker cho kokoro; `xlRun` lấy voice kokoro từ #xlPiperVoice (gửi tts="kokoro"+voice). Hook xlLangInit+xlLangSave. **JS 0 lỗi cú pháp (new Function ×2).**
- **Research (user yêu cầu):** review đa nguồn xác nhận Kokoro = chất lượng #1 open-weight + Apache-2.0 + nhanh GPU; Piper nhanh hơn CPU nhưng "nghe máy" → giữ Piper cho VN, Kokoro cho EN. Quyết định Kokoro+Edge-English chuẩn.
- **CÒN LẠI:** (1) **arTts (auto-render) CHƯA sync** — vẫn giọng VN khi EN (dropdown optgroup khác cấu trúc; niche, flag); (2) **Chromium click-test + e2e render target=en+Kokoro trên laptop** (CHƯA — cần deploy+restart app); (3) dub_preview kokoro (nút nghe thử, phụ); (4) deploy app-src laptop + **stage_app.ps1 phải thêm kokoro_synth.py + cai_kokoro.py vào installer** + commit.
- **NGHE THỬ GIỌNG (VN+EN) XONG:** nút UI từng bị gỡ → THÊM LẠI nút "🔊 Nghe thử giọng" panel Render + `xlNgheThu()` (đọc engine+voice đang chọn, câu mẫu EN nếu đích=en, fetch /api/dub/preview → phát wav). `dub_preview.py` +choice "kokoro" → `localize.long_tieng_kokoro`. web_app endpoint +whitelist kokoro + voice/text mặc định EN khi kokoro + truyền --voice. **py_compile + JS 0 lỗi.** Chạy cho CẢ tiếng Việt (edge/piper/f5/omni) LẪN tiếng Anh (kokoro/edge-en).
- **File đụng (thêm):** `dub_preview.py`, `web_app.py`, `web/index.html`.
- **CHƯA verify e2e/laptop** nghe-thử kokoro (cần .venv_kokoro ở runtime app + restart) — gộp vào bước deploy.
- **FIX "đổi ngôn ngữ bị lỗi" (user báo):** `xlLangInit` chỉ chạy khi CLICK tab Render → đổi `#appLang`=en → reload vào sẵn tab Render thì panel Render VẪN hiện giọng Việt (sai). Fix: `_xlInitVoices` đồng bộ `xlLangTgt=APP_LANG` lúc init. **VERIFY Playwright chromium dev: localStorage app_lang=en → load KHÔNG click tab → xlTts=[kokoro,edge]+picker 8 Kokoro, 0 pageerror.** Cũng test target-switch + interface-switch(_walkI18n dict thật 1185) + kết hợp = 0 lỗi. File: `web/index.html`.
- **Trạng thái:** 🟢 CODE XONG (backend+UI Render+nghe-thử VN/EN, lang-switch fixed, Playwright verified). **CÒN: deploy laptop + restart app + e2e render EN** (arTts sync niche để sau). CHƯA commit.

### ✅ [2026-06-29] Fix "app đóng gói VẪN hiện web login" (đăng nhập lần 2) — đụng desktop/main.js
- **Gốc (smoking gun):** `startBackend()` probe cổng 8770 → **CÓ ai đáp thì REUSE**, chỉ spawn backend mới khi 8770 TRỐNG. Nếu web_app CŨ/standalone đang giữ 8770 (auto-render bật lúc boot qua Startup .lnk, HOẶC bản cũ crash mồ côi) → app Electron nối vào bản đó. Bản standalone **KHÔNG có `VC_EMBEDDED`** → `/api/auth/bootstrap` trả `embedded:false` → index.html không auto-auth → **tool đòi đăng nhập LẦN 2 (web login hiện)**. (Code `af8e30f` embedded-skip của longloha VẪN đúng — lỗi ở chỗ app dùng nhầm backend standalone trên 8770.)
- **Fix:** `startBackend` **LUÔN `killByPort(PORT)` trước rồi spawn bản EMBEDDED** (bỏ probe-reuse). web_app ThreadingHTTPServer có `allow_reuse_address` nên bind lại ngay; chừa 0.8s cho taskkill nhả cổng. `node --check` OK.
- **Lưu ý:** user chạy auto-render NỀN standalone (Startup .lnk) sẽ bị kill khi mở app — nhưng web_app embedded lo auto-render lúc app mở (đóng app thì dừng). Khách thường = leftover mồ côi → kill là đúng 100%.
- **⚠️ `desktop/main.js` = LÕI ELECTRON → CẦN BUILD + RELEASE lại** mới tới khách (KHÔNG hot-sync python được).
- **File đụng:** `desktop/main.js`.

### 🟢 [2026-06-28] Task Queue (scheduler chung) — xếp hàng NHIỀU job cào [P1 BACKEND] — đụng web_app.py
- **Việc:** hàng đợi TÁC VỤ chung (`kind='crawl'` giờ; render/translate/tts thêm sau bằng 1 nhánh) — user bấm nhiều job cào (Douyin/Bili) rồi đi ngủ, KHÔNG ngồi canh. Gap lớn nhất từ audit: trước CÀO 1-job/lúc (`_proc`/`_dang_cao`). Render GIỮ `_queue` riêng (gộp sau khi ổn định).
- **Backend (web_app.py):** `_tasks/_task_lock/_task_them/_task_worker/_task_nap/_task_luu` + `_khoi_dong_task()` (gọi cạnh `_khoi_dong_queue` trong main 5198). Worker 1-luồng chờ→đang→xong/lỗi; kind=crawl **TÁI DÙNG `chay_crawl` nguyên vẹn** (chỉ điều phối + đợi `_proc` xong → KHÔNG đụng crawl logic Douyin/Bili). Persist `DATA_DIR/_task_queue.json` (userData ghi được; app-src READ-ONLY) + resume (dang→cho khi mở lại app). Endpoint: `/api/task/{add,list,remove,clear}`.
- **ĐÃ XONG + COMMIT:** backend `354d0ea` + UI `e6cd079`. UI: panel "Hàng đợi tải" (tab Cào) poll `/api/task/list`, nút "📋 Thêm vào hàng đợi" (`addToQueue`→/api/task/add, GIỮ "Cào hết" chạy-ngay), xoá task (✕) + "Xoá xong/lỗi".
- **Verify (PHẠM VI CHÍNH XÁC):** đã kiểm chứng **SCHEDULER BACKEND** qua stress-test `chay_crawl()` THẬT trên laptop (import web_app + worker thật, tải video thật): (1) **3 job tuần-tự** đúng thứ tự [1,2,3], KHÔNG race, `_proc`/`_dang_cao` không kẹt; (2) **resume-giữa-queue**: giả lập crash khi Task2 'đang' → restart (`_task_nap`) → Task2 'đang'→'cho' **RE-RUN** rồi Task3, Task1 (xong) **KHÔNG** chạy lại → đúng thiết kế "dở thì chạy lại từ đầu, xong thì skip". ⚠️ **UI + HTTP flow MỚI verify TĨNH** (endpoint có trong file + node-check) — **CẦN restart app 1 lần + click-test để xác nhận end-to-end** (chưa qua tầng `/api` thật vì CSRF + app đang chạy tiến trình cũ).
- **DEPLOY laptop:** ĐÃ patch app-src `web_app.py` + `index.html` (backup `.bak_task`), py_compile + node OK, TaskManager chạy thật. **CHƯA:** restart app ViralCrawl (load endpoint mới) + 1 click-test UI.
- **File đụng:** `web_app.py`, `web/index.html`. ĐÃ COMMIT (354d0ea + e6cd079).
- **#2 Login-aware + Cancel/Retry/reason (commit `12ef59e`+`1946726`):** worker check `_trang_thai_1lan` trước crawl → "out" = "🔒 Cần đăng nhập" + skip cùng nền + chạy nền khác; `reason` chuẩn (login_expired/cancelled/timeout/error); `/api/task/{cancel,retry,retry_all}` (cancel-đang-chạy = dung_crawl kill _proc → "⛔ Đã huỷ" → worker tiếp); UI nút ⛔/↻/"↻ Thử lại lỗi". **Verify laptop THẬT:** login-aware skip + reason; cancel task đang-chạy → đã-huỷ + worker tiếp tục; resume-giữa-queue. Deploy laptop = OVERWRITE web_app.py+index.html (forward-sync, backup `.bak_c2`).
- **#3 Progress/ETA + Retry-Platform + Stop-After-Current + Notification (`55aa150`+`0ccdfa3`):** task hiện "📊 N/M·MB/s·còn ~Zm"+thanh% (`_tien_do_cao` từ `_eta_log`); `/api/task/retry_all` lọc platform → "↻ Douyin (N)"; `_task_pause` Stop-After-Current (worker nghỉ sau task hiện tại, KHÔNG kill) + nút ⏸/▶; Notification OS toast 3 trạng-thái (hoàn tất/lỗi/chờ-đăng-nhập). **Verify:** progress crawl thật (pct 20→100+ETA); pause on→0 chạy/off→chạy; retry platform=dy chỉ dy; edge-case core (corrupt-file→[], worker-exception sống, đua add/remove, atomic persist).
- **PHASE 2 #1 — Tải video SONG SONG (`b0342aa`):** Douyin+Bilibili core.py — `_tai_nen`/`_drain_tai` (asyncio gather+Semaphore MC_DL_CONCURRENCY mặc định **2**); crawl/API VẪN tuần tự (semaphore fetch-detail/play_url không đổi), chỉ phần TẢI bytes chồng lấp. Douyin nhánh DEEP giữ tuần tự (đếm video mới); creator-deep đếm ở `_collect_user_posts` nên tải nền OK. Bili 3 site (search/time_range/specified). An toàn race vì asyncio 1-thread. Deploy laptop backup `.bak_dl`. **A/B PASS: conc=1 peak 2.8 MB/s vs conc=2 peak 5.4 MB/s (~1.9× throughput), tải đúng không crash.**
- **PHASE 2 #2 — INSTRUMENT (đo trước khi tối ưu, `47982aa`+`68dad3e`):** KHÔNG refactor pipeline khi chưa có data. `localize.chay` ghi `_render_profile.jsonl` mỗi render: `{video, mb, dur, total, stages:[[name, secs, {cpu,gpu,ram,vram}]]}` — `_tg` timer (có sẵn) + `_ResMon` dep-free (ctypes GetSystemTimes/GlobalMemoryStatusEx + nvidia-smi; venv prod KHÔNG có psutil/pynvml). `dich_gemini_web` tách `GEMPROF|open/load/wait` (gemini-web tạo profile TẠM mỗi render = startup cố định). VC_PROFILE_LOG env ghi chung. Deploy laptop `.bak_prof`/`.bak_res`. **Validate THẬT:** ⏱ ASR 35.5s(gpu16%) · Dịch 27.6s(gpu22%) · TTS 31.6s(gpu12%) — GPU IDLE mọi stage, VRAM 759/4096MB (headroom lớn). Bẫy test: tên file VN→src-lang sai (early-return); gemini profile-tạm login-wall; thiếu PYTHONUTF8→cp1258 crash (prod web_app set sẵn nên OK).
- **PHASE 2 #3 — PERSISTENT RENDER WORKER Step 1 (`4a0d179`, thiết kế `plans/260628-persistent-render-worker/`):** Data (laptop, batch google sạch + startup breakdown) cho thấy startup ~11-23s/render (Whisper import 2s+load 5.5s, OCR 1.5s, Piper-pool 2.3s×N≈14s) + Gemini cold-flaky (treo 400s lần đầu). → `render_worker.py`: process SỐNG, đọc job JSON-line stdin → `xu_ly_chon.main()` IN-PROCESS (set sys.argv + bắt SystemExit, KHÔNG refactor xu_ly_chon) → emit log/done stdout; WhisperModel/OCR warm qua `_MODEL` cache. Bug fix: `_emit` ghi `_REAL_OUT` (pipe IPC) chứ KHÔNG `sys.stdout` (bị `_Tee` redirect lúc job→đệ quy→code=1). **VERIFY laptop THẬT: 2 job → code=0; ASR 39.4s(cold)→11.3s(warm)=−28s/render; tổng 117→58s.** Audit: `chay()`/`main()` gọi in-process OK (sys.exit=SystemExit bắt được). **CHƯA wire web_app `_queue_worker`** (đang subprocess/video) + chưa warm Piper-pool/Gemini-browser (Step 2-3).
- **RedNote (`10f2a18`):** thử browser-download (`download_note`), test laptop → note-detail/video GATED (xsec_token ở SPA-state, click card virtualized) = đúng tường cũ → GIỮ preview-only, code vô hại để sẵn. [[xhs-rednote-cao-104-khong-quyen]].
- **PHASE 2 #3b — WIRE web_app → worker (`45f04c4`) — LIVE PRODUCTION:** `_queue_worker` render qua `render_worker.py` bền (worker-primary) + **subprocess FALLBACK** (worker chết/treo→vẫn render); `_rw_ensure`/`_rw_kill`/`_rw_reader`+`_rw_evq` (1 reader chung); `_render_via_worker` (gửi job stdin→event qua queue, trả T/F/None/'cancelled'); `_xl_xu_ly_dong` parse chung; cancel=cờ+`_rw_kill`; watchdog stall `VC_RENDER_STALL`=1800s; tắt khẩn `VC_RENDER_WORKER=0`; dịch-thủ-công giữ subprocess. Bug fix: `_emit` ghi `_REAL_OUT` (KHÔNG sys.stdout bị _Tee redirect). **VERIFIED LIVE (user restart app + render 3 video THẬT qua UI):** rw=2 worker sống xuyên suốt, localize IN-PROCESS (loc=0), 3/3 xong, ASR warm (video ngắn 8.7s vs ~22s cold), không fallback/crash. wire/real-opts test (Piper+watermark) ra _xuly.mp4 ok.
- **Trạng thái:** ✅ queue + parallel-download + instrument + **Persistent-Worker LIVE PRODUCTION (warm, verified 3 video thật)**. Kế (tuỳ chọn): Step 2 warm Piper-pool (cache PiperVoice global, −~14s video dài) + Step 3 warm Gemini-browser (−~24s + fix cold-flaky). Concurrency=2 CHỜ benchmark Piper (CPU dễ bão hoà). KHÔNG parallel-crawl, KHÔNG RedNote. Restart app laptop = USER tự tắt+mở (Electron env; SSH không làm được). Gotcha batch nền: WMI+self-redirect+poller ([[laptop-deploy-ssh-tailscale]]).

### ✅ [2026-06-27] Tab "Chưa render / Đã render" trong picker Render + fix badge sai — đụng web/index.html
- **Vấn đề:** picker tab Render (`xlGrid`/`xlLoadVideos`) chỉ hiện video GỐC (`fileNguon==="goc"`, lọc bỏ output `_xuly/_phude/_longtieng`) → KHÔNG phân biệt gốc đã-render hay chưa; badge `stTxt` lại check tên `_xuly` của CHÍNH file gốc (luôn false vì gốc đã lọc) → **badge LUÔN "Chưa render"** (sai).
- **Fix:** `xlTrangThai(f)` = gốc đã có bản render chưa (build `xlRenderedBase` từ `r.files` — base có output `_daRenderF`). Thêm thanh tab `.seg #xlRenderTabs`: **⬜ Chưa render / ✅ Đã render / Tất cả** (mặc định Chưa render — chính là việc cần render). `xlLoc()` lọc theo tab; badge dùng `xlTrangThai` (hết "luôn Chưa render"). Đếm + tab gọi trong `xlRenderGrid`.
- **Self-contained:** `xlRenderedBase` build từ chính fetch của Render (KHÔNG phụ thuộc `_daRenderBase`/`allFiles` của tab File đã tải — có thể chưa load). Dùng `_xlBase` (map gốc↔output cùng base).
- **Verify:** node --check OK; `.seg` CSS có; 3 hàm defined; test logic PASS (vidA có _xuly→Đã render; vidB→Chưa; tab lọc đúng). Deploy laptop (backup `.bak_rtab`). index.html fresh → **F5**, không restart.
- **File đụng:** `web/index.html`. **⚠️ chưa commit.**
- **Trạng thái:** ✅ code + verify + deploy laptop — chờ user F5 click-test + commit.

### ✅ [2026-06-27] FIX render lỗi + picker "Giọng CPU" rỗng (regression d15206b xoá splitTts) — đụng web/index.html
- **Lỗi user (laptop):** chọn "Giọng CPU" + render BỊ LỖI. **Gốc:** commit `d15206b` (gỡ tab Lồng tiếng) vô tình xoá nguyên block JS `splitTts()` + `PIPER_VOICES` + `integratePiperVoices()` (nằm gần section dub) — NHƯNG `splitTts` vẫn được render gọi ở 5 chỗ (xlUpd 2761, render-config 2829/2830, arUpd 3030/3041) → **ReferenceError: splitTts is not defined** khi mở/đổi render = render hỏng; + picker `#xlPiperVoice` (+ arTts) không được populate = **rỗng** = "chọn giọng CPU lỗi". 1 gốc → 2 triệu chứng.
- **Fix:** khôi phục 3 hàm/const (lấy từ `git show d15206b^`) chèn trước `function xlUpd`: `PIPER_VOICES` (8 giọng), `splitTts` (hoisted), `integratePiperVoices` (fill xlPiperVoice + merge optgroup arTts; `if(!s)return` tự bỏ qua dubTts/rdTts đã gỡ). DOMContentLoaded hook.
- **Verify:** node --check OK; splitTts logic PASS (piper:voice/piper/f5); fill xlPiperVoice PASS; quét tĩnh hàm-tự-do-thiếu-định-nghĩa = không còn (mọi hàm render gọi đều defined). **Laptop:** xác nhận DÍNH (`splitTts=0,PIPER_VOICES=0`) → deploy index.html đã fix (backup `.bak_splittts`); index.html phục vụ fresh → chỉ F5, KHÔNG restart.
- **File đụng:** `web/index.html`. **⚠️ chưa commit — COMMIT SỚM** (regression đang ở main!). Bài học: [[ide-ghi-de-mat-code]] họ hàng — gỡ section UI dễ xoá nhầm HELPER DÙNG CHUNG.
- **Trạng thái:** ✅ fix + verify + deploy laptop — chờ user F5 click-test + commit.

### ✅ [2026-06-27] Cào KHÔNG TRÙNG (đào sâu video mới) + sắp xếp Xem trước (mới↑/đã-thấy↓) — đụng douyin/core.py, web_app.py, tim_anh.py, web/index.html
- **YC1 — "Cào không trùng" (đào sâu):** trước cào CỐ ĐỊNH N/10 trang rồi skip-tải video đã có → "tải hết rồi ra 0 video". Giờ bật `MC_DEEP_NEW=1` → cào tới khi đủ **N video CHƯA tải** (bỏ qua đã tải, đào trang SÂU hơn), trần `MC_DEEP_PAGE_CAP=40` chống đào vô hạn. **CHỈ Douyin** (có ledger `_da_tai_ids.txt`), **search + creator**.
  - `douyin/core.py`: `search()` đổi điều kiện while (DEEP: `new_count<N && page<cap`) + đếm `new_count` (was_new=aid chưa-trong-ledger TRƯỚC, sau get_aweme_media tự mark_seen → `aid in seen_ref` = tải-mới-thành-công, KHÔNG đếm fail); `_collect_user_posts()` (creator): gom tới đủ N mới + lọc bỏ đã-tải. GIỮ nguyên hành vi non-DEEP.
  - `web_app.py chay_crawl`: `nt==dy && che_do in (search,creator) && cfg.khong_trung` → set `MC_DEEP_NEW=1` + log.
  - `web/index.html`: checkbox `#caoKhongTrung` (Douyin-only, hiện qua `_khongTrungVis()` gọi ở updateMode+platform-onclick) + truyền `khong_trung` vào `_opts`.
- **YC2 — sắp xếp Xem trước:** video MỚI tinh lên TRÊN, đã-thấy-lần-trước-chưa-tải ở giữa, ĐÃ TẢI xuống DƯỚI (KHÔNG ẩn — sort ổn định giữ thứ tự cào trong nhóm).
  - `tim_anh.py`: `_seen_ids_before(folder,snap)` (ID trong jsonl TRƯỚC snapshot = "đã thấy") + gắn cờ `da_thay` mỗi item.
  - `web_app.py _gan_badge`: sau gắn da_tai → `items.sort(key=(2 đã-tải/1 đã-thấy/0 mới))`. (UI `_xemFiltered` chỉ lọc Ảnh/Video, KHÔNG re-sort → giữ thứ tự server.)
- **Verify:** py_compile 3 file + core.py OK; node --check JS OK; sim deep-crawl PASS (N=15 đã-tải-0..49 → đào 7 trang tải đúng 50..64; non-deep 1 trang 0 mới = đúng bug cũ; toàn-bộ-đã-tải dừng cap 40); sort PASS (B,D,A,E,C); `_seen_ids_before` thật xhs 471 ID + snapshot-rỗng=0. **CHƯA test crawl LIVE** (cần login+mạng, rủi ro anti-bot) — logic đã chứng minh. **CHƯA restart web_app** (route/env mới cần restart); index.html F5.
- **⚠️ Đụng web_app.py + index.html (file chung) → COMMIT SỚM kẻo IDE đè buffer.** Đào sâu = nhiều request hơn = rủi ro anti-bot cao hơn (đã có trần + log cảnh báo).
- **File đụng:** `MediaCrawler/media_platform/douyin/core.py`, `web_app.py`, `tim_anh.py`, `web/index.html`.
- **Trạng thái:** ✅ code + verify (unit/logic) xong — chờ user restart + duyệt + commit + (deploy laptop nếu muốn).

### ✅ [2026-06-27] Bỏ "cào video TỪ TỪ" của Bilibili — đụng MediaCrawler/media_platform/bilibili/core.py
- **Gốc:** `_ngu_sec()` (fork tự thêm) ngủ giữa MỌI request bili (7 chỗ, gồm `get_bilibili_video` line 781 = ngủ sau mỗi video tải): no-browser 0.5-1.5s, browser mode (BILI_NO_BROWSER=0) base2×1.5-4 = 3-8s/video → "cào từ từ".
- **Sửa:** rút gọn `_ngu_sec()` còn `random.uniform(0.1, 0.4)` (giữ jitter NHẸ tránh nhịp ĐỀU = dấu hiệu bot; an toàn hơn ngủ 0). Bỏ nhánh browser-slow + kill-switch. **GIỮ chữ ký hàm** (7 caller không đổi) + **GIỮ `BILI_NO_BROWSER` mode-switch line 146** (chỉ gỡ timing, no-browser feature nguyên). py_compile OK.
- **⚠️ Trade-off:** nhanh hơn nhiều nhưng giảm "đệm" anti-bot. Repo gốc NanmiCoder cũng không có slow này (user xác nhận). Nếu bị kill phiên/anti-bot → nâng lại khoảng trong `_ngu_sec()`.
- **File đụng:** `MediaCrawler/media_platform/bilibili/core.py`. Commit + scp app-src laptop.

### ✅ [2026-06-27] Xoá tab Lồng tiếng + xoá logic giảm-tốc-trước-render + ETA cào theo MB/s×size — đụng xu_ly_chon.py/web/index.html
- **(a) xu_ly_chon.py:** GỠ khối auto-chậm-0.8 khi lồng tiếng (cũ ~139-144) + khối "CHẬM-TRƯỚC" tạo `_slow_tmp.mp4` (cũ ~232-258). Giờ tốc độ giữ ĐÚNG user chọn, áp ở CUỐI (burn) như bình thường — không tự giảm, không tạo bản tạm. `shutil` vẫn dùng chỗ khác (không dead import). py_compile OK.
- **(b) web/index.html:** ETA cào đổi từ "video/phút" → **MB/s + size**: FE parse dòng log backend `📊 … X MB · Y MB/s` (web_app `_eta_log` đã có sẵn) → "Tốc độ: Y MB/s", "Ước tính còn" = size-TB-mỗi-video × số-còn-lại ÷ MB/s. Fallback video/phút khi chưa có MB/s. (Toán: avg-size÷speed = el÷done nên ETA value ~tương đương, khác chính là ĐƠN VỊ tốc độ.)
- **(c) web/index.html:** hợp nhất removal tab Lồng tiếng standalone (WIP session khác: gỡ nav `data-v="dub"` + `<section id="v-dub">`, −348 dòng, dub dời vào Render). Verify: 2 inline script 0 lỗi cú pháp (node `new Function`), KHÔNG còn ref chết tới dub.
- **File đụng:** `xu_ly_chon.py`, `web/index.html`. Đã commit + scp app-src laptop (subprocess/đọc-fresh → live lần render/load kế).

### ✅ [2026-06-26] FIX che TRƯỢT sub clip dọc + render "mất tích" (auto-refresh) + cache-buster thumbnail — đụng localize.py/web/index.html
- **Bug che (clip 9:16):** `localize.burn_phude` khi nới dải blur, nhánh nửa-dưới ÉP `by1=max(by1,0.995)` (kéo SÁT đáy) + cap 25% → dải blur về **74-99% đáy**, TRƯỢT khỏi chữ Trung ở GIỮA (clip dọc nội dung+sub ~55-64%, KHÔNG ở đáy). Đo Laplacian var: band=2042 NÉT (che không áp). **Fix:** chỉ kéo-sát-đáy khi `by1>=0.85`; sub giữa chỉ `by1+noi`. Sau fix band=3 (MỜ đúng chữ). OCR đọc-text dùng band thô nên KHÔNG dính.
- **Bug "render mất tích":** render KHÔNG mất — vào `processed_videos/<nt>` (mặc định) hoặc out_dir (C:\audio); /api/files trả đủ (9+14, không de-dup). NHƯNG `qLoad` poll queue KHÔNG gọi `loadFiles()` khi xong → đang ở tab File đã tải thì view đứng = tưởng mất. **Fix:** qLoad đếm "xong" tăng → `loadFiles()` nếu tab files mở. +cache-buster `&v=mtime` card (render đè cùng tên → cache thumbnail cũ).
- **Verify:** Laplacian var band/face (che áp chưa), Chromium 74 card 0 lỗi JS. localize.py áp cho render KẾ (subprocess load fresh, KHÔNG cần restart); index.html cần F5.
- **Che HỘP đúng text (không full-width/không "to quá"):** dai_sub_rapid +trả x0,x1 (union box trong dải y) → blur HỘP (crop x*w) thay full-width; tighten margin (y±0.006/0.008, x±0.01, bỏ nới burn). Mặt người NÉT (sharp 97), chữ Trung MỜ.
- **Phụ đề Việt ĐÈ lên blur + KHÔNG hard-code:** burn_phude sinh ASS `\pos`(tâm dải)+`\an5`, PlayRes=video thật, font×H/288 → phụ đề đúng tâm dải chữ Trung, 2 dòng tự căn, clip sub giữa/đáy/trên đều đúng. Bỏ MarginV cứng (đo thật libass áp MarginV KHÔNG đáng tin: 72≡144). Verify: 29002px chữ Việt ở dải, 0px ở đáy.
- **BLUR ĐỘNG + HỢP NHẤT OCR (user chỉ đạo):** sub DI CHUYỂN trong clip (cảnh02 spread 23%) → blur tĩnh trượt. `dai_sub_rapid.phat_hien_hop_dong` dò ĐOẠN vị-trí RẺ (ảnh trắng+gradient, KHÔNG OCR). HỢP NHẤT: `ocr_text.ocr_dong` = dò-rẻ ra đoạn → RapidOCR det+rec mỗi đoạn cho TEXT+box CHÍNH XÁC (đoạn không chữ→loại); 1 lần quét ra cả text (dịch) + box (blur động + phụ đề bám, qua box_sink→chay→burn_phude blur động split N+1 + overlay enable=between(t)). Verify: câu chính text đúng+vị-trí chuẩn (hết "1++"), còn sót câu NGẮN/nhanh (user OK "đủ tốt"). +env `CHE_DONG_FPS` giãn mẫu (+24% tốc), stride thích ứng video dài (≤600 mẫu phủ hết).
- **File đụng:** `localize.py` (burn_phude blur hộp/động + _srt_to_ass_pos + asr_segments box_sink + chay blur_segs), `ocr_text.py` (ocr_dong+_doc_box), `dai_sub_rapid.py` (phat_hien_hop_dong+_box_1frame_cheap+x0,x1), `dai_sub.py` (x0,x1), `xu_ly_chon.py` (band 5-tuple), `web/index.html` (qLoad auto-refresh + cache-buster).
- **LƯU Ý dead code:** `ocr_timing.py` + `ocr_text.ocr_theo_khoang` giờ KHÔNG còn được asr_segments gọi (OCR đã chuyển ocr_dong) — có thể dead, CHƯA gỡ (cần rà nhánh funasr_ocr).
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-06-26] File đã tải render DÙNG CHUNG panel tab Render + rule component hoá + lịch sử card to/5 nền — đụng web/index.html/web_app.py/rule.md
- **Vấn đề (đúng rule):** File đã tải bấm Render (📋 Render đã chọn → `rdOpenMulti`; render từng card → `rdOpen`) mở **modal `rd*` RIÊNG** (HTML ~1755-1823) nhân bản panel Render (xl) rồi LỆCH — vd `rdSpeed` mặc định 1.1 trong khi `xlSpeed` đã đổi 0.8 + thiếu chậm-trước/giọng Piper mới.
- **Fix:** `rdOpen`/`rdOpenMulti` KHÔNG mở modal nữa → `_renderTabRender(vids)`: nạp video (ngoài data→xlVideoThem) + `xlSel` rồi click nav `data-v=xuly` (xlLoadVideos giữ xlVideoThem+xlSel) → dùng PANEL xl thật (luôn đầy đủ/đồng bộ). Verify Chromium: bấm→renderTabShown, modalHidden, xlSel=1, 0 error. ⚠️ **Modal rd* + rdRun/rdUpd/... giờ là CODE CHẾT (unreachable)** — chưa gỡ (báo user, gỡ sau).
- **rule.md:** +rule "COMPONENT HOÁ + ĐỒNG BỘ (BẮT BUỘC)" — UI/chức năng dùng nhiều nơi phải là component dùng chung; sửa 1 bản phải sửa hết bản trùng / rà mọi nơi dùng.
- **Lịch sử cào (tiếp):** card to (ảnh 88→160px, grid 150→210px, chữ 13px); dropdown nền tảng chỉ 5 (`NEN_TANG_HO_TRO`=dy/bili/xhs/yt/tt, bỏ wb/tw/ig) — UI+lich_su_cao iter + endpoint nen_tang. Verify Chromium: card 213px, dropdown 6 opt đúng.
- **File đụng:** `web/index.html` (rdOpen/rdOpenMulti/lsGrid/render card), `web_app.py` (NEN_TANG_HO_TRO/lich_su_cao/endpoint), `rule.md`.
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-06-26] Cào ngay phản hồi tức thì + badge "đã tải" đúng + Lịch sử cào (gỡ log, +lọc thời gian) — đụng web_app.py/index.html
- **A (trễ 3s):** `startCrawl` bấm 3s sau mới chạy (chờ `guardLogin`→subprocess kiểm login Chromium) mà KHÔNG phản hồi → user tưởng hỏng bấm lại. Fix: vô hiệu hoá `crawlBtn` + đổi text "⏳ Đang kiểm tra đăng nhập..." NGAY khi bấm, restore ở `finally` (chống double-click; độ trễ login vẫn ~3s nhưng THẤY rõ).
- **B (badge đã tải):** `it.da_tai` chỉ từ SQLite (luồng Xem-trước) → cào CHÍNH (/api/crawl) không ghi → thiếu. Fix: `_da_tai_keys(nt)` HỢP SQLite + ledger `DATA_DIR/<leaf>/_da_tai_ids.txt`; `_gan_badge`+`lich_su_cao` dùng; match `(url in mk) or (id in mk)`. Test: douyin 800 bài → 36 đã tải (trước ~0).
- **C (lịch sử cào):** 2 section TRÙNG `id="v-lichsu"` → bản LOG (#logFull ~1554) che bản METADATA (~1588). Fix: GỠ section log (poll #logFull có guard). +Lọc THỜI GIAN: `lich_su_cao(so_ngay)`+endpoint `ngay`+dropdown `lsThoiGian` (Mọi lúc/Hôm nay/7/30/90)+lsLoad `&ngay=`. Test: hôm nay 329 < 7 ngày 800.
- **Verify:** API (time filter đơn điệu + 36 badge) + Chromium (1 section v-lichsu, h1 "🕘 Lịch sử cào", coLogFull=false, dropdown 5 opt, startCrawl async, 0 error). **ĐÃ RESTART web_app.**
- **File đụng:** `web_app.py` (_da_tai_keys/_gan_badge/lich_su_cao/endpoint), `web/index.html` (startCrawl/gỡ log/lsLoad/dropdown).
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-06-25] Picker chọn giọng Piper (Banmai + NghiTTS) cho Render + Lồng tiếng — đụng localize.py/web_app.py/index.html/giong_piper.py — ĐÃ COMMIT 6d37796
- **User muốn:** thêm nhiều giọng Piper (tải từ Drive NghiTTS) cho khách chọn ở cả Render lẫn Lồng tiếng. Đã nghe thử 23 giọng (Desktop\giong_thu), loại bớt → GIỮ: Banmai, Ngọc Huyền, Ngọc Huyền mới, Mai Phương, Phương Trang, Thanh Phương (nữ) + Mạnh Dũng, Minh Khang (nam).
- **giong_piper.py (MỚI):** map name→(gdrive_onnx_id, json_id, tên) 7 giọng NghiTTS + `tai(name)` tải on-demand qua gdown (Banmai vẫn qua tai_banmai). onnx GITIGNORED (piper_vn/) — tải lần đầu chọn.
- **localize.py:** dispatch `tts_engine=="piper"` đọc `voice` (tên giọng) → giong_piper.tai → `long_tieng_piper(model_path=)`; trống/banmai/vi-/en-/path = mặc định, try/except an toàn.
- **web_app.py:** chay_long_tieng truyền `--voice` cho tts in (edge,piper). Render đã truyền voice sẵn (_lenh_xu_ly).
- **index.html:** `PIPER_VOICES`+`piperOpts()`; picker ở Lồng tiếng (dubTtsChange nhánh piper→dubVoice), Render chính (xlPiperWrap+xlUpd+xlRun voice), modal render-lại (rdPiperWrap+rdUpd+voice).
- **pyproject:** +gdown>=5.0.0.
- **Test:** dispatch voice→đúng model+synth OK (banmai/ngochuyen/manhdung/phuongtrang); JS không lỗi cú pháp; web_app restart UP 200. (Không có MCP browser nên chưa click-test UI — user tự click-test.)
- **Lưu ý:** 6 giọng (Chiều Thanh/Duy Oryx/Tài An×2/Lạc Phi/Yan) tải LỖI do quota Drive gdown — chưa thêm; retry sau được (ID trong memory).
- **Follow-up COMMIT 6e2d5fc:** GỘP giọng Piper THẲNG vào dropdown engine (1 dropdown chọn cả engine+giọng, value `piper:<giọng>`, optgroup), BỎ ô giọng phụ (dubVoice-piper/xlPiperWrap/rdPiperWrap); `splitTts()`/`integratePiperVoices()` ở index.html + net tách `piper:x`→tts+voice ở chay_long_tieng/_lenh_xu_ly/endpoint Việt hóa (web_app.py). Test THẬT: render 测试张雪_30s với maiphuong (fresh 152s, tải on-demand cache-MISS) + render 2 cache HIT srt/band/dub 13s; render manhdung qua flow tích hợp 97s OK; net `piper:manhdung`→`--tts piper --voice manhdung` PASS. Artifact gửi Desktop laptop (test_giong_maiphuong.mp4 + test_giong_manhdung.mp4).

### ✅ [2026-06-25] Editor Băm: tua/cắt mượt như YouTube (faststart on-open) + fix seek crash — đụng web_app.py/index.html
- **User muốn:** mở editor cắt là tua/nhảy tới đâu tải đúng chỗ đó (Range), KHỎI đợi load cả bài. Chốt: cả 2 loại video, tối ưu LÚC MỞ EDITOR.
- **Bug thật đã fix:** `_serve_video` chỉ bắt BrokenPipe/ConnectionReset, THIẾU `ConnectionAbortedError` (WinError 10053 — Windows ném khi trình duyệt hủy stream lúc seek) → văng lỗi, server fail (exit 127). +`ConnectionAbortedError`.
- **Faststart on-open (web_app):** `_la_faststart()` (đọc atom — moov trước mdat?), `_bam_seekable()` (chưa faststart → remux `+faststart -c copy` KHÔNG re-encode/giảm chất → cache `tempfile/vc_seekable`, register `_bam_serve_files`), `_ffmpeg_bin()`; GET `/api/bam/seekable?p=`.
- **UI (index.html):** `bamOpenEditor` async → gọi /api/bam/seekable (hiện "⏳ đang tối ưu" lần đầu) → dùng path faststart; `bamPhanTich` async await; `bamDragMk` throttle seek bằng rAF (kéo vạch mượt, hết "bão seek").
- **Verify THẬT:** (1) API — _nonfast (moov cuối) → optimized:True + file trả về faststart✓; faststart sẵn → optimized:False (không remux phí). (2) Chromium — editor dùng `vc_seekable/*.mp4`, bamPhanTich async, 0 console error. **ĐÃ RESTART web_app.**
- **File đụng:** `web_app.py` (_serve_video/_la_faststart/_bam_seekable/_ffmpeg_bin/endpoint), `web/index.html` (bamOpenEditor/bamPhanTich/bamDragMk).
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-06-25] Băm nhỏ: thêm video NGOÀI (picker) + clip về "File đã tải" — đụng web_app.py/index.html
- **User muốn:** tab Băm cho upload/chọn video của mình (trước chỉ có video đã tải / dán THƯ MỤC) + băm xong clip về "File đã tải".
- **Backend (web_app.py):** `_bam_out_dir(video)` — video NGOÀI (không thuộc DATA_DIR/PROCESSED_DIR/_OUT_DIRS) → clip ra `DATA_DIR/bam_nho/<tên>/` (video đã tải vẫn clip_nho/ cạnh nguồn, đã được quét sẵn); `chay_bam_nho` dùng helper; `liet_ke_file` +base `(DATA_DIR/bam_nho, "Băm nhỏ")`; `_resolve_video` +root bam_nho (phục vụ /video bền sau restart). Picker tái dùng `/api/them_video` (native, đã có của Render).
- **UI (index.html):** nút "➕ Thêm video của tôi" cạnh "📂 Liệt kê" (tab Băm) + `bamThemVideo()` (mirror xlThemVideo+bamFolderLoad: post /api/them_video → bamItems src="của tôi" → tự tick); tooltip cập nhật.
- **Verify THẬT:** (1) API test — POST /api/bam/start video ngoài (CSRF nonce) → 2 clip ra `MediaCrawler/data/bam_nho/_omni_src/`, /api/files nhãn "Băm nhỏ" (0→2). (2) Chromium — nút present + bamThemVideo defined + tooltip + 0 console error. (3) /api/files HTTP 200 (không lặp regression 500). **ĐÃ RESTART web_app** (backend đổi).
- **File đụng:** `web_app.py` (_bam_out_dir/liet_ke_file/_resolve_video/chay_bam_nho), `web/index.html` (nút+JS+tooltip).
- **Trạng thái:** ✅ XONG (chờ user commit).

### ✅ [2026-06-25] Cân bằng audio lồng tiếng + tốc độ "chậm-TRƯỚC" + UI mặc định 0.8 — đụng localize.py/xu_ly_chon.py/index.html
- **Vấn đề user (test laptop RTX 3050):** (1) tiếng gốc 20% vẫn hơi to; (2) giọng OmniVoice hơi nhỏ; (3) video Trung nhanh → giọng Việt không bắt kịp dù làm chậm 0.8x (vì chậm áp Ở CUỐI = nén-rồi-chậm).
- **localize.py — audio:** `tron_audio` CHUẨN HOÁ giọng dub về peak −1dBFS (`AudioSegment.normalize(headroom=1.0)`) → giọng to nhất quán, áp đảo nền. goc_vol đường cong DỐC `30·log10` (20%→−21dB thay −14dB); nhánh mặc định (goc_vol=None) `−14→−20dB`. Giữ nghĩa goc_vol = GIỮ tiếng gốc ở mức %.
- **xu_ly_chon.py — slow-TRƯỚC:** giảm tốc (`speed<1.0`) + lồng tiếng → làm chậm video bằng `xlv.dung_lenh_ffmpeg` (cfg chỉ-speed) NGAY trước localize → ASR/dub khớp khe đã giãn (đỡ nén, bắt kịp). Đặt `a.speed=1.0;co_speed=False` để KHÔNG áp lại ở burn. **Tăng tốc (>1.0) GIỮ NGUYÊN cách cũ** (áp ở CUỐI sau dub — đã verify localize.py:608-617 đúng thứ tự: lồng tiếng trước → setpts/atempo sau).
- **web/index.html:** `xlSpeed` mặc định **0.8x (chậm)** + thêm option 0.85/0.9/1.0, giữ 1.05–1.5 (ai muốn nhanh tự chọn); JS fallback `||0.8`. (index.html session khác đã commit kèm.)
- **Verify laptop:** v3 (gốc−20dB+giọng chuẩn hoá) + v5 (chậm-TRƯỚC, log "🐢 Làm chậm video 0.80x TRƯỚC", ASR ra 16 câu khớp khe rộng) → **user duyệt "giọng ổn rồi"**. Output Desktop OneDrive.
- **File đụng:** `localize.py`, `xu_ly_chon.py`, `web/index.html`. localize subprocess (không restart); index.html F5.
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] Tích hợp vietnormalizer (THAY tts_chuan_hoa, fallback port cũ) — GIẢI phần "transliteration CHƯA làm"
- User gửi github nghimestudio/vietnormalizer → kiểm: **MIT, stdlib-only (no deps), Py3.8+, pip install vietnormalizer**. Test thật: số/ngày/giờ/tiền/%/đơn-vị/SĐT đúng + **PHIÊN ÂM Anh→Việt** (iPhone→ai-phôn, coding→cô-đinh, freeship→phờ-ri-xíp) — câu Việt thường KHÔNG mangle. → hơn hẳn tts_chuan_hoa.py (port NghiTTS không transliterate). ĐÂY LÀ phần "Transliteration CHƯA làm" của entry NghiTTS bên dưới — GIỜ CÓ.
- **Tích hợp (user chọn "thay, fallback port cũ"):** `localize.py` `_chuan_hoa_tts` ƯU TIÊN `vietnormalizer.VietnameseNormalizer().normalize()`; lỗi per-call → lùi `tts_chuan_hoa.chuan_hoa`; không import được → port cũ; cả 2 lỗi → no-op. Verify: localize dùng vietnormalizer (iPhone→ai-phôn). `MediaCrawler/pyproject.toml` +`"vietnormalizer"` (deploy uv sync cài; TOML valid 47 deps). dev venv đã `uv pip install` sẵn.
- **⚠️ ĐỤNG `localize.py` + tts_chuan_hoa-area của session khác** (user OK). Hạn chế nhỏ vietnormalizer: "199k"(k tắt) không đổi, "GS.TS" dấu chấm chưa gọn — fallback port cũ lo phần số nếu cần. KHÔNG cần restart (localize subprocess).
- **File đụng:** `localize.py`, `MediaCrawler/pyproject.toml`. 
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] OmniVoice cho FREE (giọng mẫu sẵn) + batch theo VRAM — đụng gating/OmniVoice session khác (user OK)
- **Gating FREE (web_app dub/start ~3945):** trước `tts in (f5,omnivoice)`→PRO. Sửa: PRO chỉ khi **tts==f5 HOẶC giọng TỰ TẢI (path trong CLONE_DIR)**. → OmniVoice + giọng-mẫu-sẵn (giong_mau/default) = FREE; clone-giọng-của-bạn (upload) vẫn PRO; F5 vẫn PRO. Verify logic: omni default/giong_mau→FREE, omni+CLONE_DIR→PRO, f5→PRO, edge/piper→FREE. Render (/api/xu_ly) KHÔNG gate giọng nên đã free sẵn.
- **OmniVoice trong 4 dropdown ✅ visible** (xlTts/dubTts/arTts/rdTts) — "vừa vào là có".
- ⚠️ **CHƯA auto-install OmniVoice** (user muốn "tải lúc tải app"): KHÔNG có trong setup.js/installer, KHÔNG có nút cài (như cai_f5/cai_gpu), KHÔNG có tải-nền (như FunASR). Hiện chỉ cài TAY trên laptop RTX. OmniVoice GPU-only + .venv_omnivoice + model vài GB → bundle-cho-mọi-máy = phí (non-GPU không dùng). → ĐỀ XUẤT GPU-conditional auto-install, CHỜ user chốt cách.
- **File đụng:** `web_app.py` (gating). RESTART để áp gating (web_app).
- **AUTO-INSTALL NỀN GPU-conditional (user chọn "tự tải nền chỉ máy GPU"):** `cai_omnivoice.py` (MỚI, mẫu cai_f5: co_nvidia gate → uv venv .venv_omnivoice + uv pip install omnivoice + torch cu121 + snapshot_download k2-fsa/OmniVoice; idempotent, da_cai() check) + `web_app.chay_tai_omnivoice()` (nền, CHỈ khi `_co_nvidia()` + chưa có .venv_omnivoice) + auto-trigger trong main() cạnh chay_tai_funasr. Verify dev (no GPU): SKIP sạch (OMNIVOICE_SKIP_NOGPU, _omni_dl done+not-started, KHÔNG spawn install), startup OK.
- **⚠️⚠️ CHƯA TEST THẬT phần CÀI** (dev không GPU + vài GB CUDA): recipe theo cai_f5 + note laptop "uv pip install omnivoice" nhưng **CHƯA verify trên GPU** → omnivoice package/torch CUDA có thể thiếu dep (flash-attn?). **BẮT BUỘC test `cai_omnivoice.py` trên laptop RTX 3050 trước khi ship.** Chưa có banner tiến trình (chỉ them_log; có thể thêm #omniBar như #funasrBar nếu cần).
- **File đụng:** `web_app.py` (gating + chay_tai_omnivoice + main trigger), `cai_omnivoice.py` (MỚI untracked). RESTART rồi.
- **Trạng thái:** gating ✅ + wiring auto-install ✅ (dev-skip verify) — **CÀI chờ verify laptop GPU**.

### ✅ [2026-06-25] OmniVoice: batch TỰ SCALE theo VRAM (user OK đụng file OmniVoice session khác)
- Trước: batch CỐ ĐỊNH 8 (OMNI_BATCH default), `_vram_gb()` chỉ quyết định daemon/one-shot. → thêm `localize._omni_batch()`: ≤4.5GB→8 (giữ tune RTX3050), 4.5-8→16, 8-12→24, ≥12→32; ENV OMNI_BATCH ép cứng. Truyền `--batch` vào CẢ daemon (`_omni_daemon_synth` Popen) LẪN one-shot cmd + log "batch N theo VRAM".
- **⚠️ ĐỤNG `localize.py` (vùng OmniVoice của session khác)** — user đồng ý. Giữ batch 8 cho ≤4.5GB nên KHÔNG đổi hành vi máy deploy RTX 3050 4GB. KHÔNG đụng omnivoice_synth.py (nó nhận --batch sẵn).
- **Verify:** thang batch đúng (0/4→8, 6→16, 8→24, 12→32, ENV ép OK); syntax OK. KHÔNG cần restart (localize subprocess).
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] Xoá tab "Tiến trình" (tiendo) — user không cần xem tiến trình CHI TIẾT (thanh % đủ)
- Gỡ: nav link `data-v="tiendo"` + section `#v-tiendo` (bước/phụ-đề-điền-dần/preview) + toàn bộ JS tiendo (tdoOpen/tdoStop/tdoTick/tdoPick/_tdoRenderQueue/tdoDoneRender/_tdoQItem/_tdoShow/_fmtT/_dubStepIdx/STEPS_*) + 3 ref nav-handler (tdoStop on switch, tiendo→tdoOpen, tiendo:5 trong pwiz map). GIỮ thẻ tiến-trình ở tab Cào (#caoProgWrap) + thanh % dub/render.
- KHÔNG đụng backend: /api/queue_get,render_progress,queue_detail,dub/log DÙNG CHUNG render/dub → giữ.
- **File đụng:** CHỈ `web/index.html`. Verify Chromium: nav hết tiendo, 0 console error.
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] XHS "cào 0 video" = bài ẢNH (KHÔNG phải anti-bot) + thêm filter Ảnh/Video ở Xem trước
- **Điều tra:** XHS crawl "mèo cute" rednote → tải **3 video OK** → XHS login/detail/download CHẠY. "0 video" là do bài là ẢNH (XHS chủ yếu ảnh) — user/khách chọn nhầm bài ảnh. `get_video_url_arr` trả [] khi type≠'video'. MediaCrawler có sẵn SearchNoteType.VIDEO=1 (chưa dùng).
- **Fix (user yêu cầu) — filter Ảnh/Video ở Xem trước:** `web/index.html` xemRender trước HARDCODE `it.video` (chỉ video). Thêm segmented `#xemLoaiSeg` (🎬 Video mặc định / 🖼 Ảnh / Tất cả) + state `_xemLoai` + `_xemFiltered()` + `xemSetLoai()`; badge thẻ động (▶video / 🖼ảnh N); count+msg theo `_lbl`. tim_anh.py vốn đã trả `loai`+`video`+`so_anh` nên KHÔNG đụng backend.
- **File đụng:** CHỈ `web/index.html`. KHÔNG cần restart.
- **Trạng thái:** ✅ XONG (verify Chromium filter).

### ✅ [2026-06-25] Kiểm preview TẤT CẢ nền tảng + FIX TikTok kênh (yt-dlp bỏ format secUid)
- **Test preview live (count 5):** Douyin 13(12vid+1ảnh) · Bilibili 59(toàn video) · **XHS 20(5vid+15ảnh — xác nhận 75% ảnh)** · YouTube 5 · đều ✅.
- **Weibo = 0:** KHÔNG có `browser_data/weibo_user_data_dir` → chưa login → cần đăng nhập Weibo (không phải bug; wb là nền tảng phụ ẩn "(ảnh)").
- **TikTok = 0 → FIX:** `tai_ytdlp.chuan_hoa_kenh_tiktok` resolve @user → `tiktokuser:<secUid>` nhưng **yt-dlp 2026.06 TỪ CHỐI** secUid cũ ("Unable to extract secondary user ID") → 0 entries. yt-dlp giờ xử lý THẲNG URL `https://www.tiktok.com/@user` (test: 3 entries OK). FIX = trả URL @username thẳng, bỏ `_tiktok_secuid` (Playwright, đã hỏng). Áp CẢ preview (l.434) LẪN tải (l.666). Verify API: tt creator → **5 bài (5 video)** + thumbnail.
- **File đụng:** `tai_ytdlp.py` (chuan_hoa_kenh_tiktok). KHÔNG cần restart (subprocess).
- **+ FIX TikTok KÊNH tải thật (0 video):** preview/liệt kê đã OK nhưng TẢI kênh `ydl.download([url_kênh])` (extract playlist NON-flat) vẫn 0 (TikTok anti-bot khâu này). Fix: tt creator giờ **liệt kê flat → URL từng video → tải từng URL** (như link, đã chứng minh chạy). Verify: kênh @tiktok count=2 → "Tải được 1 video" (cái còn lại trùng archive từ test link). LINK tải thật cũng verify OK ("✔ Đã tải 1"). → **CẢ LINK + KÊNH TikTok đều tải được.**
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] TTS: OmniVoice = CLONE giống F5 + ns 8/16/32 (CHỐT cuối — đã bỏ Voice Design/forever/VieNeu)
- **OmniVoice CLONE-only (giống F5):** UI "Loại giọng"→OmniVoice = clone engine (DUB_CLONE_ENGINES, dùng panel Giọng clone: mặc định `giong_mau/nu.wav` hoặc upload) + dropdown chất lượng **ns 8/16/32** (`dubOmniRow`, env OMNI_NS). Tối ưu: `create_voice_clone_prompt` encode speaker embedding 1 lần + cache; cắt ref >12s→~10s (cat_giong_clone).
- **VRAM-gate (QUAN TRỌNG cho 4GB):** `localize._vram_gb()` (nvidia-smi) <6GB ⇒ **bỏ qua daemon, one-shot thẳng** (daemon warm OOM trên 4GB vì ASR+OmniVoice >VRAM, lại phá ASR render kế). Verify laptop RTX 3050 4GB: render đi thẳng one-shot, ra `_xuly.mp4` OK, 0 orphan. Daemon safeguard: lỗi synth→thoát nhả VRAM (orphan chỉ PowerShell Stop-Process giết được, taskkill /F vô dụng). Memory: [[omnivoice-clone-4gb-daemon-vram]].
- **ĐÃ BỎ:** Voice Design/instruct (drift timbre "nhiều giọng") · "giữ model mãi" OMNI_IDLE=0 (OOM 4GB) · **VieNeu** xoá sạch (engine/UI/dispatch/`long_tieng_vieneu`/`vieneu_synth.py`; `.venv_vieneu` còn trên đĩa).
- **File đụng:** `localize.py` (OMNI clone dispatch + _vram_gb + long_tieng_omnivoice), `omnivoice_synth.py` (voice_clone_prompt + cache + safeguard), `web_app.py` (OMNI_NS env, validation, gating), `web/index.html` (clone engine + dubOmniRow ns, bỏ vieneu/VIENEU_VOICES), `xu_ly_chon.py` + `dub_preview.py` (choices). Mới untracked: omnivoice_synth.py, dub_preview.py, cat_giong_clone.py, tts_chuan_hoa.py, tai_banmai.py.
- **Trạng thái:** ✅ XONG (verify render laptop) — push main.

### ✅ [2026-06-25] Port chuẩn-hoá text TTS từ NghiTTS (số/ngày/tiền/% → chữ)
- **Việc:** TTS (Piper/Edge/F5 trong localize) đọc SAI số ("1.000.000đ" đọc từng chữ). Port logic chuẩn-hoá của NghiTTS (github nghimestudio/nghitts, vietnamese-processor.js) sang Python.
- **File ĐÃ đụng:** `tts_chuan_hoa.py` (MỚI — doc_so + chuan_hoa: số/ngày/giờ/tiền/%/SĐT/La Mã/âm/thập phân), `localize.py` (import guarded `_chuan_hoa_tts` + áp vào 3 hàm TTS: long_tieng/long_tieng_piper texts + _tts_edge_wav). CHỈ áp cho text TTS, KHÔNG đụng phụ đề hiển thị.
- **Verify:** standalone (1.000.000đ→một triệu đồng; 25/12/2024→ngày...; 50%→năm mươi phần trăm; 0987...→từng số; XXI→hai mươi mốt) + in-pipeline + synth edge câu có số ra wav. KHÔNG cần restart (localize subprocess).
- ⚠️ **CLOBBER:** `dub_preview.py` (tạo sáng nay cho Nghe-thử-giọng) bị MẤT (IDE ghi đè/clobber file untracked) → đã TẠO LẠI. **Khuyến nghị COMMIT** để bảo vệ file mới (tts_chuan_hoa.py, dub_preview.py untracked dễ mất).
- **Giọng Banmai .onnx — ĐÃ TẢI (user chỉ đúng, model KHÔNG khóa):** lần theo VNTTS (PyPI `vntts`, repo Devhub-Solutions/VNTTS). model_downloader.py tải `models.zip` từ GitHub Release. URL: `github.com/Devhub-Solutions/VNTTS/releases/download/models/models.zip` (293MB, gộp TTS+ASR). Tải PHẦN bằng HTTP Range (đọc zip central dir) chỉ kéo `banmai/model.onnx` (60.6MB) + json → `piper_vn/banmai.onnx(.json)`. Là Piper chuẩn (espeak 'vi', 22050Hz) → tương thích trực tiếp long_tieng_piper(model_path=).
- **A/B:** user nghe → **CHỌN BANMAI** (hay hơn vais1000). → ĐÃ WIRE: `tai_banmai.py` (MỚI, tải partial-Range banmai từ VNTTS release) + localize.py đặt `PIPER_VOICE_MAC_DINH=banmai.onnx` + long_tieng_piper tự tải Banmai khi thiếu (fail → lùi vais1000). Verify: piper mặc định = banmai, synth OK. KHÔNG cần restart (subprocess). ⚠️ kiểm license Banmai trước khi ship thương mại.
- **Giọng khác:** VNTTS release chỉ có Banmai (1 giọng TTS). App NghiTTS có thêm giọng celebrity trên R2 nhưng không tìm ra URL app + license không rõ. Piper VN chuẩn khác (25hours/vivos) chất lượng THẤP. Tool đã sẵn đa giọng: Edge (nhiều giọng MS) + Piper(Banmai) + F5 clone. Có thể thêm picker giọng Piper nếu user muốn.
- **TÍCH HỢP Banmai vào chọn giọng (user: "thay luôn piper"):** đổi nhãn 4 dropdown (xlTts/arTts/dubTts/rdTts) `piper` → "Banmai — giọng Việt tự nhiên (offline, nhanh)" + đặt **mặc định** (selected). value giữ "piper" (backend piper=Banmai). **MỞ KHOÁ piper khỏi PRO** (user chọn): web_app.py:3845 `adv` đổi `(tts!="edge")` → `(tts=="f5")` → Banmai+Edge FREE, F5 clone vẫn PRO. Verify free-tier: piper QUA ✓, f5 CHẶN ✓. RESTART rồi (gating ở web_app).
- **Transliteration (Anh→phiên âm):** CHƯA làm (user redirect sang tìm model). Chờ user.
- **Trạng thái:** normalizer ✅; giọng Banmai tải xong + A/B chờ user nghe; transliteration tạm hoãn.

Khóa mục = `[ngày] mô tả ngắn` (không có session-id ổn định nên dùng ngày + mô tả).

---

## Đang hoạt động

### ✅ [2026-06-26] TÁCH XHS → 2 nền tảng: xhs (nội địa) + rednote (quốc tế) — fix badge xanh-giả + bỏ toggle global
- **Gốc bug:** 1 nền tảng `xhs` + 1 toggle global `xhs_intl` → login lưu chung 1 profile, badge lẫn cookie 2 domain (guest rednote có web_session→xanh giả; login domain này check domain kia→vàng). User chốt: tách 2 nền tảng (profile/badge/data RIÊNG) + data/rednote riêng + BỎ toggle.
- **MediaCrawler patch:** `store/xhs/xhs_store_media.py` (+`_xhs_leaf()` env `MC_XHS_LEAF` → data/rednote tách data/xhs); `media_platform/xhs/core.py` (env `MC_XHS_PROFILE` → profile `rednote_user_data_dir` riêng). Domain đã sẵn `XHS_INTERNATIONAL`=`MC_XHS_INTL`.
- **web_app.py:** `_xhs_alias(platform)`+`_ap_alias_env` (rednote→mc_platform xhs + set MC_XHS_INTL/PROFILE/LEAF per-crawl); +rednote vào NEN_TANG/NEN_TANG_HO_TRO/COOKIE_AUTH/HOST_KEY/NEN_TANG_LOGIN/_ANH_TRUNG/_ANH_DATA_FOLDER; crawl-builder dùng nt_mc; `_trang_thai_1lan` xhs→(xhs,rednote); **GỠ** `_ap_dung_xhs_intl`+call+2 endpoint `/api/xhs_intl`.
- **kiem_tra_login.py + mo_dang_nhap.py:** HOST/URL domain CỐ ĐỊNH theo platform (xhs=xiaohongshu.com, rednote=rednote.com — KHÔNG còn theo toggle vì 1 lần check nhiều nền không thể vừa 0 vừa 1); +AUTH rednote; `_xhs_dom(page,plat)`→URL[plat]; nhánh xhs→(xhs,rednote). profile `{plat}_user_data_dir` tự ra rednote_user_data_dir.
- **tim_anh.py (preview):** +rednote vào DATA_FOLDER/_PARSER + alias spawn (rednote→--platform xhs + env MC_XHS_*).
- **web/index.html:** +thẻ chọn nền tảng `data-p=rednote` + dropdown lịch sử + NEN_TANG_DS + _NEN_TRUNG + nhãn "nội địa/quốc tế"; **GỠ** toggle UI `#xhsIntlRow`+JS `xhsIntlLoad/Set`.
- **TEST PASS:** `kiem_tra_login xhs rednote`→`{xhs:in, rednote:out}` (2 profile ĐỘC LẬP = fix badge); data rednote→`data/rednote/videos`, xhs→`data/xhs/videos`; profile→`rednote_user_data_dir`; JS index.html `node --check` OK; 4 file py compile OK.
- **CHƯA:** restart web_app để thẻ login RedNote + dropdown hiện (running app dùng list cũ); live-login rednote thật (cần user login QR). ⚠️ **web_app.py/index.html đụng file session kia — commit sớm.**
- **File đụng:** `web_app.py`, `kiem_tra_login.py`, `mo_dang_nhap.py`, `tim_anh.py`, `web/index.html`, `MediaCrawler/store/xhs/xhs_store_media.py`, `MediaCrawler/media_platform/xhs/core.py`.
- **Trạng thái:** ✅ code+verify xong — chờ restart + user duyệt + commit.

### 🟢 [2026-06-26] Fuzzy-merge câu OCR (gộp lỗi-OCR-1-ký-tự, chống tách câu giả) — đụng ocr_text.py (⚠️ WIP session "BLUR ĐỘNG")
- **Việc:** `ocr_text.ocr_dong` merge câu liền kề đang dùng `==` TUYỆT ĐỐI → lỗi OCR lác đác (我们/我门) tạo câu tách giả. Thêm `_giong()` + `_khoang_cach()` (Levenshtein thuần Python, KHÔNG thêm dep) → gộp khi sai 1-2 ký tự TRÊN câu ≥4 ký tự; câu <4 phải trùng tuyệt đối. Tắt: env OCR_FUZZY_MERGE=0.
- **Calibration (đo thật, fix bug):** thử `difflib.ratio≥0.90` TRƯỚC → SAI cho CJK (你好/你妤=0.5, 我们要降薪了/我门要降薪了=0.83 đều <0.90 → không gộp). Đổi sang edit-distance ≤ max(1, len//8). Verify: 4 cặp câu cảnh03 khác nhau→KHÔNG gộp; lỗi-OCR câu dài→gộp; câu ngắn khác (上山/下山)→KHÔNG gộp.
- **⚠️ ĐỤNG `ocr_text.py` (file WIP của session "BLUR ĐỘNG + HỢP NHẤT OCR" — ✅ chờ commit).** Chỉ THÊM 2 hàm + đổi 1 dòng điều kiện merge (164), KHÔNG đụng logic blur động/box. User đã đồng ý làm. Cùng file đang chứa WIP session kia → **NÊN COMMIT SỚM** kẻo IDE đè buffer.
- **File đụng:** `ocr_text.py` (_giong + _khoang_cach + đổi điều kiện merge trong ocr_dong).
- **Trạng thái:** ✅ code + verify đơn vị xong — chờ user commit.

### 🟢 [2026-06-26] GỠ HOÀN TOÀN FunASR (OCR-first đã là mặc định) — đụng localize.py/web_app.py/index.html/pyproject.toml + xoá funasr_asr.py
- **Bối cảnh:** user gửi plan "kiến trúc OCR-first". Đối chiếu code: OCR-first (RapidOCR PP-OCRv5 qua `ocr_text.py`) ĐÃ là mặc định ASR tiếng Trung (`localize.asr_segments`), FunASR đã hạ opt-in. User chốt scope: **gỡ FunASR hoàn toàn**; giữ zh→vi thẳng (không QuickMT); giữ RapidOCR-band ưu tiên; giữ file phẳng.
- **Đang làm:** xoá `funasr_asr.py`; gỡ 2 nhánh dispatch `funasr_ocr`/`funasr` trong `localize.asr_segments` (giữ OCR-first + whisper fallback); gỡ khối tải-ngầm FunASR + 2 endpoint `/api/funasr/{status,start}` + call trong main (`web_app.py`); gỡ banner `#funasrBar` + `funasrPoll` + sửa text tooltip GPU (`web/index.html`); gỡ dep `funasr` (GIỮ torch/torchaudio — F5/OmniVoice/demucs cần) `MediaCrawler/pyproject.toml`.
- **Lưu ý đụng file session khác:** chỉ sửa khối FunASR-riêng (tách biệt vùng che-sub/render/dub WIP). Log-sanitizer tuple (`_LOG_AN/_LOG_THAY/_RAC`) GIỮ (vẫn lọc noise HF của whisper/OmniVoice). plans/*_smoke_import.py còn ref funasr_asr — file test cũ, không sửa.
- **Đã làm:** ✅ xoá `funasr_asr.py`; localize.py gỡ 2 nhánh dispatch (điều kiện OCR-first đổi `eng != "whisper"`) + gỡ `funasr_asr.giai_phong()` trong cleanup; web_app.py gỡ khối tải-ngầm (87 dòng) + 2 endpoint + call main; index.html gỡ banner+poll + sửa 2 tooltip GPU; pyproject gỡ dep `funasr` (giữ torch/torchaudio/soundfile + sửa comment).
- **Verify static:** `ast.parse` localize.py + web_app.py SẠCH; grep `/api/funasr|chay_tai_funasr|_funasr_|funasrBar/Poll` = 0 orphan. Còn sót CHỈ comment lịch sử + tuple lọc-log HF (giữ) + suffix `.funasr.wav` + cache-key env `FUNASR_MAX_SEG` (=`""`, vô hại). **CHƯA boot-test UI** (web_app cần restart để áp; index.html F5).
- **File đụng:** `localize.py`, `web_app.py`, `web/index.html`, `MediaCrawler/pyproject.toml`, xoá `funasr_asr.py`.
- **Trạng thái:** ✅ code xong — chờ user restart MO-WEB + duyệt + commit. (deploy: `uv sync` gỡ gói funasr; model cũ `~/.cache/funasr_models` user tự xoá nếu muốn nhẹ ~2GB.)

### 🟢 [2026-06-24] Thiết lập điều phối đa session + brainstorm cache ASR/TTS
- **Việc:** tạo cơ chế `SESSIONS.md` + thêm rule "đọc mỗi prompt". Đã brainstorm cache ASR/TTS (mới TƯ VẤN, CHƯA code).
- **File đụng:** `SESSIONS.md` (mới), `CLAUDE.md` (+1 rule điều phối), `rule.md` (+1 rule điều phối).
- **Ghi chú:** phần cache (`localize.py` / `web_app.py`) mới ở bước brainstorm, **CHƯA sửa**. Chờ user chạy `/plan`.

### ✅ [2026-06-24] CACHE artifact lossless (localize) — ĐÃ CODE + TEST
- **Việc:** implement cache lossless vi/zh.srt + band.json + _dub.wav (tái dùng khi render LẶP cùng video). Plan ở `plans/260624-cache-artifact-localize/`.
- **File đã đụng:** `cache_artifact.py` (MỚI), `localize.py` (chay: 3 móc HIT/MISS srt+band+dub), `web_app.py` (import + `_ap_dung_cache_settings`/`_cache_sweep_nen` + sweep on-start & after-render + endpoint `/api/cache/clear|set|stats`), `web/index.html` (panel Cài đặt + cacheLoad/Toggle/CapSave/Clear). Test ở `plans/260624-.../_test_cache_artifact.py` (24 PASS) + `_test_cache_integration.py` (21 PASS).
- **Kết quả test THẬT:** re-render cùng video 27s→5s (lossless, byte-identical); đổi giọng→dub MISS+srt HIT; key chống serve-nhầm OK. Chờ user duyệt + commit (CHƯA tự commit).
- **Ghi chú:** chưa restart web_app trên máy đang chạy (route mới cần restart). KHÔNG đụng encode/data/processed.

### ✅ [2026-06-24] Test toàn diện UI + fix Douyin anti-bot + i18n/favicon + điều tra CDP
- **Việc:** Test thật toàn bộ UI (Puppeteer, báo cáo `plans/260624-test-ui-flows/RESULTS.md`) → KHÔNG bug functional. Fix Douyin anti-bot (retry), i18n EN, favicon; điều tra "giữ browser" (CDP).
- **File ĐÃ đụng:** `MediaCrawler/media_platform/douyin/core.py` (retry+backoff search); `web/favicon.ico` (MỚI); `web/lang_en.json`+`lang_en.js` (regen 457→988); `_gen_lang_en.py`+`_lang_overrides.py` (guard UTF-8); `MediaCrawler/config/base_config.py` (4 cờ CDP đọc env, mặc định OFF); `MediaCrawler/tools/cdp_browser.py` (FIX bug connect-existing ws URL thiếu id → 404); `dai_sub_ocr.py` (FIX che dương-tính-giả: OCR dò dải chỉ chấp nhận MÉP DƯỚI ≥60% / trên ≤28%, bỏ vùng giữa — trước blur đè nội dung biển hiệu giữa khung); `web_app.py` (chay_long_tieng: fallback bền khi `_resolve_video` trượt → tìm theo TÊN trong DATA_DIR/PROCESSED_DIR + log path đã thử; fix "Lỗi không tìm thấy video" với tên có #/Hán hoặc DATA_DIR ổ khác).
- **KẾT LUẬN anti-bot:** 风控 Douyin = IP+tốc độ (server-side). Chứng minh: cùng keyword OK khi IP tươi, chặn khi IP bão hoà — ở CẢ no-browser/browser/CDP. CDP-connect (giữ browser) chỉ nâng chất lượng a_bogus/msToken nhưng request VẪN qua httpx cùng IP → KHÔNG vượt IP-rate. "Nhanh + không chặn" chỉ giải triệt để bằng proxy/IP-rotation. Pacing 6/6 OK; CDP env-opt-in để dành cho ai có proxy.
- **FIX REGRESSION đa-session (`/api/files` 500 → File đã tải + lưới Render/Lồng tiếng/Băm RỖNG):** `web_app.py` có 2 hàm TRÙNG TÊN `_nen_tang_tu_path` — dòng 1377 (gốc: parts list→tuple ten,ma cho listing) bị dòng 1521 (session render-grouping thêm sau: full path→1 chuỗi) ĐÈ → `liet_ke_file` truyền list vào hàm mong path → `abspath(list)` TypeError → 500. FIX: đổi tên hàm mới + caller (3946) → `_nen_tang_seg_tu_path`; giữ nguyên gốc cho listing. Verify: /api/files 500→trả 129 video, UI hiện đủ thumbnail. ⚠️ session render-grouping lưu ý KHÔNG đặt lại trùng tên.
- **FIX Gemini web dịch trả 0 câu (rơi sang Google):** `dich_gemini_web.py` launch headless THIẾU cờ ổn định → Gemini (SPA nặng) "Target crashed" → 0/N câu. KHÔNG phải do chưa login (Gemini cho ô nhập ẩn danh — dịch được không cần login). FIX: thêm `--disable-dev-shm-usage --disable-gpu --no-sandbox --disable-features=IsolateOrigins,site-per-process`. Verify: headless dịch 3/3 câu đúng (你好世界→Chào thế giới...). Hiệu lực ngay (subprocess). ⚠️ phụ: profile Gemini dùng path tương đối `MediaCrawler/browser_data/gemini_user_data_dir` (không theo MC_BROWSER_DATA_DIR) — nhưng vì KHÔNG cần login nên không nghiêm trọng.
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit (CHƯA tự commit).

### ✅ [2026-06-24] Nút Đăng nhập/Đăng xuất nền tảng trên thẻ login — XONG
- **Việc:** thêm nút Đăng xuất (khi platform 'in') / Đăng nhập (khi 'out') ở đáy mỗi thẻ login (tab Cào). Logout = xoá cookie profile nền tảng → lần sau login lại (QR).
- **File ĐÃ đụng:** `web_app.py` (helper `_logout_nentang` + endpoint `POST /api/logout_nentang`), `web/index.html` (renderLcards thêm nút + JS `dangXuatNenTang` + CSS `.lcard` thêm flex-wrap + rule `.lcard .lcbtn{order:4;flex:0 0 100%}` cho nút xuống đáy full-width). KHÔNG đụng vùng cache/Kira/localize.
- **Verify thật (Puppeteer):** nút render đúng theo trạng thái (dy/bili/xhs='in'→Đăng xuất; tt='out'→Đăng nhập; yt='na'→không nút); endpoint test an toàn backup→logout dy→cookie xoá+trang_thai='out'→KHÔI PHỤC→dy='in' (KHÔNG mất phiên user). Restart MO-WEB, route live.
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit.

### ✅ [2026-06-24] POC Kani TTS Vie (DỪNG) + gỡ dep supertonic
- **Việc:** (1) POC Kani-TTS-Vie trên laptop RTX 3050 → **DỪNG**: NanoCodec cần NVIDIA NeMo, mà `nemo_toolkit[tts]` build FAIL trên Windows (thiếu MSVC C++ Build Tools/cdifflib) + cài lean cascade vô tận → KHÔNG deploy được cho app Windows khách. (2) Gỡ dep **supertonic** khỏi `MediaCrawler/pyproject.toml` + `uv.lock` (3 chỗ, `uv lock --check`=0 OK) — supertonic vốn đã là code chết (localize.py không còn dispatch; chỉ còn 2 chuỗi ghost `||"supertonic"` trong index.html, KHÔNG đụng vì index.html đang có WIP session logout).
- **File ĐÃ đụng:** `MediaCrawler/pyproject.toml`, `MediaCrawler/uv.lock`, `SESSIONS.md`. (Laptop: thư mục scratch `C:\Users\vannh\caovideo\kani_poc` — chờ user duyệt xoá.)
- ⚠️ **CLOBBER:** toàn bộ code Kira AI TTS tôi tích hợp trước đó (localize.py/web_app.py/xu_ly_chon.py/kira_keys.py/index.html) đã **BIẾN MẤT** (không commit, git vẫn f63eff0) — nghi IDE ghi đè buffer cũ hoặc session khác revert. User đã yêu cầu gỡ Kira nên coi như xong.
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit.

### ✅ [2026-06-24] FIX dub "không tìm thấy video" khi data ở ổ đĩa khác (D:)
- **Việc:** tab Lồng tiếng báo "không tìm thấy video" dù video tồn tại — KHI data ở ổ KHÁC app (D:, mà app khuyến nghị). Gốc: `chay_long_tieng` check cứng `video.startswith(THU_MUC_GOC)` (chỉ ổ app) → path D: bị từ chối.
- **Fix:** `web_app.py` `chay_long_tieng` dùng `_resolve_video(rel)` (như /video,/thumb — commonpath, bắt ValueError cross-drive) thay startswith. + `web/index.html` `dubLoad` escape `esc(v.p)` trong option value (phòng path có `"`).
- **Verify thật:** subst ổ ảo Y: + file thật → OLD startswith=False (BUG), NEW commonpath=True (FIX); same-drive C: dub vẫn chạy (no regression). Restart MO-WEB.
- **Trạng thái:** ✅ XONG — chờ user xác nhận (data của bạn/khách có ở ổ D: không?) + commit.

### 🟢 [2026-06-24] Che chữ Trung ĐỘC LẬP + fix render robustness (đang làm)
- **Việc:** (1) "Che chữ Trung" nay chạy ĐỘC LẬP — bật che mà KHÔNG cần phụ đề/dịch/lồng tiếng → vẫn XOÁ sub Trung gốc (blur dải), **GIỮ tiếng gốc**, 1 encode nhanh (không ASR). Trước đây che bị bỏ qua nếu không có localize → "vẫn phụ đề". (2) Fix decode body `errors="replace"` (body lạ làm 500 rỗng → "Không thêm được"). (3) CHƯA làm: output sang folder mặc định riêng (chờ user chọn folder).
- **File ĐÃ đụng:** `xu_ly_chon.py` (cờ `co_che_doc_lap` + nhánh dò dải `dai_sub.detect_blur_band`→`burn_phude(vi_srt=None,audio_path=None,blur_band)`), `localize.py` (`burn_phude` đường đơn-giản: chỉ burn sub khi có `vi_srt`, bỏ `-vf` rỗng — cho che-only), `web_app.py` (decode errors="replace").
- **Verify thật:** clip 18s có sub Trung → render che+mirror (không phụ đề/dịch/lồng tiếng) → output có **video+audio**, frame: sub Trung BLUR + lật ngang + giữ tiếng. Render add UI = 200 ok (không tái hiện "Không thêm được" trên dev).
- **Trạng thái:** 🟢 đang làm (chờ user chốt folder mặc định cho Part 2).

### ✅ [2026-06-24] Modal "render lại": DÙNG LẠI dịch/lồng tiếng vs RENDER TỪ ĐẦU — XONG
- **Việc:** video ĐÃ có cache dịch → modal cho user chọn (a) dùng lại bản dịch/giọng (nhanh, chỉ đổi logo/watermark/khung) = cache HIT; (b) render từ đầu (cờ `dich_lai` → localize BỎ QUA lookup srt+dub, dịch/lồng mới, vẫn LƯU ĐÈ).
- **File ĐÃ đụng:** `cache_artifact.py` (+`co_srt(video)` glob srt_<vhash>_*), `localize.py` (param `dich_lai` + gate LOOKUP srt+dub, GIỮ key để vẫn lưu đè + argparse `--dich-lai`), `xu_ly_chon.py` (argparse `--dich-lai` → chay), `web_app.py` (endpoint `POST /api/cache/co` + `chay_long_tieng`/`_lenh_xu_ly` thêm `--dich-lai` khi opts `dich_lai`), `web/index.html` (helper `hoiCache()` modal + chèn vào `xlRun`+`dubStart`). ⚠️ Đụng code SESSION CACHE (commit f63eff0) — CHỈ thêm nhánh bypass, KHÔNG phá HIT/MISS/key.
- **Verify thật (Puppeteer):** cached video + phụ đề → modal hiện 3 nút; "Render từ đầu"→`/api/xu_ly` gửi `dich_lai:true`; "Dùng lại"→`dich_lai:false`; video KHÔNG cache→KHÔNG modal, render thẳng. 0 JS error. Endpoint co_srt: tìm đúng video có cache. Restart web_app rồi test.
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit.

### ✅ [2026-06-24] Thêm VIDEO CỦA USER (ngoài data cào) để render — XONG
- **Việc:** tab Render thêm nút "➕ Thêm video của tôi" → mở hộp thoại chọn FILE native (multi-select) → đăng ký file ngoài DATA_DIR vào `_bam_serve_files` (cơ chế đã có của băm/preview5s) → thêm vào lưới Render, tự tick. App LOCAL nên KHÔNG upload (file nặng); output về `processed_videos/khac/`.
- **File ĐÃ đụng:** `chon_thu_muc.py` (+chế độ `--files` askopenfilenames, giữ folder-mode cũ), `web_app.py` (endpoint `POST /api/them_video` spawn picker → register `_bam_serve_files` → trả videos), `web/index.html` (`xlVideoThem[]` + `xlThemVideo()` + merge trong `xlLoadVideos` để sống qua 🔄 + nút). KHÔNG đụng cache/encode.
- **Verify thật:** picker `--files` mở không crash; Puppeteer: nút + `xlThemVideo`/`xlVideoThem` defined, 0 JS error; render file ngoài = cơ chế `_bam_serve_files` đã proven (băm/preview5s). Picker là thao tác người dùng (không auto-test chọn file được).
- **Trạng thái:** ✅ XONG — chờ user test chọn file thật + commit.

### ✅ [2026-06-24] Lồng tiếng: ĐẶT TÊN FILE XUẤT (thay vì <gốc>_longtieng.mp4) — XONG
- **Việc:** tab Lồng tiếng thêm ô "Tên file xuất (tuỳ chọn)" → video lồng tiếng ra theo tên user đặt thay vì tên gốc tiếng Trung. Trống = giữ mặc định.
- **File ĐÃ đụng:** `web_app.py` (`chay_long_tieng`: parse `params['ten_xuat']` → sanitize basename + bỏ ký tự cấm Windows + ép .mp4 → `out_tuy_chon`; worker sau wait `os.replace(out, out_tuy_chon)` dùng biến `final_out` để KHÔNG đụng closure `out`), `web/index.html` (input `#dubTenXuat` + gửi `ten_xuat` trong `dubStart`).
- **Bug đã fix trong lúc làm:** gán lại `out=` trong worker (closure) → UnboundLocalError → đổi sang `final_out`.
- **Verify thật:** dub video gốc + ten_xuat="QA ten xuat 2" (cache HIT 12s) → output `QA ten xuat 2.mp4` (7MB) đúng tên; input UI hiện, 0 JS error. (Đã dọn file test.)
- **+ XUẤT CẢ FILE ÂM THANH:** dub giờ tách thêm `<tên>.mp3` (giọng lồng tiếng) CÙNG TÊN video (`chay_long_tieng` worker: ffmpeg `-vn -c:a libmp3lame 192k` từ video output; localize đã xoá wav tạm nên tách lại từ video). Verify: dub → ra CẢ `QA audio test.mp4` (7MB) LẪN `QA audio test.mp3` (394KB). Luôn xuất mp3 mỗi lần dub.
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit.

### ✅ [2026-06-24] rdModal (render từ File đã tải) THIẾU option → bổ sung khớp tab Render — XONG
- **Việc:** modal render mở từ "File đã tải" thiếu so với tab Render: chỉ có checkbox Watermark (KHÔNG ô nhập chữ → vô dụng), thiếu Nén câu tràn/Dịch thủ công/Chủ đề. rdRun ĐỌC `rdMaxSp/rdDichTC/rdChuDe/rdWmText/rdWmChay` nhưng HTML không có (fallback `||{}` → mất option).
- **File ĐÃ đụng:** `web/index.html` — rdModal HTML thêm `rdWmText`+`rdWmChay` (watermark CHỮ), `rdMaxSp` (nén câu tràn), `rdDichTC` (dịch thủ công), `rdChuDe` (chủ đề); `rdRun` build `text_wm` từ rdWm+rdWmText+rdWmChay (bỏ watermark-ảnh `watermark:false`) + thêm modal cache `hoiCache` (render từ File-đã-tải cũng hỏi dùng-lại/từ-đầu).
- **Verify thật (Puppeteer):** rdModal có đủ input; e2e render → `/api/queue_add` opts có `text_wm:{text,chay}`, `max_speed:1.8`, `chu_de:"phim hài"`, `watermark:false`; 0 JS error. (rdLogo cố ý không thêm — logo qua editor "🎯 Vẽ vùng mờ/chèn logo".)
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit.

### ✅ [2026-06-24] FIX CRITICAL che-sub bỏ sót phụ đề GIỮA khung (split-screen) — XONG
- **Việc:** test deploy (plans/260624-test-deploy-toandien) phát hiện render video Douyin "2 ảnh chồng, sub ở dải GIỮA ~50%" → sub Trung KHÔNG bị che (lộ nguyên). REGRESSION từ fix `dai_sub_ocr` sáng nay (giới hạn chỉ đáy ≥60% → loại sub giữa).
- **File ĐÃ đụng:** `dai_sub_ocr.py` — thay "chỉ đáy" bằng: gom chữ CJK theo 24 dải, **ƯU TIÊN dải có chữ ĐỔI giữa frame** (≥2 chuỗi khác = sub thật, mọi vị trí kể cả giữa); KHÔNG có dải đổi → fallback mép dưới ≥60%/trên ≤28%. Phân biệt sub (đổi) vs biển-hiệu-tĩnh (không đổi → loại). Giải CẢ 2 lỗi.
- **Verify thật:** dai_sub_ocr 536626 dò 42.8%–55.2% (giữa, đúng) thay 86-94%; re-render → frame: sub Trung giữa ĐÃ BLUR, mặt sắc nét. ⚠️ ca DISNEYLAND-static chưa regression (cần video gốc user).
- **Trạng thái:** ✅ XONG — chờ user duyệt + commit + (gửi lại video DISNEYLAND để regression 100%).

### 🔵 [2026-06-24] UI: khu Cào theo mockup ViralCrawl v2 (nút mô tả phụ + header wizard 5 bước) — ĐANG LÀM
- **Việc:** user chọn restyle "chỉ khu Cào video" theo mockup. Chốt 2 điểm: (1) nút hành động `.cao-action` có dòng mô tả phụ; (2) header wizard 5 bước (Đăng nhập→Chọn nền tảng→Cào→Xem trước→Render) ở `.top .t`.
- **File ĐÃ đụng:** `web/index.html` (CSS .b2/.wiz + markup nút cao-action + wizard trong .top + JS wizGo/wizSync + 1 dòng trong nav onclick). KHÔNG đụng route/backend.
- **Verify Chromium:** 0 console error; wizard 5 bước hiện, bước 3 sáng khi ở tab Cào, click bước 5→sang tab Render (cur=5, 4 done); 3 nút có mô tả phụ. Wizard điều hướng + scroll tới login/nền-tảng/form. Responsive: ẩn chữ <1180px, ẩn hẳn <760px.
- **+ Render (xuly) & Lồng tiếng (dub):** thêm stepper trong trang `.pwiz` 4 bước. Render: Chọn video→Cấu hình→Render→Hoàn tất (click cuộn tới #xlGrid/#xlCfgBox). Dub: Chọn&Cấu hình→Giọng&Dịch→Giọng clone→Hoàn tất + đánh số mục "📋 1. Thông tin video & cấu hình" / "✨ 2. Giọng clone (nếu có)". JS `pwizSet/pwizClick`. Render vốn đã ~95% giống mockup; Dub reorg theo mockup.
- **KHÔNG làm (báo user quyết):** mockup Dub có "3. Xem trước giọng" (phát mẫu giọng) + sliders/checkbox xử lý âm thanh (Chuẩn hóa/Xóa tạp âm/Giảm vọng/Lọc gió) — CHƯA có backend / sliders từng gỡ ([[goc-vol-audio-ui]]); không tạo UI giả.
- **Trạng thái:** ✅ XONG (wizard 3 tab + nút mô tả + đánh số dub) — chờ user duyệt + commit.

### 🔵 [2026-06-25] Dub: XÂY THẬT "Xem trước giọng" + sliders/lọc âm (user yêu cầu "xây cả 2") — ĐANG LÀM
- **Feature 1 — Nghe thử giọng:** `dub_preview.py` (mới) synth 1 câu tiếng Việt qua localize (edge/piper/f5-clone) → endpoint GET `/api/dub/preview` (web_app, trả wav, chặn khi đang dub). UI panel "🔊 3. Xem trước giọng" (web/index.html) + hàm `dubPreview()`. VERIFY endpoint edge: wav 131KB thật.
- **Feature 2 — Âm lượng + lọc âm:** localize.py thêm `--giong-vol` (scale track giọng) + `--af` (normalize=loudnorm / denoise=afftdn / wind=highpass áp lên audio cuối); nền dùng `--goc-vol` sẵn. web_app `chay_long_tieng` đọc body `giong_vol`/`nen_vol`/`af`. UI: `<details>` "Tùy chọn nâng cao" (2 slider + 3 checkbox), dubStart gửi param.
- **File đụng:** `dub_preview.py` (mới), `localize.py` (argparse+chay: giong_vol/af_loc + scale + post-filter), `web_app.py` (preview endpoint + chay_long_tieng cmd), `web/index.html` (UI dub + JS).
- **KHÔNG làm:** "Giảm tiếng vọng" (dereverb) — ffmpeg KHÔNG có filter dereverb thật → bỏ, không làm vỏ rỗng (chỉ 3 checkbox thật).
- **VERIFY THẬT:** (1) preview endpoint edge → wav 131KB; (2) Chromium: UI đủ element, body gửi `giong_vol:80/nen_vol:25/af:[normalize,denoise,wind]`, preview phát audio, 0 console error; (3) ffmpeg chain `highpass,afftdn,loudnorm` hợp lệ; (4) **dub THẬT 536626** → log "🔊 Âm lượng giọng" + "🎚 Lọc âm" + ra file. 
- **Trạng thái:** ✅ XONG cả 2 feature (verify end-to-end) — chờ user duyệt + commit.

### ✅ [2026-06-25] Dub: GỠ "Xem trước giọng" + THÊM đổi tên giọng (user yêu cầu)
- User: "xoá phần xem trước giọng, tôi nhớ là có thể đổi tên giọng nói mà". → UI đặt-tên-giọng (giongTen/giongCards) đã bị gỡ khỏi HTML trước đây (còn JS mồ côi giongLoad/giongFeed).
- **Gỡ:** panel "3. Xem trước giọng" + hàm `dubPreview` + endpoint `/api/dub/preview` + file `dub_preview.py` (xoá hẳn).
- **Thêm đổi tên giọng clone:** ô `dubCloneTen` (đặt tên khi tải) → dubCloneUpload dùng; nút "✏ Đổi tên" → `dubCloneRename()` → endpoint mới `/api/giong_rename` (chỉ giọng trong CLONE_DIR; sanitize tên + giữ đuôi + dời sidecar .txt/.used; cache hash giữ nguyên).
- **Verify:** Chromium prevPanel/dubPreview GONE, ô tên + nút Đổi tên có, 0 console error; endpoint rename test thật "Giọng QA của tôi.wav" OK.
- **File đụng:** `web/index.html`, `web_app.py`; xoá `dub_preview.py`.
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] FIX 2 bug Render: thư mục lưu reset + watermark khách không áp
- User: "nơi lưu gốc trong render bị đổi về lại từ đầu... watermark của người dùng không chạy được".
- **Bug 1 (out_dir reset):** `xlOut` KHÔNG được lưu (xlBrandSave chỉ lưu wm text/chạy/logo) → reload mất → về "cạnh video gốc". FIX: xlBrandSave lưu `vc_out_dir`; xlBrandLoad khôi phục xlOut; picker + nút ✕ gọi xlBrandSave. Backend `--out-dir` vốn đã dùng đúng (whitelist).
- **Bug 2 (watermark khách):** text_wm CHỈ gửi khi tick checkbox `xlWm` + có chữ; checkbox xlWm KHÔNG được persist (chỉ chữ) → reload mất tick → không áp. FIX: điều kiện text_wm chỉ cần **xlWmText có chữ** (bỏ yêu cầu tick) ở CẢ Render tab (2976) + modal (4527); xlBrandLoad tự tick xlWm khi có chữ. (FREE: Loha Tech + watermark khách CẢ HAI hiện — backend đã đúng.)
- **File đụng:** CHỈ `web/index.html` (KHÔNG đụng web_app.py/localize.py của session khác). KHÔNG cần restart (HTML đọc mới mỗi request; user Ctrl+F5).
- **Verify Chromium:** out_dir giữ qua reload; text_wm áp khi chỉ gõ chữ (không tick); 0 console error.
- **+ Watermark khách CHẠY trên FREE (user chọn):** `web_app.py` khối FREE watermark (~2225, KHÁC vùng gating Banmai ~3845 của session khác) trước ÉP watermark khách `chay=False`+góc tr (tránh chồng Loha). User chọn "cho vanhong chạy luôn" → bỏ ép, tôn trọng tick chữ-chạy (wms=[loha, _uw nguyên]). RESTART rồi.
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] Gợi ý engine TTS theo cấu hình máy (Render + Lồng tiếng)
- User: "kiểm tra cấu hình máy → gợi ý engine. máy yếu Edge/Banmai-Piper; máy mạnh có GPU → model nặng chất lượng".
- **Backend:** `web_app.py` `_may_goi_y()` (sau dub_engines) — CPU (os.cpu_count) + RAM (ctypes GlobalMemoryStatusEx, không cần psutil) + GPU (`_co_nvidia` sẵn) + f5 cài chưa → phân loại: GPU→f5(clone)/piper; CPU≥4+RAM≥7.5→piper(Banmai); yếu→edge. Endpoint GET `/api/may_goi_y` (cạnh dub/engines). Cache _MAY_CACHE.
- **Frontend:** `web/index.html` note `#xlGoiY` (Render, trước xlEdgeWrap) + `#dubGoiY` (Dub, trên dubEngineNote); hàm `mayGoiY()`+`mayApply()` (nút "Áp dụng" set dropdown); gọi ở nav handler tab xuly+dub.
- **Verify:** /api/may_goi_y máy dev = 4 nhân/15.9GB/không GPU → gợi ý piper(Banmai) đúng; UI Render+Dub hiện note + nút Áp dụng (xlTts→piper), 0 console err.
- **File đụng:** `web_app.py` (vùng MỚI, khác session khác), `web/index.html`. RESTART rồi.
- **Trạng thái:** ✅ XONG.

### ✅ [2026-06-25] Cắt giọng MẪU clone khi quá dài (tránh mẫu >15s làm F5 chậm/kém)
- User: "thêm script cắt giọng clone tránh giọng mẫu quá dài".
- **Script MỚI `cat_giong_clone.py`** (untracked — DỄ MẤT, commit): `cat(inp,out,target=12,maxs=15)` — ngắn (≤15s) giữ nguyên chỉ chuẩn hoá mono24k; DÀI → bỏ lặng đầu (silenceremove start) + lấy 12s liền (fallback -t 12 nếu silenceremove lỗi). + `thoi_luong()` qua ffprobe. tim_exe bundle-aware.
- **Nối vào `/api/upload_ref`** (web_app.py): thay convert thẳng = `cat_giong_clone.cat` (fallback convert nếu script lỗi) → mẫu upload >15s TỰ cắt ~12s. Response thêm `giay`+`cat`; log "(cắt 28s→12s)". `web/index.html` dubCloneUpload hiện "(12s — đã cắt cho clone tốt hơn)".
- **Verify:** script 30s→12s; API upload 28s → file lưu 12.0s, resp cat=True. RESTART rồi.
- **Trạng thái:** ✅ XONG.

> ⚠️ NHIỀU file mới untracked (cat_giong_clone.py, dub_preview.py đã clobber 1 lần...) + sửa rải rác — **KHUYẾN NGHỊ COMMIT** để khỏi mất.

### 🔵 [2026-06-25] Redesign premium THEO MOCKUP — trang Cào làm chuẩn (page-by-page) — ĐANG LÀM
- User chốt: làm TỪNG TRANG, Cào trước làm chuẩn → duyệt → nhân sang Render+Dub. Giữ nguyên chức năng.
- **Đã làm:** `.lcard` (web/index.html CSS) đổi từ viên ngang gọn → **thẻ TO kiểu mockup** (grid: logo trái + tên + dòng trạng thái + badge phải + nút Đăng nhập/Đăng xuất full-width đáy, bo 16px, glass). `_bs` nút chỉnh padding. `.lcards` gap 14px. Verify Chromium: render đúng, 0 console error.
- **Chưa làm (chờ user duyệt hướng):** crawl section đổi textarea→ô tìm 1 dòng + nút Tìm; platform cards→dropdown+icon nhỏ (rủi ro rewire JS); TTS engine cards row (thuộc Dub). Stats/wizard/action-panel đã sẵn khớp.
- **Trạng thái:** chờ user duyệt style trang Cào trước khi nhân sang Render + Lồng tiếng.

### ✅ [2026-06-25] FIX BUG: video Trung user upload (tên không có chữ Hán) bị đoán nhầm tiếng Việt → không lồng tiếng
- **Gốc:** localize.py dò nguồn qua tên file (`_co_chu_han`) → không có Hán + không ở folder `data/<nền tảng>` → `src_lang=tgt(vi)` → tắt dịch/lồng tiếng. ASR dò ngôn ngữ NÓI (`_do_ngon_ngu_noi`) TRƯỚC ĐÂY chỉ chạy khi nền tảng=zh → video user tự upload (không nền tảng) không bao giờ được dò.
- **Fix:** khi `lam_long_tieng` + tên không có Hán → LUÔN dò ngôn ngữ NÓI thật (whisper tiny). Nói tiếng Trung → ép `src_lang=zh` (bật lồng tiếng dù tên Việt); nói khác → giữ logic tránh đè 2 giọng.
- **File đụng:** `localize.py` (khối dò src_lang). Áp CHO CẢ Render + Dub (đều gọi localize.chay). KHÔNG cần restart (localize chạy subprocess mỗi lần).
- **Verify:** copy video Trung 536626 → tên "video cua toi khong co chu han.mp4" → _co_chu_han=False, _do_ngon_ngu_noi='zh', _la_tieng_trung=True → bật dub.
- **Trạng thái:** ✅ XONG — chờ user test render thật.

### ✅ [2026-06-25] Tắt "Lật ngang" MẶC ĐỊNH (video user upload bị lật gương → chữ ngược)
- User render video Trung-học (nhiều chữ trên màn hình) → bị lật gương toàn bộ (tiêu đề/thoại đọc ngược) do "Lật ngang" mặc định BẬT.
- **Fix:** bỏ `checked` 3 chỗ trong web/index.html: `xlMirror` (Render), `arMirror` (auto-render), `rdMirror` (modal). Giờ mặc định KHÔNG lật; ai cần né bản quyền tự tích. xlRestoreCfg KHÔNG khôi phục giá trị checkbox nên không override.
- **Trạng thái:** ✅ XONG — user render lại (flip off) sẽ hết ngược.
