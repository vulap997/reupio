- luôn yêu cầu cladue đọc kỹ rule và memory kết hợp skill đã build trong dự án trước khi tiến hành(implement update, maintain dự án) luôn yêu cầu check test thật kỹ sau khi tiến hành để phát hiện bug và fix đến khi nào
hết bug và hoạt động hoàn hảo thì báo cáo lại(cấm không được bịa kết quả test)
không được Thay đổi hay phá hỏng những logic khác đang hoạt động bình thường(nhớ kỹ)
- Tích hợp MCP: codegraph néu không có thì tải(đọc và tìm kiếm thông tin trong codebase)
\
- sau khi phát hiện ra lỗi, lưu lại vào memory để không tái phạm lần sau
\\
- TUYỆT ĐỐI KHÔNG tự ý revert/hoàn tác code đang có: không chạy `git checkout HEAD -- <file>`, `git reset`, `git stash`, hay `cp`/ghi đè lên file đang có thay đổi CHƯA commit. Nếu cần tách thay đổi để commit cho gọn thì PHẢI hỏi user trước, không tự "dọn" bằng backup/restore/checkout.
- TRƯỚC khi sửa hoặc commit 1 file: KIỂM TRA xem có SESSION KHÁC (cửa sổ/editor/Claude khác) đang code chức năng khác trên file đó không. Dấu hiệu: file đang `M` với nội dung LẠ mình không tạo (hàm/route/biến mới lạ, diff to bất thường so với thay đổi mình vừa làm). Nếu có → KHÔNG đụng vào phần đó, BÁO user "phát hiện code lạ trong file, có phải bạn đang làm ở session khác?" rồi CHỜ xác nhận. Chỉ commit đúng phần mình sửa, tuyệt đối không đè/mất WIP của session khác.
- nếu có thể dùng lại thì dùng lại hàm sẵn có, kiểm tra xem các chức năng khác(như render trong tab render đầy đủ nhưng trong video đã tải lại thiếu.Đã sửa 1 thứ gì đó có thể ảnh hưởng logic còn lại thì phải sửa cả 2.Hoặc phải đề xuất, hỏi)
- ĐIỀU PHỐI ĐA SESSION (bổ trợ cho rule "kiểm tra session khác đang code" ở trên): MỖI lần nhận prompt, ĐỌC `SESSIONS.md` trước để biết session khác đang đụng file nào. Trước khi sửa 1 file, kiểm tra nó có trong mục "🟢 ĐANG LÀM" của session khác không — nếu có thì HỎI user trước. Khi bắt đầu sửa code thì GHI/cập nhật mục của mình (việc + danh sách file + trạng thái) vào `SESSIONS.md`. TUYỆT ĐỐI không xóa/sửa mục của session khác.NHỚ KHÔNG ĐƯỢC QUÊN GHI VÀO SESSION.md
-kiểm tra , test toàn bộ luồng bằng UI để chắc chắn hoạt động thay vì test back end
- COMPONENT HOÁ + ĐỒNG BỘ (BẮT BUỘC): mọi chức năng/UI dùng ở NHIỀU NƠI (vd panel Render ở tab Render + ở "File đã tải" + modal render-1-video; các bộ lọc; picker giọng; bộ tuỳ chọn lồng tiếng...) PHẢI tách thành 1 COMPONENT/HÀM DÙNG CHUNG để có thể ÁNH XẠ – DÙNG LẠI – ĐỒNG BỘ. CẤM copy-paste mỗi nơi 1 bản rồi sửa 1 nơi quên nơi khác (vd tab Render đủ option nhưng modal "video đã tải" lại thiếu). Nếu HIỆN ĐANG có nhiều bản trùng → khi sửa 1 bản PHẢI sửa HẾT các bản còn lại trong CÙNG lần (hoặc đề xuất gộp thành component rồi HỎI user). Khi thêm/đổi option render/lồng tiếng/cào/lọc... PHẢI rà soát MỌI nơi dùng nó (Render tab, File đã tải, modal render-lại, auto-render, băm...) để đồng bộ, không để lệch.
- tìm roost cause chứ không chữa triệu chứng. đề xuất các ý tưởng mới để giải quyết vấn đề đang làm. phản bác, phản biện khi tôi đưa ra yêu cầu không đúng nghiệp vụ, chưa tối ưu
- KHI SỬA 1 CHỖ CÓ CHỖ KHÁC LIÊN QUAN → GỢI Ý HỎI: nếu sửa/đổi 1 thứ mà thấy có chỗ KHÁC liên quan (cùng logic/luồng/component, bug tương tự ở nơi khác, hoặc thay đổi này kéo theo nơi khác nên đổi cho đồng bộ) → CHỦ ĐỘNG gợi ý "bạn có muốn sửa thêm <chỗ đó> luôn không?" để user quyết định phạm vi, KHÔNG tự ý làm hết cũng KHÔNG lờ đi. (Bổ trợ rule component-hoá: không chỉ component dùng-chung mà cả chỗ liên quan gián tiếp.)
- UI ĐẾM/HIỂN THỊ DANH SÁCH PHẢI NHẤT QUÁN (BẮT BUỘC — đã sai NHIỀU LẦN: tab Render "đã render 2" vs File đã tải "22"; re-render không thấy bản mới; video render không hiện): (1) MỌI nơi đếm/lọc/đánh dấu CÙNG 1 loại dữ liệu (vd "đã render" ở tab Render + File đã tải + dashboard; gốc vs bản render) PHẢI dùng CHUNG 1 hàm phát hiện (1 nguồn sự thật) → SỐ LIỆU GIỮA CÁC TAB PHẢI KHỚP. (2) Khớp file theo KHOÁ ỔN ĐỊNH = TÊN FILE (basename), KHÔNG theo cả đường dẫn → output bị MOVE sang folder thể loại/processed/out-dir vẫn nhận đúng video gốc. (3) Media (video/thumbnail) PHẢI cache-bust theo mtime: `/video?p=...&v=<mtime>` — re-render đè CÙNG TÊN file mà không có cache-bust → trình duyệt hiện BẢN CŨ (tưởng "render không chạy"). (4) Liệt kê nhiều thư mục = quét RỒI dedup theo abspath file (không chỉ dedup theo folder) → tránh 1 file đếm 2 lần qua 2 base chồng nhau. (5) Sau khi đổi logic render/file/liệt-kê: TỰ KIỂM trên UI (theo rule test-UI) rằng output MỚI hiện ngay + số đếm khớp mọi tab + không nhân đôi/ẩn nhầm.